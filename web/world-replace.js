// story-world-v2/web/world-replace.js
// ★★★leg103（A3）：**「覆盖现存世界」的告知文案与事实归一，整族搬出接线层**。
//
// 为什么会有这个文件（原来的病，逐条都在源码里）：
//   `bus['init-world']` 当年**一声不响就覆盖现存世界**——没有确认框、没有存在性检查，
//   而同文件的「回到此步」**有**确认框（同一份代码里两种标准）。更贵的是**顺序**：
//   起根（读整本书，真账实测 170–490 秒）与抽取都跑在 `writeHotMeta` **之前**，
//   于是用户**在覆盖真正发生前一句提示都看不到**，只看见状态条慢慢跑了几分钟。
//   ⇒ leg40b 立了那道闸：① 加确认框 + 存在性检查；② 闸放在**最前面**（任何一次模型调用之前）。
//
// ★为什么现在搬出来（leg103）：
//   ① 本模块是**纯文案 + 纯归一**（零 DOM、零 Node 内建、零引擎依赖）⇒ 它本来就不该住在接线层里；
//   ② 「导入恢复」是**同等破坏力**的操作，却一直**没有**这一问（用户令"我捋一遍"时被捋出来）
//      ⇒ 这一族要有**两个**入口（初始化 / 导入），两处文案必须同一口径 ⇒ 收在一处才不会各写各的；
//   ③ 接线层在 `test/web-view-state-layout.test.js` 里有条硬锁（`web/index.js` 行数 `< 3100`，
//      "接线层不许回涨"）；把一族搬走是本仓反复用过的治法（leg72/73/78/79/80/82 六次）。
//
// ★★★leg103 同批纠出一处**假承诺**（本笔最值钱的一条）：
//   旧文案那句「换掉前会自动给当前状态拍一份快照（换错了可以再退回来）」**不成立**——
//   `requestSnapshot` 唯一的调用点是 `writeHotMeta` 末尾（`web/hot-ledger.js:129`），
//   而初始化是**先抽取、后 `writeHotMeta`** ⇒ 那份快照拍到的是**刚抽出来的新世界**，不是被换掉的那个。
//   用户照那句话去退，退回来的是新世界 —— 也就是说，**唯一一条"换错了能回来"的退路其实不存在**。
//   它是怎么躲过判据的：`test/init-overwrite-guard.test.js` 只断言**文案里有没有这句承诺**，
//   从不断言**承诺的行为真的发生了**（"判据在、判的却是文案不是行为"——本仓"空绿"家族的又一员）。
//   ⇒ 两条路里选了"让行为变成真的"：初始化在确认之后、**任何改动与调用之前**先拍一份自保快照
//     （`reason: '换掉前自保'`，与 `restoreSnapshot` 的"恢复前自保"同一形状）。
//     导入那条路**不拍**（它带的是文件里那份世界）⇒ 它照实说"不会替你先拍"。
//
// 分层：编排层（与 `view-state.js` / `memory-store.js` / `snapshot-store.js` 同一层）。
// 依赖方向：`web/index.js → web/world-replace.js`（叶子，本文件不 import 任何东西）。

// ---------- ★leg40b（I-1）：**覆盖现存世界的守门**（用户点单修；这条会真丢用户数据）----------
// 判据（都能机械核，见 `test/init-overwrite-guard.test.js`）：
//   · **存世界在 ⇒ 必问**（不按"推进过才问"分层：tick 0 的世界也是用户的账）；
//   · **问了才烧调用**——取消路径**一次模型调用都不许发**；
//   · **什么都不做**——取消后盘上/内存里的世界逐字节原样；
//   · **没窗口**（`window.confirm` 不存在，Node/vm 里）⇒ 照旧放行，**绝不因为"问不出来"就把人卡死**。
//   · 文案只说**用户看得懂的事实**（名字 / 推进到第几轮 / 多少条名号 / 被换掉的时刻），
//     不出现 tick/entity 这类引擎词（A-3），也不评价世界好坏。

/** 把热账归一成「要被换掉的那个世界」的事实面；没有世界就返回 null（= 没有可丢的东西）。 */
export function worldToBeReplaced(world) {
    if (!world || typeof world !== 'object') return null;
    const tick = world?.meta?.tick;
    return {
        name: String(world?.context?.world || '').trim() || '未名世界',
        tick: Number.isFinite(tick) ? tick : null,
        entities: Array.isArray(world?.entities) ? world.entities.length : 0,
        savedAt: String(world?.savedAt || '').trim(),
    };
}

/** 覆盖确认的文案（**玩家视线内的文本**：人话、零引擎术语、可逐条被用例核）。
 *  ★入参契约（本函数**只吃归一后的 brief**，不吃世界本体）：这是踩过的一格——
 *    第一版写成"吃世界"，于是调用方把 `worldToBeReplaced()` 的结果又喂进来 ⇒ 二次归一出「未名世界 / ? 轮」。
 *    同一个形状、两种身份 = 本仓"一字段一义"要治的病 ⇒ 契约写死：**先 `worldToBeReplaced()`，再喂结果**。
 *  brief 为 null/空名 ⇒ ''（= 调用方据此不问）。 */
export function initWorldOverwriteNotice(brief) {
    const old = brief && typeof brief === 'object' ? brief : null;
    if (!old || !old.name) return '';
    const lines = [
        `「✨ 开始新世界」会**换掉**当前这个世界「${old.name}」。`,
        '',
        '将被换掉（世界账）：',
        `· 已推进 ${old.tick == null ? '?' : old.tick} 轮${old.entities ? ` · 记着 ${old.entities} 条名号` : ''}`,
        `· 这个世界的进度、事件、盘算都会从头开始（它背后的对话记录不受影响）`,
        // ★leg103：这句原来是**假承诺**（见文件头）。现在它**是真的**了——接线层在确认之后先拍一份自保快照，
        //   而且拍在**任何改动与任何模型调用之前**；理由里写「换掉前自保」，玩家在快照页一眼能认出来。
        '· 按「确定」之后会先给当前状态拍一份快照（在快照页显示为「换掉前自保」，换错了可以退回来）',
        '',
        '换掉之前没有任何调用能先替你试一下，所以先问你一句。',
        '点「确定」= 换掉它；点「取消」= 什么都不做（世界一个字节不动，也不会发生任何调用）。',
    ];
    if (old.savedAt) lines.push('', `（这份世界最后落盘：${old.savedAt}）`);
    return lines.join('\n');
}

/** ★★★leg103（A3）：**导入恢复**那一问的文案。
 *  病：导入与初始化**同等破坏力**（`writeHotMeta` + `loadWorld` 立刻换掉当前世界），
 *    却只有初始化问了这一句；导入那条路选错文件就直接覆盖，一声不响。
 *  ★与初始化那一份的**唯一区别**（如实写清，别让两份文案变成"看起来一样、其实承诺不同"）：
 *    · 初始化**真拍**一份自保快照（`reason: '换掉前自保'`）⇒ 那句承诺成立；
 *    · 导入**不拍**（它带的是文件里那份世界）⇒ 这里不许照抄"会先拍一份"，
 *      改成如实说"它不会替你先拍 —— 要留退路请先按「📷 拍一份快照」再导入"。
 *  brief 为 null/空名 ⇒ ''（= 调用方据此不问；没有世界可丢时不该多问一句）。 */
export function importOverwriteNotice(brief) {
    const old = brief && typeof brief === 'object' ? brief : null;
    if (!old || !old.name) return '';
    const lines = [
        `「⬆ 导入恢复」会用文件里那份世界**换掉**当前这个世界「${old.name}」。`,
        '',
        '将被换掉（世界账）：',
        `· 已推进 ${old.tick == null ? '?' : old.tick} 轮${old.entities ? ` · 记着 ${old.entities} 条名号` : ''}`,
        '· 这个世界的进度、事件、盘算都会被文件里那份取代（它背后的对话记录不受影响）',
        '· 它**不会**替你先拍快照 —— 要留退路，请先按「📷 拍一份快照」再导入',
        '',
        '点「确定」= 换成文件里那份；点「取消」= 什么都不做（世界一个字节不动，也不会发生任何调用）。',
    ];
    if (old.savedAt) lines.push('', `（这份世界最后落盘：${old.savedAt}）`);
    return lines.join('\n');
}
