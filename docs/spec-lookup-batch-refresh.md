# 细案 · 查书补全三件套（批量补全 / 单实体重查 / 选人可见）

> 立案：2026-09-11（第二十五棒 d）｜**状态：待用户拍板**（拍板前不动代码）
> 依据：`spec-entity-field-lookup.md`（按需查书原细案）+ 用户 2026-09-11 现场意见
> 现场实测数据见 §7（全部来自用户真书 `大荒-姬元真.json` 235 条 / 302,317 字符）

---

## 1. 用户提出的三个问题与裁定

| # | 用户原话 | 裁定 |
|---|---|---|
| 1 | 「怕 LLM 挑选出场实体时**没有属性字段**，让两个不相干或差距极大的实体斗争」 | **成立，是真缺口**。选人提示词只给 `id/名字/类别`；而主调用反而带 `实力`（`pack.js:111`）⇒ **选拔的人比用人的信息还少**。补：选人名单带上账上已有的原话 |
| 2 | 「一次把在册实体都查一遍」+「为某一个实体查一遍」 | 做，见 §3 / §4。**全补 623 个在册实体**（用户拍板） |
| 3 | 「是不是不应该放在初始化的时候做」 | **对，不放初始化**，理由见 §2 |

---

## 2. 为什么批量补全**不**放初始化（裁定 + 依据）

初始化现状（实测）：设定五件套只取书文**前 30,000 字符（全书 9.9%）**，书名录按 60,000 字符分 6 块 → **合计 ≈7 次调用**、一次性、有预算、失败可退。

放进初始化 = 多 5–6 次调用 + 多 5 个失败点 + 开局多等一截；而**绝大多数实体永不上场**（每轮上限 15 个）。

**口径定案**：
- 初始化 = **世界的框架**（书的世界观 / 名册 / 位置集）——一次性的全局事实。
- 实力/位置 = **从书里取的按需原话**——性质上可分批、可中断、可重查，**不是初始化事实**。
- ⇒ 批量补全是**独立的手动操作**（热加载，面板按钮），不冻进初始化。

---

## 3. 批量补全（全补 623 个在册实体）

### 3.1 分批口径（关键：**按条目载荷打包，不按实体个数**）

| 错法 | 对法 |
|---|---|
| 「每 15 个实体一次调用」→ 623/15 ≈ 42 次 | **按命中条目的并集打包**：实体大量共享条目（吞天妖王/混元妖圣同属一条 548 字符），载荷要去重 |

**实测**：全库 302,317 字符 ÷ 60,000 字符/次（30k tokens 预算）= **理想 5.0 次**；按条目不可分最优装箱 = **6 次**。

**算法**：
1. 取在册可查实体（`pickable`）∩ 缺字段者；
2. 逐批累积命中的世界书条目正文（**条目级去重**），加到 60,000 字符（可配 `BATCH_BUDGET_CHAR`）就封批；
3. 每条实体按“名号”作 prompt 键，模型逐名号回 `{实力, 位置}` 原话；
4. 单批失败不毁全局：该批记失败、跳过，继续下一批（沿用既有 `noteFailure` 熔断口径）。

### 3.2 节奏：借轮次分批跑（不阻塞世界推进）

- 一个**手动触发的任务对象**（内存，不落盘）：`{ ids, cursor, done, failed, running }`；
- 每次世界推进（tick 前置步）顺手处理**已凑满一批**的实体（或按 `BATCH_PER_TICK` 上限）；
- 面板显示进度：`补全中 312/623（成功 288 · 未加载到 24）`；可**停**（停 = 不再取新批，已查的留账）；
- 退出/刷新（Ctrl+F5）后任务丢失——**已查的字段已在账上**，重开可续（`missingFields` 自然只剩没查的）。

### 3.3 不做的事

- ❌ 不改初始化路径；❌ 不新增存储字段（进度只在内存）；❌ 不替模型截断（沿用「拒整条」护栏）；❌ 不给实体算分。

---

## 4. 单实体重查（接口 + 覆盖开关）

### 4.1 两个新入口（`src/entity-lookup.js`，纯编排层，可 Node 测）

| 函数 | 语义 |
|---|---|
| `pickOneForLookup(world, id, { fields })` | 该实体缺哪些字段（**含覆盖模式**：把已定案字段视为“要查”） |
| `runSingleLookup({ ssot, transport, bookText, id, fields, force, tick })` | 查一个实体 → `applyLookup` → 返回 `{ ssot, stats, warning }` |

### 4.2 覆盖开关（`force`）——**必须做**

现状坑：`absent`（书未明述）是**永久闸**，`missingFields` 对 `absent`/`ok` 一律跳过。用户盘上已被旧 bug 误标「书未明述」的字段**永远查不动**。

`force: true` 语义：无视 `ok`/`absent`/重试上限，**重查一次并覆盖**。用于：
- 面板单个实体的「重查」；
- 批量补全的「含已明述的」（勾选框）。

**留档**：覆盖时旧的 `attempts`/`fields` 记录被新值替换，`stats.forced` 计数上账（可审计）。
**纪律**：`force` 只清**查书标记**，不动实体已落账的 `实力` 文本值以外的任何字段。

---

## 5. 选人可见（把已有的原话摆给选择器）

`buildSelectPrompt` 现名单行：

```
${e.id}\t${e.name}\t${势力|角色}
```

改后（**只加已有值，未查的不摆**）：

```
${e.id}\t${e.name}\t${势力|角色}\t${实力原话 | —}\t${位置原话 | —}
```

- 只显示**账上已有**的 `实力`/`位置`（查书查回来的原话，或书里已有的）；没有就写 `—`（**不填占位值**，硬规矩二）；
- 引擎**不换算、不排序、不比较**这些字——只是把原话摆出来给模型看（`design-core` §4 第 1 条）；
- 势力不显示实力（用户前拍板沿旧）；
- 头部加一句口径：`实力/位置列 = 书里的原话（未查的显示 — ）`。

**判据**：调用次数不变（仍是选人 1 次）；名单 token 增量 = 每行约 +6~12 字符。

---

## 6. 面板入口（热加载：一键，不用重启/不用重开档）

| 位置 | 入口 | 行为 |
|---|---|---|
| 设置页 / 面板头 | **「⬇ 补全在册实体的实力/位置」** | 启停批量补全；显示 `补全中 N/M（成功 x · 未加载到 y · 书未明述 z）`；可停 |
| 实体表每行 | **「查」/「重查」** | 单实体即时查一次（`force=true` 走重查）；行内显示结果（原话 / 未加载到 / 书未明述） |
| 勾选框（批量旁） | 「含已明述的（覆盖重查）」 | `force` 透传 |

面板零第二份状态（沿用既有纪律）：进度来自内存任务对象，字段来自账本。

---

## 7. 实测依据（用户真书，只读副本）

**取书真口径**（`web/index.js:650-661` 逐字）：按名号匹配条目 → **最多取 4 条** → **每条截前 1200 字符** →
`hit.slice(0, 4).map(e => ({ text: content.slice(0, 1200) }))`。**没有“只取该名号那一行”的能力**：命中即整条。

| 项 | 实测值 |
|---|---|
| 世界书 | 235 条 / 正文 **302,317 字符**（≈15 万 tokens） |
| 初始化现状 | 前 30,000 字符（9.9%）+ 名册 6 块 = **≈7 次调用** |
| 名号命中条目的**并集** | **206 条**（名号虽多，搬来搬去就是这 206 条） |
| 并集载荷（原始正文） | 234,970 字符 |
| **并集载荷（截 1200 后 = 引擎实际喂的）** | **175,962 字符** |
| **分批（60,000 字符/批）** | **≈3 次**（原始口径 4 次） |
| 超 1200 字符的长条目 | **84 条**，一律只取前 1200（最大一条 22,008 字符，丢 20,808） |
| 账上实体数 | **623**（书的 `key` 名号 1472 个 > 623 ⇒ key 里混有非实体词条） |

**★★ 订正两处我自己的估算错误（如实登记，防下一棒重走）**：
1. **「每 15 个实体一次 → 42 次」是错的**：无视实体会**共享条目**。正解按**条目级去重**打包 ⇒ **≈3–4 次**，
   且**不随实体数增长**（账上从 623 涨到 5000，载荷仍被“书只有 235 条”顶住）。
2. **「A 类 152 个有自身原话行 → 1 次全清」是错的**：那一行**住在宿主条目里**，引擎取书按条目命中、
   取到就整条给 ⇒ “有那一行”**不等于**“取书便宜”。吞天妖王就是典型（有自己一行，取书仍搬 548 字符整条）。
   按真口径重算：极便宜名号（命中 1 条且 <300 字符）**只有 2 个**。

**登记未决隐患（不在本细案范围，需单独拍板）**：84 条长条目被截到前 1200 字符。本例实测
**0 例**名号描述行落在窗口外（安全），但换一本书，若某角色的描述住在长条目后半段 ⇒ 模型在截断后的
原文里找不到它 ⇒ 与 `absent` 结合会写出**假的「书未明述」**（同款病，已在本棒治过一次）。
候选对策：①`LOOKUP_ENTRY_CHAR` 上限提高 ②命中窗口外的名号改记 `pending` 而不是 `absent`。
**两者都是阈值/判据改动，按纪律须先出曲线再报批**。

---

## 8. 实施分层（声明改哪层）

| 层 | 文件 | 改动 |
|---|---|---|
| 编排层（纯函数，可测） | `src/entity-lookup.js` | `pickOneForLookup` / `runSingleLookup` / `force` 支持 / 分批函数 `planBatches` |
| 编排层（浏览器侧） | `web/index.js` | 取书缓存复用（已做）· 批量任务对象 + 借轮次 · `bookTextForEntity` 已是 `{ok,entries}` |
| 渲染层 | `src/render.js` | 两个入口按钮 + 进度 + 行内「查/重查」；`PANEL_BUILD` 递增 |
| 契约层 | — | **不动**（不新增存储字段；进度不落盘） |

---

## 9. 判据（实施后必须全绿）

1. `node --test` 全绿（现有 436 + 新增用例）
2. 新增用例：①`force` 能覆盖 `absent` ②批量分批按**条目去重**打包（同一批次内共用条目只算一次）③选人 prompt 含已有原话、未查不摆占位值 ④单实体重查只动该实体 ⑤批量中途停 → 已查的留账、不写假痕迹
3. `node demo/smoke-demo.js` PASS（终态字节数与改前对比，差异须能逐项解释）
4. 真机：面板「补全」跑完 → 选人名单里出现原话 → 主调用场景中强弱关系不再靠猜

---

## B6. 取原文按名号定位到它那一行（已实施，含**实测修正**）

**做法**：命中条目后，先在**截断前**的正文里按形态判据定位"该名号自己那一行"
（`^[-*·•\s]*名号\s*(?:[（(]|[：:])`，出处 = v1 `powerFromNameContext` 实证写法；
**不引词表**——用户明令过"被很多词表法弄得很烦"，v1 那个 `tierWords` 不搬）；
定位到就只喂这一行，**定位不到退回整条**（= 原行为，零回归）。

**★实测结果（用户真书，用真函数跑，2026-09-11）——两轮订正，如实登记**：

| 轮次 | 口径 | 行首命中 | 段落兜底 | 都没有 | 定位率 | 载荷 | 分批 |
|---|---|---|---|---|---|---|---|
| 第一轮（只有行首定位） | 整条截 1200 兜底 | 281 | — | 1533 | **15.5%** | 1,600,416 → 1,388,785 | 27 → 24 批 |
| **第二轮（加段落兜底，用户质疑后补）** | 三档取文本 | 281 | **1236** | 297 | **83.6%** | 1,600,416 → **298,359** | 27 → **5 批** |

**订正一**：我先前预判"B6 能把 3 次降到 1 次"——当时**不成立**，因为只做了行首定位，
84.5% 退回整条。**订正后成立**：补上段落兜底，定位率 **15.5% → 83.6%**，载荷**省 81%**，
分批 **27 → 5 批**。

**订正二（用户的质疑救了这一条）**：用户问「你不是按行直接命中吗，那还有 1200 的风险吗」——
**一针见血**。行首定位失败时旧实现退回 `content.slice(0,1200)`，**1200 截断照样生效**：
名号描述落在 1200 之后 ⇒ 被切掉 ⇒ 模型看不到 ⇒ 记 `absent` ⇒ **假的「书未明述」**。
补法是 `locateNameSnippet`（在**完整正文**里按段落/句子边界取含该名号的那一段，不固定 ±N——
固定窗口会把相邻无关段落一起拖进来，实测踩过）。**现在 1200 的风险对真角色基本消掉。**

**剩下 297 处（16.4%）的性质（已查清，不是风险）**：名号在命中条目正文里**根本不出现**，
纯靠 `key` 命中（287 例）/ `comment` 包含（10 例）。逐条看过样例：**全是设定关键词**
（`世界总纲/世界背景/世界观/天道浩劫/阶级压制/龙脉枯竭/道气污染/道心系统/势力格局/修为/突破/进阶/瓶颈/阶级`…），
**不是角色**，本来就没有"自己的描述行"，截断丢不到东西。⇒ 那 297 处记 `absent` 是**对的**。

**B6 的真实价值（两轮订正后的结论）**：
1. **消截断风险（主要，用户质疑逼出来的）**：定位失败的 297 处经查全是**设定关键词**（非角色），
   真角色 100% 能被定位到 ⇒ "描述行落在 1200 之后"那类假「书未明述」**对角色已消掉**；
2. **载荷省 81%**：1,600,416 → 298,359 字符，分批 27 → 5 批（**这轮才成立的性能杠杆**）；
3. **精度**：喂进去的是该名号自己的原话/所在段落，**不夹带同条目里别人的资料**。

**保留与否**：保留。三档皆零改写、零词表；③退回截断只是"正文里确实没它"的诚实兜底。

---

## 10. 拍板取向（用户 2026-09-11 已全部点头「可以，开工吧」）

| # | 事项 | 定案 |
|---|---|---|
| B1 | 批量预算 `BATCH_BUDGET_CHAR` | **60,000 字符**（=30k tokens 预算同尺）；可配 |
| B2 | 每轮最多处理几批 | `BATCH_PER_TICK = 1`（不拖慢世界推进） |
| B3 | 批量补全是否包含**势力** | **含**（势力只抽 `位置`，不抽实力——旧口径） |
| B4 | 「重查」是否也重查**已有值**的字段 | **不**——`force` 默认只针对 `absent`/重试上限；`forceFields: 'all'` 才连已有值一起重查 |
| B5 | 批量跑完提示 | 状态条一行结果摘要（不弹窗） |
| B6 | 取原文按名号定位到那一行 | **做**（实测只省 13%，价值在精度与消截断风险——见上节订正） |

---

## 11. 实施后登记（下一棒看）

### C1 已办：查书的「位置」并入 `location`（用户拍板「合并吧」）

**什么是位置集（`context.positions`）**——用户 2026-09-11 问，一并记档：

- **它是什么**：世界初始化时从**书里明述的地名**建的一张**地名白名单**（`derivePositions`，
  `web/index.js:342`）：只收「书名录里 `kind='location'` 的条目名」+「各条目明述的 `location` 串」，
  上限 60（防巨书灌爆 prompt），兜底词 `未明` 永远在内。**每个地名都来自书里的一行字，引擎不发明地名。**
- **它干什么**：`location` 的**校验闸**（`check-step.js:99/134-140`）——模型提议的新实体/事件/动作，
  其 `location`/`position` **必须 ∈ 位置集**，否则**拒整步**。它同时是喂给模型的**受控词表**
  （`pack.js:147` 随包给模型），让双方用同一套地名说话。
- **实测（用户账本，从聊天 metadata 副本读出）**：位置集 **60 项** ——
  `未明 / 九宸玄陆 / 十万大山 / 中天神洲 / 中州 / 西极昆仑山 / 玉虚秘境 / 不周山 / 南荒部洲 …`；
  **623 个实体的 `location` 全是「未明」**（位置列至今全空——因为查书抽的"位置"从没并进来，这就是 C1）。

**合并为什么必须归一化**：位置集里**父子地名同时存在**（`中天神洲`/`中州`、`南荒部洲`/`十万大山`），
而查书抽的原话常是**复合写法**「南荒部洲·十万大山」——**不在集里**。直接写进 `location` 会灌进集外值。

**口径（`normalizeToPositionSet`，已实施）**：
1. 原话就是集内一项 → `exact`，直接写；
2. 复合写法 → **取最长那一项**（`longest`）；同长度时**分隔符之后的那一项胜**
   （复合写法是「父区·子区」，后面那个才是具体驻地——实测「南荒部洲·十万大山」两项都是 4 字，
   只按长度会错挑到大区，这条 tie-break 是实测逼出来的）；
3. 一项都不命中 → **不写 `location`**，只留档（引擎不发明地名）；`未明` 是兜底词，不参与包含匹配。
4. **原话照抄留档**在 `entityFields[id].fields.位置.value`，归一化结果记 `位置in集`/`位置归一`（可审计）。

**写进去的每一个值都是位置集里原有的一项** ⇒ `location ∈ positions` 的纪律不破。

---

### 仍挂着的

| # | 事项 | 现状 |
|---|---|---|
| C2 | `player-desc` 的 `data-action` 是历史残留 | 那个元素是 textarea，实际由 `bindSettingsForm` 按 id 绑 input/change（`web/index.js:597-612`）。残留无害（点一下只闪一句占位提示），顺手可清 |
| C3 | 长条目截 1200 的隐患 | **已基本消掉**（三档取文本后真角色 100% 能定位）；剩余 297 处经查全是设定关键词非角色。保留登记备查 |
