# 交接 · 第一百棒（leg100）——**归档不许当候选**（第一笔：收场被拒那句出路指对栏 · 第二笔：甲案，两栏在给模型的资料里分得开）

> **接手第一件事：读 §0 与 §1**（第一笔）＋ **§8**（第二笔）。
> 本棒由用户给出 leg99 交接文档开工。★**开工第一件事不是动手，是核验继承状态**（§0.3），
> 核验当中**用户问了几个问题**，那几个问题就是本棒的全部路径：
> 「**说人话，为啥有这个报错**」→「**那行你改一下**」→（我改了那句话之后）「**你就改了报错时显示的话？我是让你修复问题**」
> →「**什么意思，只说人话**」→「**可以按甲吧**」。

**git 现场**：分支 `leg62-scales-concept-table` · 接手时 HEAD **`3cb2b4d`**（= leg99 收尾，工作区干净）
· 第一笔提交 **`4f80f78`**。

**终检读数（收尾时重跑）**：
`node --test` **1078/1078 · suites 0 · pass 1078 · fail 0 · cancelled 0 · skipped 0 · todo 0**
· 冒烟 **PASS · 终态 SSOT 8231 字节 · 警告 0**（★与 leg86–99 **逐字节相同** ⇒ **引擎零漂移**）
· `PANEL_BUILD` = **`leg100-archivemark`** · `CSS_VERSION` = **`20260922-leg99-pageflow`**（★样式表零改动 ⇒ 不升）
· `MAIN_PROMPT_V` = **`v2-agenda-t1-23`**（第二笔动了提示词正文 ⇒ 同批升）
· 反向自证 **3/3 ＋ 3/3 全部当场红**（两组装置，逐字节还原产品并复跑）。

---

## 0. ★★★ 一句话：模型不是"乱点号"，是**照另一栏抄的**——而那句出路只指了一栏

### 0.1 病（用户在实机看到的原文）

```
$.eventClosures[0].event: 事件「ev_6_4」（老兵肉弹在长城防线自爆）**已经了结**——
收场是一次性的，**把这一项删掉**（它不在输入那份"还没结束的事"里，换一件还没收场的）
$.eventClosures[1].event: 事件「ev_7_2」（妖圣夺取半块核心，不周山彻底坍塌）**已经了结**——（同上）
（世界原样未动，可重试）
```

★**引擎这一刀本身是对的**：收场是一次性的（`src/ref-rules.js:526` 的 `closure-event-closed`），
世界没被动过。**本棒要从"谁的错"往下追一格**：模型为什么会去点那两个号？

### 0.2 ★★★ 真机取证：那两个号**确实进了它的视野**，只是从**另一栏**进去的

装置 `F:/deepseek/tmp/prototypes/leg100-closure-leak.mjs`（★**调产品那一口** `buildEvolutionPack` 建包，
**不手抄一份"近 8 条已闭环"**——手抄的尺子一定会与产品漂移）：

```
账： 大荒z1/大荒z - 2026-09-14@16h54m07s001ms.jsonl | 轮: 12 | 事: 92 | 已了结: 20 | 未了结: 72
包自证：外层键=pack,text,estTokens · 包内键数=16 · pendingEvents=72 · recentClosedEvents=8
        · entities=689 · text=54790 字节 · trimmed=null

★被引擎拒收的那两个 id：
  ev_6_4: 已了结 · closedBy=(无) · closedAt=11 · source=ripple
     在包里 pendingEvents?      不在（对的 —— 引擎没喂错料）
     在包里 recentClosedEvents? ★在（模型就是从这一栏抄的）
  ev_7_2: 已了结 · closedBy=(无) · closedAt=7 · source=plot
     在包里 pendingEvents?      不在（对的）
     在包里 recentClosedEvents? ★在

台账尾巴（进包的那 8 条）：ev_6_4 ev_7_1 ev_7_2 ev_7_3 ev_8_11 ev_8_12 ev_10_4 ev_11_3
```

★★**被拒的那两个号，恰好是那份尾巴的头两条。**

### 0.3 ★★ 为什么"进了视野"就算真病（而不是"文案不好听"）

三条叠在一起：

| # | 事实 | 出处 |
|---|---|---|
| ① | 那一栏叫 `recentClosedEvents`，`prompts.js:243` 把它叫**「最近了结的事」**，而它**排在包里、逐轮都带** | `pack.js:938` |
| ② | ★真被预算裁时它**只剩 `{id}`**（没有标题） ⇒ 一串**光秃秃的号**，读起来正是一份"还能收的事"的候选池 | `pack.js:1018-1021` |
| ③ | 而**收场走的是整步拒**（`check-step` 拒整步、净化器**不替模型摘这一项**）⇒ 画不出"往哪走"就等于**白烧一轮** | `check-step.js:366-392` · `ref-rules.js:524` 那条自律的原话 |

★**旧话术只指了 `pendingEvents` 那一栏**（"它不在输入那份'还没结束的事'里"）——那句话**对**，
但**指错了地方**：模型是从 `recentClosedEvents` 抄的号，照旧话去"还没结束的事"里找，**它当然找不到**。

### 0.4 ★ 顺手核清的两件事（不猜，读代码）

- `closedBy` **缺格 = 引擎机械扫的**（`ssot.schema.js:467`）。真账那两件都缺格 ⇒ **旧话术里
  "你已经判过它收场"那一支没有误用**，两分支的措辞是准的（本笔只加不删）。
- `packTextOf` 是**裸 `JSON.stringify`**（`pack.js:300`）⇒ **模型看到的就是 `recentClosedEvents` /
  `pendingEvents` 这两个键名**——所以新话术点这两个名字，模型是**找得到**的（这不是自造词）。

---

## 1. ★★ 修法与它的边界

**修法**（`src/ref-rules.js` 的 `'eventClosures.event'` 那一格，**只动文案**）：

```js
message: (r) => `事件「${r.ref}」（${…title…}）`
    + `${hit.target.closedBy === 'model' ? '你已经判过它收场' : '**已经了结**'}——`
    + '收场是一次性的，**把这一项删掉**'
    + '（★这个号住在输入的 **recentClosedEvents**（最近了结的事）里——那是归档，'
    + '**不许从那一段取号**；要收场只认 **pendingEvents**（还没结束的事）里那一批）',
```

★**语义一个字未动**：只动"出路"那句（`ref-rules.js` 自己写着"出路要给够"，本笔是**把那条自律落实**）。
★**旧话术里那两条一个字未动**："收场是一次性的" ＋ "把这一项删掉"（M3 那条 actionable 锁照旧绿）。
★**判据（什么合法、什么不合法）一个字未动**——变的只是"拒绝时怎么说"。

---

## 2. ★★ 判据（`test/ref-rules.test.js`，紧跟 M3b 之后新增一节，+1 条）

★**为什么写在这个位置**：M3b（`newAgendas.source` 那条）锁的正是"**必须指出那个号在输入的哪一栏里**"
（`closedRoots`/拾遗），本笔那条是**同一个手法用在收场通道上** ⇒ 两条住一处，下一任一眼看得出是一族。

| 面 | 断言 |
|---|---|
| 老底子照旧（三条） | 指名到号 · 指名到事（带标题）· `收场是一次性的` · `删掉` |
| ★★**本笔的正面（三条）** | 必须点到 **`recentClosedEvents`** · 必须点到 **`pendingEvents`** · 必须说出**"不许从那一段取号"**这句禁令 |
| ★反面（两条） | 不许把"已了结"说成"不存在"（leg32i/leg64 那条）· 禁令面必须在（`不许\|只认`） |

★**量词选择**（leg98 §8.8 那一族的纪律）：这里咬的是"**那个号在输入里的住处有没有被点出来**"，
不是"文案好不好听"。

---

## 3. ★★★ 反向自证 **3/3 全部当场红**（装置 `F:/deepseek/tmp/prototypes/leg100-mutate.mjs`）

跑法：`node F:/deepseek/tmp/prototypes/leg100-mutate.mjs`
★纪律同 leg98 §8.3 / leg99 §4：**就地改被 import 的那份文件 → 跑 → 逐字节还原 → 复跑**（`finally` 兜底），
**并且每个变异体先过"变异体自证"**（改完先单独证明**行为真的变了**，否则不报"咬住/没咬住"）。

| 组 | 变异（把错法放回去） | 实测 |
|---|---|---|
| ① | **旧话术整句放回**（= leg95 的原话，指错栏） | ✔ 红（本判据） |
| ② | 两栏名**都留着**，但**删掉禁令**（"不许从那一段取号"） | ✔ 红（本判据） |
| ③ | 禁令**留着**，但**两栏名都换成中性说法**（模型找不到栏） | ✔ 红（本判据） |

★★**三处都是"外科式"的**（一次只拆一个承重点）：①拆"指对栏" ②拆"禁令" ③拆"栏名"。
★**为什么不能只做 ①**：①一次替换了**两段**，红了也只能说明"这句短语在"，
**分不出判据到底咬住哪一层**（这正是 leg99 §4 那条"变异体自己是空转的"教我的事）。

### 3.1 ★★ 本笔踩的两脚（都如实留档）

1. **装置的变异③第一版"找不到要替换的那一段"**：我按**字面串**写 `from`，而那段正文里本来就有
   `——` 与 `★`（我把它拆行读了，字面串与产品**不同形**）⇒ 替换没发生 ⇒ 被自证拦成"**作废**"。
   ★**危险的地方在于**：它当时离"判据没咬住"只差我一句误读——**我又一次差点照错方向去补判据**。
   ⇒ 定稿：装置改成**支持正则 + 把"找不到"单独报成一类**（`⊘ 作废（装置前提不成立）`），
   与"红/没红"**分开报**。**这是 leg99 §4 那条纪律的第二次现场。**
2. **变异③第二版"变异体自证"又拦了一次**：我把 `to` 写得**把禁令一起吞掉**了，
   于是自证报"**空转**"——★**它拦对了**：那个变异动了两处，证明不了什么。定稿把禁令留在 `to` 里。

---

## 4. ★★ 升位：`PANEL_BUILD` 升、`CSS_VERSION` **不升**（判据还是那一条）

| 号 | 旧 | 新 | 判据 |
|---|---|---|---|
| `PANEL_BUILD` | `leg99-paramquiet` | **`leg100-closureexit`** | ★那句话是**拒收时印给模型与用户看的原文**（同 leg64/leg66/leg67 那条：**报错给了新出路就该升位**） |
| `CSS_VERSION` | `20260922-leg99-pageflow` | **不动** | `web/style.css` **零改动** ⇒ 同 leg95d/leg96/leg94 那条纪律（**动没动样式**才是判据） |

★起名避禁词：`closure` / `exit` 都不在禁词表内（禁的是 `agenda`/`tick`/`ssot`/`schema`/`chronicle`/
`entity`/`kind`；★`event` **在**表内，见 leg95 那次改名）⇒ 合 `/^leg\d+[a-z]?-/`。

### 4.1 ★★★ 那条锁**当场咬了我两次**（两次都是它在干活）

改构建号之后 `test/render.test.js` 的 `leg50` 那条**当场红**，而且**连红两次**：

| # | 红在哪 | 怎么修 |
|---|---|---|
| ① | `assert.equal(PANEL_BUILD, 'leg99-paramquiet')`（存在性断言） | 同批改值 + **把升位理由写在旁边**（本仓惯例） |
| ② | ★`assert.equal(buildLeg, '99')`（**锁自己的前置自证**："锁自己也要能被反向自证咬住"） | 同批改 `'100'`，并注明**它咬的不是"号该不该升"，而是"下面那条比较真的在比哪两个数"** |

★②那条**此前从没被顶红过**（leg99 时才刚加上）——⇒ **"升位"这件事本身就是它的一次实机演练**。
★下面那条 leg 比较（`buildNum - cssNum === 1 || buildNum === cssNum`）**照旧绿**：
CSS 停在 99、面板走到 100 ⇒ **恰好落后一笔**，正是 leg99 收窄它时要允许的那种情形。

---

## 5. ★ 本笔**登记但没动**的两处（下一任先看这里）

### 5.1 ★★ 结构性的那一半：`recentClosedEvents` 被裁成**只剩 id**

- 现状：`trimmed=null`（真账没触发裁剪）时它是 `{id,title,source}`；**一旦裁到它**（`pack.js:1019-1021`）
  就**只剩 `{id}`** ⇒ 一串没有标题的号，**从形状上就像候选池**。
- ★**本笔只治了"话术"那一半**（拒收时告诉它别从那一段取号）；**"那段为什么看起来像候选池"这一半没动**。
- ⇒ 候选出路（**未裁**）：给那段留一个一眼看得出是"归档"的标记，或干脆给它一个**与"候选"不同的形状**
  （比如统一带一个 `已了结` 的标记位）。★动它要**先量字节**（那 8 条每轮都进包）并过 `lens.test.js` 的**键序锁**。
- ⚠ 注意别**顺手删掉**那段：`pack.js:201-202` 明写它与 `closedRoots` 的**分工**
  （一个"发生过什么"、一个"可以动手接的"）——删了会重演 leg39 的"名单空转"。

### 5.2 提示词那一半：`prompts.js` 第 15 条只指了"还没结束的事"

`prompts.js:321-326` 教模型收场时写的是「照抄**输入里'还没结束的事'**的号」——
★**它是对的**（收场确实只认那一栏），但**它没有告诫**"别从 `recentClosedEvents` 里挑"。
本笔**没动提示词**（用户令只给了"那行"）⇒ 若实机仍见模型照台账抄，这里是下一刀，
且那条提示词有 `prompts.test.js` 锁着（改它要同批改判据）。

---

## 6. ★★ 本笔踩的坑（下一任照做）

1. ★★★**"包的那个口交的是信封，不是包"**：`buildEvolutionPack` 交的是 `{pack, text, estTokens}`
   （`pack.js:986`）。我的取证装置第一版按 `pack.pendingEvents` 读 ⇒ **两段都印 0 条**，
   差一点报成"**包是干净的**"（其实一个字都没读到）。
   ⇒ ★**装置必须先自证"我真的读到东西了"**（本笔把 `包自证：包内键数=16 · pendingEvents=72 · …` 印在最前面）。
   这与 leg99 §4 那条"变异没打上 ≠ 判据不灵"**同族**：**"装置没接对"与"结论是好的"长得一模一样**。
2. ★★**"找不到要替换的那一段"必须单独报一类**（§3.1 第 1 条）：装置里"红 / 没红 / **作废**"三态分开报，
   否则我会照"判据没咬住"去改判据——**方向全错**。
3. ★**改构建号 ⇒ 先搜"谁把这个号写死了"**：本笔被咬两次（§4.1），第二次是**锁的自证**那一行。
   ★纪律：**升位不止改一处**（本仓先例 leg66/leg67 都是同批改判据）——
   `grep 旧构建号` 是升位后的必做动作。
4. ★**我第一遍把一条注释读漏了**：我在 `test/ref-rules.test.js` 的 import 块里没看见 `renderVerdict`，
   差点去"修"一个**根本不存在的 bug**（它就在那几行的换行列表里）。
   ⇒ 含中文的长文件里**逐行扫会看漏**——**用 `grep` 定位，别用眼睛数行**。
5. ★**台账是 markdown 表格**：插行时我**误把表头那一行替换掉了**（两行都是 `| 日期 | …` 形状），
   而且新行与旧行之间**多出一个空行 ⇒ 表格会断成两张**（右侧整列渲染不出来）。
   ⇒ 定稿：**插在"表头分隔行 `|---|---|`"之后**，并且**插完立刻回读那 3 行**。

---

## 7. ★ 只有人能跑的真机验收（下一任用）

★**本笔只该看见一处变化**（其余一个字都不该动）：

1. **F5 硬刷新** ⇒ 页脚构建号应是 **`leg100-closureexit`**
   （CSS 号应**仍是** `20260922-leg99-pageflow`——**那是对的**：本笔样式表零改动）。
2. ★**要验的那一处**：下一轮**再遇到收场被拒**时，报错里应当**同时出现两个键名**
   （`recentClosedEvents` 与 `pendingEvents`），并且**模型不再反复换号**。
   ★**注意**：本笔**没有浏览器**，我能证到"判据全绿 ＋ 装置量出那两个号的真实住处"，
   **"它真的少烧轮次了没有"只有你在实机上看得出来**。
3. ★**其余一律不该变**：账一个字未动（冒烟 8231 字节逐字节未变）、版面归置、颜色、卡片上的字。

---

## 8. ★★★ 第二笔（用户令「**可以按甲吧**」）：归档不许当候选——**让两栏在给模型的资料里长得不一样**

### 8.1 ★★ 用户为什么不接受第一笔（他说得对，这一条要记住）

第一笔改完之后用户当场顶回来：**「你就改了报错时显示的话？我是让你修复问题」**。
★**他指对了**：第一笔改的是**"骂得更清楚"**，而真病是**模型手上没有分辨"候选/墓碑"的尺子**——
`recentClosedEvents`（最近了结的事＝归档）与 `pendingEvents`（还没结束的事＝候选）此前
**形状完全一样**（都是 `id` + `title` + `source`）⇒ 它挑错号是**必然**，骂它一句只是让它少挑。

★**纪律（本棒最值钱的一条）**：**"改报错文案"不等于"修问题"**——
先问一句"**它为什么会犯这个错？它手上的尺子够不够？**"，够不着再谈措辞。

### 8.2 ★★★ 动它之前先量清楚"这个错到底毁了多少"（否则会过度修）

装置 `F:/deepseek/tmp/prototypes/leg100-heal-probe.mjs`（**调产品那几个口**，不手抄口径）：

```
① checkWorldStep（原样）：ok=false  ← 那两句报错（第一刀）
② dropInvalidProposals：丢掉 2 条 → 净化后 checkWorldStep：ok=true（actions=1 留着）
③ settleWithHealing：ok=true · healed.used=true · healed.fallback=false
   ★账：tick 3 → 4（照常前进）
```

⇒ ★★**自愈机制本来就吃掉了这个错**：那一轮**没有白跑**（`tick.js` ①原样→②降级重试→③世界安静一步），
被丢的只有那两个乱点的收场提议，正常提议照落账、世界照前进（状态条印的是「已同步」）。
⇒ 所以本笔**不是**"一错毁一轮"的急病，真正的损失只有一条：**模型想收的那件事没收成**（那一轮 `eventClosures` 被清成 0）。

### 8.3 改动两处（**引擎与提示词各一半**）

| 处 | 改动 |
|---|---|
| `src/pack.js` | 归档那一栏每一行盖 **`closed: true`**（**独立一格**）；★**预算裁剪时跟着留**——`trimPack` 的 ③ 级原来只留 `{id}`，而"只剩一串光秃秃的号"**正是**最像候选池的形态 |
| `src/prompts.js` | 第 15 条补指路：要收的事**只从 `pendingEvents` 挑**；`recentClosedEvents` 是归档、**别从那一栏拿号**（并同步 `MAIN_PROMPT_V` → **`v2-agenda-t1-23`**） |

★**为什么记号做"独立一格"而不是往 `title` 里塞前缀**：塞进去的字会被 ③ 级裁掉，
而**裁剪后的那一态才是最危险的**（见上）⇒ 记号必须能被裁剩之后**仍然活着**（`{id, closed}`）。
★**为什么不改栏名**（本该更彻底）：栏名动了会连带动 `prompts.js` 第 9 条、台账与两条**键序锁**
⇒ 收益不抵风险（本笔只加一格、不动栏名）。
★**真账复验**（`leg100-closure-leak.mjs` 加了一段）：归档那一栏 **8 条全部带 `closed:true`**、
**候选那一栏一条都没被误盖**；包文本 **54790 → 54902 字节**（8×14 字节，预算可忽略）。

### 8.4 判据（+2 条用例，4 条断言）

| 用例 | 咬什么 |
|---|---|
| `prompts.test.js`「③ 级之后记号必须活着」 | ★★★**最危险的那一态**：把包摆成"实体已降级到底、且本身就超预算"⇒ ③ 成为第一个真正生效的级别 ⇒ 断言 `recentClosedEvents` 每行仍是 `['id','closed']`、**包文本**里有 `"closed":true`、**候选那一栏不许被盖上同一个记号** |
| `prompts.test.js`「提示词必须把归档不许当候选写给模型」 | 栏名 + 记号字面 + 禁令（`别从那一栏拿号`）＋该从哪一栏挑，**三样齐**（leg39 那条"给名单不给资格＝名单空转"） |
| `leg32c·包面`（**既有用例，本笔补断言**） | 未裁剪那一态：归档**每一条**都带记号 ＋ 候选栏不许带 |
| `lens.test.js:166`（**既有锁，同批改值**） | ③ 级形态从 `['id']` 改成 `['id','closed']` |

### 8.5 ★★★ 反向自证 3/3（装置 `leg100-mutate-marker.mjs`）＋ **两处"空绿"当场被抓**

| 组 | 变异 | 实测 |
|---|---|---|
| ① | **包不给记号**（`closedEvents` 那一格去掉 `closed: true`） | ✔ 红（`leg32c·包面` 那条断言） |
| ② | **裁剪时把记号丢掉**（③ 级退回只留 `{id}`） | ✔ 红（本笔那条"最危险的那一态"） |
| ③ | **提示词不给指路**（那一句整段删掉） | ✔ 红（本笔那条提示词锁） |

★★**这一手在本笔抓出两处"我以为我测了"**（都不是产品错，是**我的尺子**错）：

1. **一处真·空绿**：那条"归档要带记号"的断言我第一版写在 `recentClosedEvents[0]` 上，
   而夹具里**第一条事件是开着的**、断言又写宽了 ⇒ **把产品的记号整个删掉，判据照旧全绿**。
   ⇒ 定稿改成 `every` + **非空前置**（按"每一条都得带"咬）。★这就是 leg99 §14.2 那件事的同族。
2. **一处"装置在撒谎"**：① 明明咬住了（红在 `leg32c·包面`），我的装置却报"没红"——
   因为它只把**标题里带 `leg100` 的红**算作"咬住"，而那条断言住在**既有用例**里。
   ⇒ 定稿把认领口径改成"**标题带 leg100 或断言消息带本笔留的记号**"。
   ★**教训**：装置的报告口径与判据的住处**必须分开想**——否则你会读成"判据不灵"。

★**装置自己的两脚**（都留档）：①变异①第一版按字面串 `replace`，而 `pack.js` 里
`closed: true` 有两处（建包 ＋ 裁剪）⇒ **换到了第一处（裁剪那一行）**，"模型看到的包"根本没变；
⇒ 定稿改成**按行号定点改**，并**先核对那一行真是我以为的那一行**。
②夹具尺寸我连猜两次都不对（`cut=["entities.slim"]` / `["entities.slim","entities.idOnly"]`）
⇒ 定稿**从"不变量"出发**构造（实体已是 `idOnly` 形态 ＋ 包本身就超预算 ⇒ ③ 必然是第一个生效的级别），
并把"**真的裁到了 ③**"做成**前置断言**（第一版 3000 个实体实测 `cut=[]`，正是被它当场拦下的）。

### 8.6 升位

| 号 | 变化 | 判据 |
|---|---|---|
| `PANEL_BUILD` | `leg100-closureexit` → **`leg100-archivemark`** | 给模型看的资料变了（且那句报错也跟着变了）⇒ 同批再升一格 |
| `MAIN_PROMPT_V` | `v2-agenda-t1-22` → **`v2-agenda-t1-23`** | 提示词正文变了（本仓纪律：**同批升**） |
| `CSS_VERSION` | **不动** | `web/style.css` 零改动 |

---

## 9. ★★ 本笔踩的坑（第二笔）

1. ★★★**`MAIN_PROMPT` 是模板字符串**：我往里面写的那句用了**反引号**包键名 ⇒ **提前把模板闭合**
   ⇒ `SyntaxError`，**整个 prompts.js 加载失败、两个测试文件一片红**。
   ⇒ 提示词正文里**不许出现反引号**（那个文件已有先例：leg40b 的 `${}` 那次同族）。
2. ★★★**"改报错文案"不等于"修问题"**（§8.1）：用户当场顶回来那句，本棒最该带走。
3. ★★**动它之前先量"这个错毁了多少"**（§8.2）：不量就会把"自愈已经兜住的事"当成急病**过度修**。
4. ★★**空绿**（§8.5 第 1 条）：断言写在 `[0]` 上、而夹具第一条不是那个东西 ⇒ 空转。
5. ★★**装置的报告口径会说谎**（§8.5 第 2 条）：红在"既有用例"里，装置只认"标题"⇒ 误报"没咬住"。
6. ★**`String.replace` 换的是第一处**（§8.5 装置第 ①脚）：同名片段有多处时必须**定点**改，并先核对。

---

*第一百棒（leg100） · 2026-09-21 · 接手时 HEAD `3cb2b4d` · ★本棒**两笔**（用户五道问/令）
*——**第一笔**（`4f80f78`，用户令「**那行你改一下**」）：收场被拒的出路**指对栏**
*→ ★真机取证：被拒的 `ev_6_4`/`ev_7_2` **不在** 72 条 `pendingEvents`，**却在** `recentClosedEvents`
*  （尾巴 8 条）里、且恰好是头两条 ⇒ 模型是从**归档那一栏**抄的号
*→ ★**1076/1076 · 反向自证 3/3 全红** · `PANEL_BUILD` = `leg100-closureexit`
*——**第二笔**（用户令「**可以按甲吧**」，起因是他顶回「**你就改了报错时显示的话？我是让你修复问题**」）：
*  **归档不许当候选**——`pack.js` 给那一栏盖 `closed: true`（**裁剪后仍活着**）＋ `prompts.js` 第 15 条指路
*→ ★★**先量清楚"这个错毁了多少"**：自愈三层本来就吃掉了它（`ok=true` · `fallback=false` · tick 照常前进）
*  ⇒ 真正的损失只有"模型想收的那件没收成" ⇒ **不按急病修**
*→ ★**1078/1078 · fail 0 · skipped 0**（+2 用例/4 断言）· 冒烟 **8231 字节逐字节未变**（引擎零漂移）
*→ ★★**反向自证 3/3 全红**，且当场抓出**两处"我以为我测了"**（一条真·空绿 ＋ 一处装置口径说谎）
*→ ★升位：`PANEL_BUILD` → **`leg100-archivemark`** · `MAIN_PROMPT_V` → **`v2-agenda-t1-23`** ·
*  `CSS_VERSION` **不升**（样式零改动）
*→ ★★★**三条最值钱的纪律**：**改报错文案 ≠ 修问题**（先问"它手上的尺子够不够"）·
*  **动它之前先量它毁了多少**（免得把自愈兜住的事当急病）· **装置的报告口径与判据的住处要分开想**
*  （否则"红在既有用例里"会被读成"判据不灵"）
