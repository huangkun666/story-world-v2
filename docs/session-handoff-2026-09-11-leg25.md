# 会话交接 · story-world v2 · 第二十五棒（2026-09-11 · 检察官审计 + A–H 八组全部修复 · 439/439）

> 接手前必读（按序）：①`ANCHOR.md`（唯一尺子）②`docs/dev-process.md`（执行手册）③本文件（**§2=八组交付现状**、**§5=下一棒任务书**）④`docs/ledger.md`（本棒行 **L170=总账 / L171=H 组明细**）⑤`docs/decision-index.md`。
> 状态：**本棒零新机制**——用户令「你是这套机制的检察官，检查出尽可能的缺陷然后一一汇报」→ 四路并行深审 + 主线自查 → 用户令「全部修掉，调用子agent也得给我全部修掉，我要马上见成品测试」→ A–H 八组全修。测试 **439/439 全绿**（审计前 396）。
> **⚠️ 全部改动未提交**（36 个文件在工作区，`git status` 可见最后一个提交仍是 `88a5756`）。用户验完再落 git。
> 上一棒入口：`docs/session-handoff-2026-09-11-leg24.md`（5 片切法全部落地）。

## 0. 人话版：这一棒先当检察官，再把查出来的毛病全修了

用户让我**当检察官**：不是问"代码写得漂亮吗"，而是查**这个项目自己立的法有没有被违反**（空着就是空着／引擎不裁胜负只拦矛盾／每条变更留痕／机制有没有入口／判据是不是恒真）。四路并行深审 + 我自己审结算主线，查出**最要命的一条**：

> **"玩家这枚棋子"在生产路径上根本不存在。** `context.playerId` 从来没人赋值（只有测试和演示脚本赋过），设置页里那段"你的开档描述"从来没人读。于是五条"模型禁写玩家"的守卫全是死代码、能见度掩码永不生效、影响通道整体失效——**红线第 1 条只活在注释里**。

其余按严重度：冷档轮转**每次覆盖上一卷**（旧编年永久消失）、落盘失败被吞掉而界面报"已同步"、名册入账只进内存不入盘、`max_tokens` 文档说 16384 代码还是 4096、属性变更**不写编年**、`attr` 能写任意键（能塞"气运"）、`actor` 填个不存在的 id 就能绕过门控、30k 预算没有任何强制点、"波及 ≤3"从没执行、书指纹缓存从没接线、提示词还在教模型"分量决定谁值得动"。

用户拍板"全部修掉"后，八组全修完，逐条有代码位置与测试；标 **439/439 全绿**、端到端冒烟 PASS。**⚠️ 其中 A 组的清理判据已被真机复核推翻并修正（基线 439→445），先读 §5.5。**

**顺带修掉一个"假修复"**：用户截图质问"数值怎么还在"——查实你那份 613 实体的世界是**旧代码造的账**（`savedAt` 晚于部署时间但内容是旧抽取的：名册 290 条带 `evidence` = 已删的属性轮产物）。**⚠️ 下一棒的注意与本节后半段已被第二十五棒修正段推翻，务必先读 §5.5**：本棒当时的结论是"新代码不写这些数了，但没有人去擦旧账 → 补清理（A 组）"，而实机复验（备份逐份对账）证明**A 组的清理判据本身不成立**——旧法要求"四维**恰好全等**旧默认值"才批，而真实脏数据是**混合行**（一维真值 + 其余几维旧默认），整行判词恒假 → **500 维假数一格没清**。详见 §5.5。

## 1. 本棒主线（按时间序）

1. 接管：读 ANCHOR / dev-process / leg24 交接 / 台账 / 决策索引 → 复核基线 **396/396** → 报三个卡点（未提交的归档改动、台账行号指偏、A「查书」通道为空）
2. 用户拍板：补提交归档动作 + 修指针 → 落 `88a5756`（含"按行号引用台账必然陈旧"的根因留档）
3. 用户追问三连（属性怎么填／字段要不要改／**"耳目"这种话书里频率极低**／**不要出蠢设计，出设计前十思**）→ 我三次设计被否：四维浮点、"耳目"原话、**"编制推能见度"**（用户：谁他妈一定有组织，组织又他妈和情报有什么关系）
4. 用户定调：**`intel` 交 LLM 每轮填**（走现有 `stateChanges` 通道，不改架构）
5. 用户令"当检察官" → 四路并行深审（镜头/能见度 · 结算管线 · 初始化+契约 · 存储+编排）+ 我审 settle 主线 → 汇总分级报告（真缺陷／死链／恒真开关／单向门／设计缺口），**并当场驳回/降级三条**（见 §3）
6. 用户令"全部修掉，子代理也得上，我要马上见成品测试" → 四路子代理并行 + 我接玩家通道 + 契约层 + 存储收口
7. 端到端冒烟当场抓出子代理改出的真崩溃（`pack.js:143 est is not defined`）→ 打回重修
8. T1 子代理**中途失败**（未交付）→ 我接手它的存储面：修 `plan.hot` 语义冲突、纠正一处算错的测试断言、修夹具不真实（同一段编年不会反复轮转）
9. 用户拍板位置集口径（"位置集从书里现成的地名建"）→ D 组补齐（`derivePositions` + 入账守闸）
10. 全量验证：439/439 全绿 + 冒烟 PASS + 语法零失败 → 台账回填（**修正后 445/445**，见 §5.5）

## 2. 八组交付现状（全部零新增机制，只修已查实的缺陷）

| 组 | 内容 | 关键改动 |
|---|---|---|
| **A 旧账假数** | 清掉旧代码替实体编的四维默认值 | `src/settle.js` `migrateLegacyAttrs`（**判据已于第二十五棒修正为逐维**，见 §5.5；接线 `web/index.js` `loadWorld`，一次性标记 `meta.legacyAttrsMigratedAt` + 留档 `meta.legacyAttrsPurged`） |
| **B 玩家通道** | 让"玩家这枚棋子"真的存在 | `web/index.js` 新增 `attachPlayerPiece`／`namePlayerPiece`／`nextPlayerId`；`bus['init-world']` 接入 `runPlayerSetup`（**只在第一次**解析）；`src/player-inject.js` **默认值表整条作废**（`PLAYER_INJECT_DEFAULTS = {}`，只落有依据的维度；顺手堵住"解析器返回字符串也照塞"的洞） |
| **C 能见度** | 掩码收敛为单一实现（删内联副本） | `src/streams.js` 改调 `visibilityMask`；行为逐字节不变 |
| **D 位置死链** | 位置集从书里现成的地名建 | `web/index.js` `derivePositions(setting)`（`kind='location'` 条目 + 各条目明述的 `location`；兜底词「未明」恒在集内且第一；含 `_` 的结构标签不当位置；`POSITIONS_CAP=60`）；`src/abstract.js` `seedBookEntities` 加守闸（名册所在必须 ∈ 位置集，否则落兜底词——否则整步被拒=世界停摆） |
| **E 数据丢失四条** | ①卷号自增并跨刷新延续 ②**先落卷成功才允许提交剥段世界** ③`max_tokens` 4096→16384 ④名册入账落盘 | `src/storage.js`（`nextVolume`／`normalizeVolumeSeq`／`planChronicleRotation` 返回 `hot`=未剥段 + `applied`=剥段后／`countLedgerEntries`）；`web/index.js` `ensureChronicleRotated`（先 put、失败返回 `{ok:false}`）；`src/transport-http.js`／`src/transport-config.js` |
| **F 属性留痕** | ①`attr` 键白名单 ②`actor` 存在性 ③属性变更写编年 | `src/check-step.js`（白名单唯一真源 `INBORN_ATTR_KEYS`，越界拒整步；`actor` 必须是在册实体）；`src/settle.js` `pushAttrChronicle`／`causePhrase`（`kind:'state'`，只记**实际生效**变更） |
| **G 死代码/未执行闸** | ①30k 预算首次有强制点 ②"波及 ≤3"首次真正执行 ③删死函数 | `src/pack.js`（`trimPack` 接进 `buildEvolutionPack` + `pack.trimmed` 机器可读留痕；模块级唯一估体函数）；`src/check-step.js`（ripples 超 `RIPPLE_TARGET_CAP` 拒整步）；`src/weight.js` 删 `maxRippleTargets` |
| **H 文案与文档** | ①提示词不再教"分量决定谁值得动" ②`attrs` 承诺改正 ③界面不再承诺默认值 ④手册基线 403→396 | `src/prompts.js`（铁律 5 改写 + 输入清单删"分量" + `MAIN_PROMPT_V` → `v2-agenda-t1-5`）；`src/schemas/world-step.schema.js` 注释；`src/render.js` 设置页文案；`docs/dev-process.md` |

**新增测试 43 则**，其中三个新文件：`test/player-wiring.test.js`(6)、`test/positions.test.js`(5)、`test/storage-rotation-persist.test.js`(7)。

## 3. 本棒实测账（可复跑）

| 实验 | 结果 |
|---|---|
| **全量测试（本棒当时）** | `node --test` → **439/439 全绿**（审计前 396）；**修正后基线 445/445**（见 §5.5） |
| **端到端冒烟** | `node demo/smoke-demo.js` → **PASS**（50 tick、警告 0、终局 8270 字节；并把 demo 里写死的"预算提案 4000"改成读 `EVOLUTION_BUDGET_TOKENS`=30000） |
| **语法** | `node --check` 全 src + web + demo → **零失败** |
| **A 组实况规模（旧法·已被修正）** | 拿用户 613 实体世界实测：旧判据（四维恰好全等）清掉 **330 行**（263 全 0.15 + 67 全 0.25），37 个玩家注入 0.5×4 **不动**；幂等逐字节一致；过 `ssotSchema`。**⚠️ 这只是"纯默认行"的一半账——混合行 500 维全部漏清，见 §5.5** |
| **B 组效力** | 新测试实测：模型写玩家的**行动/属性/覆灭三种全部被拦**（旧法因 `playerId` 缺失全部放行） |
| **D 组效力** | 书里写了所在的实体**真的分散到不同地方**（大虞→中天神洲·中州、万法阁→昆仑山），没写的落「未明」；集外所在落兜底词 |
| **E 组效力** | 连轮两次写 `卷1`/`卷2` 两个不同键；写失败时**编年 12 行一行不少**且失败如实上报；跨刷新接号（卷3） |
| **F 组效力** | 编年行实测：`「独行客」人脉 0.4→0.5（因事件「旧事」）` |
| **端到端抓到的真崩溃** | `pack.js:143 ReferenceError: est is not defined`（子代理接线时辅助函数悬空）——`node --test` 全绿但 `smoke-demo` 崩，**说明单测绿≠成品**；已打回修好并补真触发裁剪的集成用例 |
| **驳回/降级三条（防重走）** | ①"`settle.js:322` 父盘算判空会整轮抛异常"——我三次构造复现**均未打中**，降级"代码缺口、可达性未证"②"静默是单向门、静默者永远无法自救"——降级（被点名即可应答）③"死人进包=自相矛盾"——降级（是噪声不是矛盾） |
| **测试侧一处算错（我方修正）** | E1「卷号延续」原断言三处合计写 13，实测 **14**（轮转是搬家不是删除：12 起始 + 2 新增）——改断言，不迁就错数改实现 |
| **我的一条错指令被子代理顶回** | 我写"`computeWeight*` 已无引擎消费者"，它实读发现四处仍有调用点（`abstract.js:641`／`settle.js`／`setting.js:45`／`pack.js membersOf`）→ 按事实改写文档 |

## 4. 铁律（延续 + 本棒新增）

1–11 条延续 leg24（没命令不动代码／上限先报批／文档开口先给人话版／改码三件事／审批 never／先 read 再 edit／检验三问／数据说话／分层归属／说人话是最高准则／**不能通用就直接丢**）。
12. **改判据=改承重墙**（leg24 新增，建议入 dev-process）。
13. **本棒新增（用户当场纠正，建议入 dev-process）**：**出设计前先十思**——具体三条自罚：①**不许拿"书里极低频的句式"当机制地基**（我拿"耳目遍天下"当 intel 来源，用户实测这种话出现频率极低）②**不许猜人的属性再拿猜出来的东西当判据**（我拿"有没有组织"推能见度，用户：谁他妈一定有组织，组织又他妈和情报有什么关系）③**任何新判据先量数据覆盖率再谈设计**（本项目已栽过三次：逐名号问 parent 3%、照书办扫标签 8 本只 1 本可用、词表判据漏一个词就让三国掉到 none）。

## 5. 下一棒任务书

1. **第一动作**：确认用户是否已验收 → **落 git**（36 个文件全在工作区未提交；建议按八组分笔提交或一笔提交+台账已记）。**验前不要动代码**。
2. **主线（等用户点单）**：
   - **甲｜掩码阈值语义**（本棒唯一明确留给用户拍板的机制缺口）：现在 `m = 情报 × 位置`，最瞎情形 = 0.25 恰好等于阈值 → **恒真=永远可见**。要不要让它真能挡？挡到什么程度？（用户已定 `intel` 由 LLM 每轮填，所以这条一旦定，能见度就真的开始工作）
   - **乙｜实机复验**（用户环境）：Ctrl+F5 → ①建新世界（应看到：位置集 N 处、玩家棋子入席、名册实体四维为空）②`intel` 是否被模型提议落账并写编年 ③面板"有据/无据"是否与新口径一致。
   - **丙｜旧世界处置**（⚠️ **已被第二十五棒修正段作废，见 §5.5**）：原文写"用户那份 613 实体世界首次打开面板时会自动跑一次清理（清 330 个假数）"——实际上那份旧世界在 `2026-09-10 20:43` 已被**新世界的 623 实体账本覆盖**（`init-world` 重跑），原话已无对象；330 个假数现在只存在于备份里（`backups/chat___z_20260910-203735.jsonl` 等）。
3. **必须守住的三条**（否则白改）：①空值就空着，不许默认值兜底 ②引擎不裁胜负，只拦矛盾 ③每条变更留痕。
4. **不要做的事**：①不要再让引擎算"谁强谁弱" ②不要把词表/低频句式当判据 ③不要据已被否掉的细案开工（`structure-layer-spec.md` 仍不得据以开工）④**不要再单独动退休/门控判据**（两次证伪的教训）⑤**不要再出"猜人属性"的设计**（本棒新增，用户明令）。
5. **现场**：`node --test` 必须**在项目目录内**跑（当前基线 **445/445**，修正后）；真模型通道 `loadStPresetConfig()` + `createHttpTransport`（密钥仅本机读、不打印）；部署 junction 在 `F:\jiuguanai\...\third-party\story-world-v2`（Ctrl+F5 即载）。
6. **已知小瑕（不影响功能，留待顺手清）**：`docs/ledger.md` L171（H 组行）因追加时的表格瑕疵少一格（首格与步骤格合并），内容完整、读得通；`suggestion.doc`（225KB，9-9 那份）仍未跟踪，用户未表态。

## 5.5 第二十五棒修正段（真机复核 · 用户令「补，改，但是 bug 要修」）

> 这一节**推翻本文件 §0/§3/§5丙/§6 A4 的旧叙述**。判据全部来自用户 ST 目录里的真账（`data/default-user/`：现役聊天 + `backups/` 逐小时自动备份，只读）。

### ① 事实：A 组的清理**确实跑过**，但**判据漏了 500 维**（用户"兵力怎么还存在"是真的）

`backups/chat___z_20260910-203735.jsonl`（20:37，旧法迁移后的第一次落盘）实证：

```
实体 613 | meta keys: tick,simLog,legacyAttrsMigratedAt,legacyAttrsPurged
legacyAttrsMigratedAt = 2 | legacyAttrsPurged = 330 条 | 带四维实体 = 283 行（37 全 0.5 玩家注入 + 246 名册行）
```

**旧判据的病根**：要求四维**恰好全等**旧默认值（character 0.15 / faction 0.25）才批。而真实脏数据是**混合行**——一维真值 + 其余几维旧默认：

```
阐教   bookEntities.attrs={office:0.5} evidence="人族正道仙修第一传承"
       → 账上 {hardPower:.25, office:.5, network:.25, intel:.25}   ← 整行判词恒假 → 一格不清
大虞   书 {hardPower:.8, office:.9} → 账上 {.85, .9, .25, .25}
天机阁 书 {network:.9, intel:.9}   → 账上 {.25, .25, .9, .9}
```

**量级**：283 行共 1132 维，其中 **500 维是旧默认值**（兵力 71 / 权位 63 / 人脉 185 / 耳目 181），旧法清 0 维。

### ② 修正：判据「整行全等」→「逐维」，两条豁免各自有可查来源

`src/settle.js` `migrateLegacyAttrs`：**每个维度独立判**——`该维 == 本类别旧默认值 ∧ 书里没写这一维的数值 ∧ 编年里该实体没有过属性变更` → 删该维，否则保留。

- 豁免①**书里明写这一维**（`bookEntities[].attrs[k]` 是数）：旧抽取产出 attrs 与 evidence **同键集**——故逐维判**不需要**整条 evidence 串；而"整条 evidence 给另外三维当挡箭牌"正是旧法漏清 500 维的机理。
- 豁免②**编年里该实体有过属性变更**：那条 `「X」兵力 a→b` 就是这维的来路（真跑出来的数哪怕恰好 0.25 也不许当假数删）。leg24 片4 的属性编年让这条判据**首次成为可能**。

**真账实跑（生产函数跑 20:37 那份旧账）**：迁移前 283 行 / 1132 维 → 迁移后 **283 行 / 632 维**（本次清 **500 维**，留档并入 `legacyAttrsPurged` 1820 维 = 旧法 330 行×4 + 本次 500）；**保留的 632 维逐维核对来源：书里明写 632 + 编年豁免 0 + 非默认值 0 → 存疑保留 0 维**（一条真值都没掉）；实体行序/名号一字不变；连跑两次逐字节一致（幂等闸语义不变）。

**测试**：445/445（`settle.test.js` 净增 6 则）+ **448/448**（`abstract.test.js` 再 +3 则，见 ⑥）。**冒烟** PASS；`node --check` 97 文件零失败。

**已知边界（登记，不在本棒解）**：`meta.legacyAttrsMigratedAt` 是**硬闸**——**已被旧判据迁过**的账（含仅存于备份里的那个 613 实体世界）再打开时不会重扫，那 500 维在那儿会一直留着。本棒没有为了"再清一次"去动幂等语义（动它=动承重墙，且会让每次载入都重扫大账）；新判据只对**未迁过的账**（用户从旧导出文件导入）生效。要清旧账里的残留，只能走"重新初始化（当前实况，20:43 那次就是这个结果）"或单独立项做一次版本化重迁。

### ③ 事实：旧世界**已被覆盖**，不是被清理

现役聊天（`chats/大荒z/…jsonl`）与全部备份逐份对账得时间线（本地时）：

| 时刻 | 事件 | 账本 |
|---|---|---|
| 20:30 | 工作区新代码落位（junction 指向项目根，ST 刷新即载） | — |
| 20:37 | 旧账**首次被新代码读到** → 旧判据清理（清 330 行、留 283 行、打标记） | 613 实体 · tick 2 |
| **20:43** | **`init-world` 重跑建新世界** | **623 实体 · tick 0 · `playerId=e_p1` · 名册 `attrs:{}` · `evidence` 0 条 · 位置集 60 处** |
| 20:43→21:15 | 连演 8 轮，逐轮自动备份 t0→t8 | 有值实体 0→4 |

⇒ 交接原文"那份 613 实体世界首次打开会自动清 330 个假数、建议先导出备份"**已无对象**；330 条留档现在只活在备份里。

### ④ 事实：**属性回写是全链路通的**（"属性也没回写"不成立，但"书那层没回写"成立）

逐轮备份 + 编年逐条对账（11 条属性编年 **11/11 吻合**，含同 tick 同键累加、负向无基线被拒、边界钳制）：

| 实体 | 账上（t8） | 编年链 |
|---|---|---|
| 妖族 | `hardPower 0.15000000000000002` | t1 账上无数→0.05 → t2 →0.15000000000000002 |
| 昆仑道宫 | `hardPower 0` | t3 →0.05 → t4 →0.1 → t5 →0.08 → t6 →0 |
| 万妖盟 | `hardPower 0` · `intel 0.05` | t5 →0.02 → t7 →0；t4 耳目 →0.05 |
| 吞天妖王 | `hardPower 0.4` | t6 →0.1 → t7 →0.3 → t8 →0.4 |

同时验证了片4 的属性编年**真在写**（旧账/旧代码里这条通道是空的）。补充断言脚本：真 `runTick` 夹具复算（同 tick 同键两条累加、负控）。

**真正的"没回写"在书那一层**：旧账 290 条 `bookEntities` 带 `attrs`+`evidence`（`阐教 office 0.5`、`天机阁 network/intel 0.9` 等真值），当前世界的 762 条书条目 **attrs 0 条 / evidence 0 条**——leg24 片1"停抄书"之后新抽取**不再产这两样**，重开世界就整体丢了。若这类"书级真值"要保，得单独立项（不是本棒的面）。

### ⑤ 用户追问澄清：他说的"属性没回写"是 **位置 / 归属 / 其他字段**——逐项量完，三条链路里只有四维是通的

用户原话："我说的属性没回写是其他的属性懂吗，位置，归属还有其他的"。**这一条成立**，而且比四维严重（当前世界实测）：

| 字段 | 账上写回 | 书里/世界里的来源 | 链路状态 |
|---|---|---|---|
| **四维 attrs** | **4 行**（妖族/昆仑道宫/万妖盟/吞天妖王） | 模型每轮提议 | ✅ 唯一全程通的 |
| **location** | **0 / 623**（全员 `未明`） | 位置集 60 处已备好，**没有任何入口写** | ❌ 断 |
| **parent / branches / organs** | **0 / 623**（归属空着） | 书里 9 条上级声明（照书办）| ❌ 断（真 bug，已修，见 ⑥） |

**"位置"为什么是 0（三重断口，逐条有代码位）**：①抽取端 `buildAbstractPrompt` 的名册形状**只收 `{name, kind}`**（leg24 片1 停抄书的显式决定，注释原话"上级/所在/种族/四维属性都不再抽"），`sanitizeCanon` 同口径**只留 name/kind/parent**——**location 连生产都不生产**；②`seedBookEntities` 的写入支只有 `(b.location && positions.includes(b.location)) ? b.location : home`（`abstract.js:565`）——b 上没有 location 就只能是 home=`未明`；③**账本里没有任何"改位置"通道**：`world-step.schema` 只有 `newEntities[].location`（新实体入位）与 `events[].position`，`stateChanges` 只有 `attr`——**存量实体无路可走**（`pack.js:92` 倒是把 location 喂给模型看，模型看得见、写不回）。

**旧账对照（说明这是"停抄书"的代价，不是新 bug）**：旧抽取（模型读的）书里 **153 条 location / 475 条 parent** → 账上写回 **111 / 400**；迁移后旧世界实体 location 已写 111、parent 已写 400。新世界两栏全 0。

**"用到时查书"是半截工程**：`abstract.js:28/161/386` 三处写着"位置与归属改为**用到时查书**（片2 接线）"，但**片2 从没做**——全仓除注释外无任何查书代码（`grep 用到时查书` 只命中注释与渲染层文案）。⇒ 停抄书停了生产，替代通道没接上，字段就成了死字段。

**位置写回的三个可选项（待用户点单；覆盖率已按纪律 13 先量）**：

| 选项 | 做法 | 实测能捞回 | 代价 |
|---|---|---|---|
| **甲｜抽取端回产 location** | 名册轮形状加回 `location`（书里 `所在地：X` 原文串）+ `sanitizeCanon` 收 + `seedBookEntities` 已有写入支 | **67 / 623**（10.8%）——世界书 235 条里 87 条写得到位置（`所在地：中天神洲·中州` 这类） | 名册输出字段变多（K49 瘦身曾因输出膨胀拆半降级）；属"停抄书"回退一格，需用户点头 |
| **乙｜用到时查书（把片2 做掉）** | 初始化后按名号查世界书条目 → 抽 `所在地` 串 → 写回 `b.location`（零模型调用，可复现） | 同为 **67 / 623**（书里写得到位置的上限） | 要新做"世界书条目 ↔ 账上实体"匹配（实测：comment 全等 67 / key 含名 533 / comment 含名 104；**85 个实体在书上根本没有条目**） |
| **丙｜给存量实体开"移动"通道** | `world-step.schema` 加 `stateChanges[].location`（或 `moves[]`），引擎按位置集校验后写回 | 由玩法长出来，上限 = 位置集 60 处 | **机制改动**（改契约=承重墙），须用户明令；且模型得真愿意填（8 轮里它只填了 4 个四维值） |

三者不互斥：甲/乙解决"开局位置"，丙解决"之后能动"。**本棒只动了 ⑥ 的 parent 名号净化**（改名号的 bug，不改机制），甲/乙/丙一律等用户点头。

### ⑥ 真 bug + 已修：上级名号带前导 `/` → 9 条真归属全被丢弃

**实况**（当前世界书 `bookEntities` 里带 parent 的全部 9 条）：`太昊仙洲 → "/太素帝"`、`界渊长城 → "/渡虚帝"`、`红尘情天 → "/织命帝"`…——模型把书里的**层级路径**照抄进了 parent。

**断在哪**：`abstract.js` `sanitizeCanon` 只做 `String(it.parent).trim()`（斜杠留着）→ `seedBookEntities` 的 `resolveSeedTarget(b.parent, idx)` 拿 `/太素帝` 去名册索引查 → 查不到 → `missWords()` 判"上级不在册 → 弃隶属，名号照常入账" → 界面「归属空着」。

**修**：新增 `normalizeParentName`（只剥**前导**层级符 `/ ／ \ > · - — –` 与空白，名字本体一字不改），在**两个入口**收口——`sanitizeCanon`（模型抽取）与 `applyDeclaredToRoster`（照书办标签声明）。**真账验证（当前世界内存重跑，不写盘）**：9 条 parent 全部写回（`太昊仙洲 → 太素帝`、`界渊长城 → 渡虚帝`…），`seedBookEntities` 返回 **warnings 0**（此前 9 条警告），账上 parent 从 **0 → 9**。测试 +3 则（净化形态 / 净化层收口 / 照书办同口径）→ 基线 **448/448**。

### ⑦ 现场只剩"用户说的不算"的一处

盘上**没有任何一份账**与"Ctrl+F5 后仍见兵力+在办盘算"相符（最接近的是备份里 20:37 那份）。**判据脚本**：ST 聊天里按 F12 贴

```js
const w = SillyTavern.getContext().chatMetadata.story_world_v2.world;
[w.entities.length, w.entities.filter(e=>e.attrs&&Object.keys(e.attrs).length).length, w.meta.tick, w.meta.legacyAttrsMigratedAt]
```

期望 `[623, 4, 8, undefined]`——若与面板显示不符，就是**浏览器缓存了旧的 `web/index.js`**（渲染的账与盘上不是同一份），下一棒从加载路径查。

## 6. 未决登记（带触发条件）

| # | 事项 | 触发条件 | 前置数据 |
|---|---|---|---|
| A1 | **掩码阈值语义**（"情报不够看不见远方"要不要真挡） | 用户点单 | 本棒 §5 甲 + `src/streams.js` 现公式 |
| A2 | 两条新纪律入 `dev-process`（leg24 第 12 条"改判据先量清" + 本棒第 13 条"出设计前先十思"） | 用户点头 | 本文件 §4 |
| A3 | 实机复验（建新世界/位置集/玩家棋子/intel 落账/无据标记） | 用户环境 Ctrl+F5 | 本文件 §5 乙 |
| A4 | ~~旧世界首次打开会自动清理假数~~ **【已作废·见 §5.5】** 该 613 实体世界已于 20:43 被 623 实体新账本覆盖（330 条留档只在备份里）。现行事实：**判据已改逐维**，任何**导入了旧账**的聊天首次打开会自动跑一次逐维清理（清 500 维量级的假数、逐键留档 `meta.legacyAttrsPurged`）——导入旧导出文件前**仍建议备份** | 用户导入旧账/旧导出前 | 本文件 §5.5 ②（真账实跑：1132 维 → 632 维） |
| A5 | `seedBookEntities` 的 warnings 在真跑里仍被丢弃（两处调用点不接返回值） | 用户点单 | 本棒审计 F 组未覆盖项，`src/abstract.js` `out.warnings` |
| A6 | 属性变更编年未带 `eventRef`（只写 `kind:'state'`） | 需要链视图串属性变更时 | `src/settle.js` `pushAttrChronicle` |
| A7 | `docs/ledger.md` L171 表格格数小瑕 / `suggestion.doc` 未跟踪 | 顺手清理 | 本文件 §5.6 |

---

*第二十五棒交接 · 2026-09-11 · 检察官审计 + A–H 八组修复 · **修正后基线 448/448**（本棒当时 439）· 改动全在工作区未提交（最后提交 `88a5756`）· 下一动作：用户验收 → 落 git → 再谈掩码阈值语义（§5 甲）。**真机复核与判据修正见 §5.5（用户令「补，改，但是 bug 要修」；§5.5⑥=上级名号净化已修，§5.5⑤=位置写回仍是断口，待用户点单）。***
