# 会话交接 · story-world v2 · 第三十三棒（落盘 22 文件 · ★甲/乙两档被实测否掉 · 位置＝自由文本 · 插件总闸）

> **接手第一件事：读 `docs/START-HERE.md`（一页读完即可开工）。**
> ⚠ 其中 §1.5 是**最高准则**：**没有用户当次明确命令，不许改任何文件的一个字节**。**本文件里的"下一步"都是待选项。**
> ⚠⚠ 同级铁律：**含中文的文件禁止用 PowerShell 读写**（leg32 踩过；**本棒我又踩了半次，见 §7.1**）。
> ⚠⚠ 同级铁律：**判语法必须用真导入**（`node --check` 给假绿，见 §7.2）。
> 状态：**工作区干净（0 条）**；`main` 停在 **`09c7ba8`**（本棒 8 笔已落，**未推送**）。
> ★本棒区间锚点：**`947aabe` → HEAD**（`git rev-list --count 947aabe..HEAD` 现读 = 8）。
> 上一棒入口：`docs/session-handoff-2026-09-12-leg32.md`。

---

## 0. 人话版：这一棒干了什么

起点是用户交来 leg32 交接文档，终点是「**交接吧**」。中间用户拍了 **四件事**，本棒**逐件做完并量穿**——★**其中两件推翻了 leg32 的结论**，一件改了承重墙，一件是纯新增入口。

| # | 用户说 | 量出来的真东西 | 落点 |
|---|---|---|---|
| ① | 「现在落盘（推荐，仅本地不推送）」 | leg32 积压 **22 个文件**落成 **4 笔**（区间 `947aabe` → HEAD）；`web/index.js` 横跨四单元 ⇒ 照旧**文件级切不开、不硬拆** | §1 |
| ② | 「先出数再拍：做甲/乙两档对照实验」 | ★★**甲、乙两档一起被否**——`挂主线的新盘算 = 0`（三臂无一例外）、结构面**本来就不塌缩**、塌缩在**内容面**且改包对它**零效果** | §2 |
| ③ | 「修「（推）」位置泄漏」→ 追问「没有位置的怎么办」→「位置集的功能换个世界书成功？」→「**位置变成自由文本，位置集干脆删了**」 | ★**跨书不成立（3/8）** + 大书里 cap 在**切真地名**（134→59）⇒ 位置**降级为自由文本**（集合保留为参照表）；★顺带挖出 `location` 是 required ⇒ "不给位置"**根本表达不出来** | §3 |
| ④ | 「顺便加一个启动和关闭插件的入口，要不然这个插件会直接自动生效」 | 病灶＝`es.on(MESSAGE_RECEIVED, …advance())` ⇒ **每收一条消息自动推一轮**；加了**插件总闸**+存量世界**一次性迁移**（防把正在跑的世界悄悄按停） | §4 |

---

## 1. 第一件：P-C 落盘（4 笔）

`947aabe` → **`b198115`**，**未推送**：

| 笔 | 提交 | 内容 |
|---|---|---|
| A | `80ef884` | 引擎闸上限放开一档 `2/15/10 → 3/20/15` + 入局第四型 `entity` |
| B | `db0fb0c` | 编排层往事面 + 待启用名单 · 契约层铁律 6/9/10/11/12 · 面板读真源 + 玩家那行打标 |
| C | `cc17450` | 测试 15 条新判据 + 三条旧口径的夹具同步改 |
| D | `b198115` | 文档台账 + 交接（含 leg32 交接文档本体） |

★**为什么是 4 笔不是 leg32 那"九格"**：`web/index.js` 一个文件里同时住着 leg32 的玩家认领、CSS 版本、状态栏计数**四个逻辑单元** ⇒ **文件级切不开**，硬拆会造出编译不过的中间提交（leg31/leg32 都留过这条档，本棒照办）。
★闸门：落盘前复跑 **562/562** + 冒烟 **PASS**；用 `git diff --staged --name-only` 判"落干净没有"（★`git log -1 -- <file>` 会骗人，leg31 留过档）。

---

## 2. ★★第二件：甲/乙两档真模型对跑 —— **两档一起被否**

**装置**：新建 `demo/measure-single-focus-ab.js`（**纯只读**：不改仓库真源、不写真账、真账走副本）。
- `connectedComponents()`：按**三种账上已有的引用关系**求连通分量（`source.ref` / 共享 `ripples` / 同源盘算）——**纯图论、零判断、确定性**；
- 三臂**包变体**：base（真包逐字节不动）· 甲（「主线／各自的线」两栏 + 留位如实）· 乙（不给主线框、自立线排最前）；
- ★**改包不变式自证**：未决事件 id 集合改包前后**逐字相等且不重复** ⇒ **它当场抓出了我自己的一个真 bug**（乙臂一度把池外分量列两次 ⇒ 17 条变 19 行）；
- 真模型走 `runTick` **同构流程**（`buildEvolutionPack` → `runMainCall` → `settleTick`），**`dialogue` 传空串 ⇒ `moveFact=null` ⇒ 玩家不模拟**（用户令）。

**实测**（同起点真账副本 tick 53 · 三臂各 5 tick · `gemini-3.1-pro-preview`）：

| 臂 | 成功/调用 | 各异主均/峰 | 最大坨占比 | 新盘算 | **挂主线的** | 自立 |
|---|---|---|---|---|---|---|
| base | 5/5 | 8.6 / 10 | 0.12 | 6 | **0** | 6 |
| 甲 | 3/5 | 10 / 12 | 0.10 | 6 | **0** | 6 |
| 乙 | 4/5 | 8.75 / 10 | 0.14 | 5 | **0** | 5 |

**四条结论**：
1. ★**「挂主线的新盘算 = 0，三臂无一例外」**——模型**从来不**把新盘算挂到那条大事件链上，每次都选 `自立(state)` ⇒ leg32 假设"模型只能在那个框里分配角色"**在真模型上不成立**；
2. ★**结构面从来就不塌缩**：起点 8 条在飞盘算落在 **7 个**互不相干的焦点里（最大一坨占 25%），其中 **6 条本来就是 `state` 源** ⇒ **按候选甲自己的判据，它们早就是"各自的线"了**；
3. ★★**塌缩在内容面、且改包对它零效果**——机械口径（n=2..6 字面子串，≥2 条共享的**极大**片段）：起点 8 条 **7/8** 踩「死煞×6／大荒×3／大虞×2」；本轮新生目标 base **5/6**、甲 **6/6**、乙 **4/5**，逐条是同一件事的不同角度（「接引大荒死煞以壮大幽都」「收集大荒死煞变异毒虫」「汲取变异死煞培育万毒蛛王」）；
4. 结构面三臂差异（8.6／10／8.75，n=3–5）**是噪声**，不足以支撑任何机制（甲臂另含 2 次位置类拒整步、乙臂 1 次超时）。

**⇒ 把 leg32 的核心发现收窄成更准的版本**：不是"引擎只递给模型一个框"（框一直有好几个、且模型从不用主线那个），而是**"世界自己只有一件事在长"**——17 条未决事件里 **12 条（72%）是同一场大乱的下游**，模型眼前可挂的锚只有一个。

★**附带读到两件**：
- **真账历史（`meta.simLog` 53 轮）**：事件 **1.08 件/轮**、编年 ~5 行/轮、包体仅占预算 **39%（11,831/30,000）**、有拒绝的轮次 **11/53**（且多为"同名丢弃"这类 leg32f 降级）⇒ **引擎产出量不是瓶颈**。★另注：本实验 base 臂 5 tick 生 6 条新盘算（≈1.2/轮），**高于真账历史的 0.4/轮**——口径差异须留心（实验无玩家、无对话），**不许拿它当"变宽了"的证据**。
- ★**位置注解泄漏（真缺陷，间歇复现）**：模型把实体表 location 列的 `北俱荒洲（推）` **原样**抄进 `newEvents[].position`/`actions[].position` ⇒ 位置闸认不出 ⇒ **拒整步**。根因＝leg31 把「（推）」标进实体表（`pack.js` 的 `locationNote`）**而没有任何地方把它剥掉**。

**同笔修（`028a5e2`）**：新增 `src/position.js`（**叶子模块**——`settle.js` 已 import `check-step.js`，真源若放 settle 会**成环**）导出 `normalizePosition`（只剥尾部注解 + trim）；`check-step` 就地归一；契约层铁律 3 补明话（`MAIN_PROMPT_V` → `v2-agenda-t1-15`）；`render.js` 构建号 → `leg33a-position-note-stripped`。
★**接线期被自己的用例抓红一次**：`export { X } from './y.js'` **不建立本地绑定** ⇒ 模块内 `normalizePosition is not defined`。**留档**。

---

## 3. ★★第三件：位置＝自由文本（**改的是承重墙，用户当场拍的**）

### 3.1 用户四连问逼出的三条硬数据

| 问 | 答案 |
|---|---|
| 「没有位置的怎么办」 | 冷门池 603 个 ⇒ 明写位置 **138** · **可由父级推出 ★0**（父级自己也没位置：`太素帝/织命帝/渡虚帝/虞/万妖盟` 全「未明」）· 无位置 **465**；再分层 ②有归属 99 ③有痕迹 290 ④**只有名字与类别 76** |
| ④ 那 76 个书里到底有没有 | 读世界书 `大荒-姬元真.json`：书里**全都有**（**76/76**）、**21/76** 有明确驻地表达式，但**没一个正好等于账上位置项**（书里写 `第十三重天·太清境西方灵山区`／`第十六重天·碧落东海之巅`／`幽冥审判层·酆都鬼城最北端`） |
| 「位置集换个世界书成功？」 | ★**不成立**：8 本真实世界书里**只有 3 本**有干净地名表（仓库自带 `demo/audit-mechanism-genericity.js` 的"机制②位置继承"读数 **3/8**），其余 4 本退化成 `['未明']` |
| 那为什么大书里也不对 | ★**`derivePositions` 在切真地名**：真账 canon **751 条**（`kind='location'` **134** 条、`location` 字段 **0** 条）而旧 `POSITIONS_CAP=60` **按书序截到 59** ⇒ 切掉 **75** 个真地名（`青阳镇/绝剑谷/落英谷/玉虚峰/万兽谷/太清境/三十三重天/万魔殿/噬道渊/血祭深渊/弑神殿…`）⇒ **模型写书里真有的地名反被拒整步** |

### 3.2 落地（`a22ee5c`）

| 层 | 改什么 |
|---|---|
| 引擎 | `check-step.js` 位置段：**拒整步 → 留痕**（新增 `warnings` 返回面，`位置集外:` 开头、**不进 errors** ⇒ **不计入拒签率分子**）· `settle.js` 的 `spawnEntities` **集外照收**（旧法抹成「未明」）· `web/index.js` 的 `derivePositions` **不再截断**（`POSITIONS_CAP` 作废＝`Infinity`，保留形参兼容旧调用点） |
| 契约 | `prompts.js` 铁律 3 重写（**位置不是闸**、书里别处地名也照写、**「（推）」注解照旧必须剥**）⇒ `MAIN_PROMPT_V` → **`v2-agenda-t1-16`** · `world-step.schema.js` 的 `newEntities[].location` **从 required 拿掉** |
| 渲染 | 构建号 → `leg33c-free-text-position` |

★**两处"用户字面 vs 实际"的偏差，如实记**：
1. 用户说"位置集干脆删了"，我**保留了 `context.positions` 当参照表**（不再是闸）——因为 `settle.js` 的 `newEntities` 位置归一 与 `entity-lookup.js` 的 `deriveLocationFromBook` **都读它**，真删掉会让**新人入局 100% 拿不到位置**；
2. ★顺手发现 **`newEntities[].location` 原本是 `required`** ⇒ "不给位置"**根本表达不出来**（我写的"空值才归未明"分支本是**死代码**，被自己的新用例当场抓红）⇒ 已拿掉，这才让"**空着就是空着**"（§2 第 2 条）在位置上真正成立。

**真账影响**：位置集 **60 → 135 项**（945 字符 ≈ **283 est**，且不参与 `trimPack` 裁剪）⇒ 被旧 cap 切掉的真地名全回来了。

---

## 4. 第四件：插件总闸（`09c7ba8`）

**病灶**：`web/index.js` 的 `setupAsyncTicks` 里 `es.on(MESSAGE_RECEIVED, () => sw2TickQueue.advance())`
⇒ **每收到一条消息自动推进一轮**；而 `boot()` 在模块载入即挂 ⇒ **装上就自动生效**。

**口径三条**：
1. **总闸只管"自动"**：关掉后发消息不推进、切聊天不自动载入；**手动永不被闸**（观棋窗口照常打开、面板照常渲染、「推进一轮」走 `dispatchAction('advance-world')` **不经过这里**）；
2. **缺省关**（`SWITCH_PARAMS.autoAdvance.def='0'`，照本仓开关惯例）⇒ 正对用户原话；
3. ★**但存量世界要给一次性迁移**——真账实测用户那本 `memoryEnabled='1'`＝**正在用**；若因"缺键=关"就把它**悄悄按停**，那是事故不是功能 ⇒ `ensureAutoAdvanceKey`：**有推进史（`meta.simLog` 非空）且该键从未写过** ⇒ 迁成 `'1'`（升级前后一字不变）、**全新世界留 `'0'`**；★**幂等**——键已存在就**一个字节不碰**（**你手动关掉的 `'0'` 永不被迁回**）。

**落地**：`params.js` 新开关（`master: true` ⇒ 参数页**排最前、单独一张卡**）· `render.js` 总闸卡带**当前后果**那句（照 leg27 h 的自证面口径：只报事实、不写"应该没问题"）· `web/index.js` 新增 `autoAdvanceOn` / `ensureAutoAdvanceKey` / ★**`sw2OnMessageReceived`**（把闸判据**提成可导出纯函数**，为的是**能真跑测试**——本仓铁律）· `style.css` 加 `.sw2-master-switch` ⇒ `CSS_VERSION` → `20260913-leg33d` · 构建号 → `leg33d-plugin-master-switch`。
**判据**：新增 `test/plugin-master-switch.test.js` **9 条**，其中最关键的是 ★**关着时 `advance()` 一次都不许被调**（＝"不自动生效"的机械判据）。

---

## 5. 队列里仍未动的（承前）

- **主战场方向**（本棒出数指向的）：★**让世界自己多长几件与主线无关的事**——细案 `docs/spec-world-model-widening.md`（**待用户拍 P-1～P-5**；★**甲/乙两档已被否**，细案 §3 的**候选乙**＝"引擎按账上真有的字段派生独立 `state` 源事件根"是推荐方向，**未实施**）。
- 细案 §6：`entityUpdates` **字段写回** + **死亡可带因复活**。
- `spec-world-widening.md` §5.3：第二世界的引擎跑（要第二本热账 ⇒ 要真模型抽取，**未获授权**）。
- `spec-entity-field-lookup.md` §6 R2 的**口径分歧**（文档写"其归属该盘算的动作"，实现是"本步全部 actions"）——**仍未解决**。
- leg30 登记的 Q2/Q3/Q4/Q6（§5.10 的表）。
- ★**残余（本棒如实登记、未做）**：`abstract.js:1123` 的 `seedBookEntities` **仍按位置集过滤书的 location**（集外落「未明」）——新 `derivePositions` 改后基本不命中；要动属另一笔。
- ★**口径待拍**：「**全新世界要不要也默认关**插件总闸」——本棒按用户原话取了"关"（改 `params.js` 的 `def` 一个字符即可）。
- ★**旧账遗留**：`docs/ledger.md` 第 20 行有一条 leg29 时期的**无行首续文**（表格结构不配对）。**已核实在 `HEAD` 里就存在**、非本棒造成，**未顺手改**（不在授权范围）。

---

## 6. ★本棒现场纪律（下一棒直接用，别重踩）

1. ★**含中文的文件禁止用 PowerShell 读写——本棒又踩了半次**：我用 `Get-Content`（仅读）看 `LEDGER.md`，**文件未损**（终检 BOM 无 / U+FFFD=0 / NUL=0），但这是**明令禁止的动作**，如实记。
2. ★★**`edit` 工具替换含中文的长表格行时，`old_string` 若只是行首一小段，会把整行从那里截断**——本棒**踩了两次**：第一次把 leg32 台账行的行首吃掉；第二次把 `LEDGER.md` 的一行拆成两行（`|` 不配对）。**两次都靠"全表成对性总检"抓出来**（判据：每行 `startsWith('|') && endsWith('|')`，逐行跑，异常行数必须为 0）。
   ⇒ **纪律：改台账长行前先 `Copy-Item` 备份；改完必跑全表成对性总检**（脚本一行：读全部行，逐行判首尾 `|`）。
   ⇒ **更稳的做法**：把 anchor 取成**行尾一小段**（如 `…停在 \`947aabe\`。 |`）并把新行**插在它前面**，而不是把行首当 anchor。
3. ★★**`node --check` 给假绿**（leg32 立的）：判语法**必须真导入**——
   `node -e "import('file:///…/src/x.js').then(m=>console.log('OK'),e=>{console.error(e.message);process.exit(1)})"`。
4. ★**TEMP 里的副本要改相对导入为绝对 `file://`**（正则可参照本棒 `measure-single-focus-ab.js` 里"同构流程"的写法）。
5. ★**"空值分支"要真跑一遍才算写对**：本棒 `settle.js` 里那条"空值才归「未明」"的分支**本来是死代码**，因为 schema 把 `location` 设成了 `required` —— 是**用例**把它抓出来的。
6. ★**换口径必须连"依赖旧口径的夹具"一起改**（leg31 立、leg32 重申）：本棒为位置这条口径改了 **5 个测试文件**（`worldstep` / `positions` D-3 / `position-normalize` / `entity-governance` / `prompts`），否则那些用例会靠**别的错因**继续假绿。
7. **git 与台账**：本棒落 **8 笔**（`947aabe` → `09c7ba8`，**未推送**）；提交信息走 `git commit -F <文件>`（PowerShell 会吃中文引号）。

---

## 7. 下一棒任务书

### 第 0 条（**高于一切**）
`START-HERE.md` §1.5 最高准则：**没有用户当次明确命令，不许改任何文件的一个字节。** 本文件所有"下一步"都是**待选项**。

### A. ★等用户拍（一件，最重要）

| # | 事项 | 说明 |
|---|---|---|
| **P-甲′** | ★★**要不要上"世界模型层"那一刀** | 细案 `docs/spec-world-model-widening.md` 已出（**待拍 P-1～P-5**）。本棒实测把 leg32 的候选甲/乙否掉，剩下的方向是**候选乙**：引擎每轮按**账上真有的字段**（`entities[].name` + `location`）派生一条独立 `state` 源事件根（形如 `〔昆仑道宫〕在西极昆仑山有所动作`），**点名**一个带真处境的冷门实体 ⇒ 直接打在 leg32 实测到的**唯一变宽机制**上（真账 `t24` 点名东海龙宫 ⇒ 它当轮立线）。代价 **28–56 est/轮**；**引擎不编故事**（标题三样全是账上字段）。★**可用本棒的装置出数**（`demo/measure-single-focus-ab.js`）。 |

### B. 小口径待拍（两件，都不大）

1. **插件总闸的"全新世界"缺省**：现按用户原话取 `'0'`（关）；要不要也默认开？（改 `def` 一字符）
2. **`abstract.js:1123` 的 `seedBookEntities` 位置过滤**：位置已改自由文本，这里要不要一并放松？（本棒未动）

### C. 队列里仍未动的（承前，见 §5）

### D. 不要做的事（在 leg32 那 30 条之上新增 5 条）

31. ★**改台账长表格行之前先备份、之后必跑"全表成对性总检"**（本棒两次踩坑都由它抓出，见 §6.2）。
32. ★**不许把 `context.positions` 真的删掉**——它现在是**参照表**（不是闸），`newEntities` 的位置归一与 `deriveLocationFromBook` 都读它；删了新人入局 100% 拿不到位置。
33. ★**不许把位置闸加回来**（`位置 ⊆ 位置集 ⇒ 拒整步`）：那是**跨书不成立**的判据（3/8），且会切真地名。要留痕就留痕，**别拒整步**。
34. ★**"引擎不发明事实"这条红线在位置上仍然成立**：位置自由文本＝**照收模型给的值**，不等于"引擎替谁编一个位置"；空值仍落「未明」（空着就是空着）。
35. ★**插件总闸的迁移不许"顺手"迁别的键**：`ensureAutoAdvanceKey` 只碰 `autoAdvance`；且**键已存在就一个字节不碰**（用户手动关掉的 `'0'` 永不被迁回）。

---

## 8. 现场（本机）

- **项目**：`F:\deepseek\plugins\story-world-v2`（仓库根 `F:\deepseek\plugins`）
- **测试**：`node --test` → **576/576**（无参、在项目目录内跑；本棒 +9 条：`position-normalize` 5 + `plugin-master-switch` 9 − 其余改写）
- **端到端**：`node demo/smoke-demo.js` → **PASS**（50 tick、警告 0、**终态 8231 字节**，本棒**逐字节未变**）
- **构建号**：`leg33d-plugin-master-switch`（`PANEL_BUILD`）｜`CSS_VERSION` = `20260913-leg33d`｜`MAIN_PROMPT_V` = **`v2-agenda-t1-16`**
- **引擎真源**：`AGENDA_CAPS = { perTick: 3, open: 20, topLevel: 15 }`｜`STALE_CHAIN_HEAD_AGE = 12`｜`IDLE_FACES_TOP = 12`｜`ENTITY_BIRTH_PER_TICK = 1`｜★`POSITIONS_CAP` **已作废**（= `Infinity`，位置集**不再截断**）
- **开关类参数**（`dynamic.env`）：`autoAdvance`（插件总闸，★本棒新增，**缺省关**）· `memoryEnabled`（真账现为 `'1'`）· `recordEnabled`
- **部署 junction**：`F:\jiuguanai\...\third-party\story-world-v2` → 项目根（★junction；改 `web/`/`src/` 都要 **Ctrl+F5**）
- **真账**：`chats\大荒z\大荒z - 2026-09-01@00h37m41s559ms.jsonl`（**一律先 cp 副本**；本棒**全程只读、未动真账、未改真账**）
- **git**：`main` 停在 **`09c7ba8`**、**本棒 8 笔已落**、**未推送**、**工作区 0 条**
- **本棒改动文件**：`src/position.js`（新增）· `src/check-step.js`（位置段改留痕 + `warnings` 返回面）· `src/settle.js`（`spawnEntities` 照收 + 收 `pre.warnings` + `adjudicate` 转发）· `src/prompts.js`（铁律 3 两次改 + 版本升到 t1-16）· `src/render.js`（总闸卡排最前 + 后果句 + 构建号）· `src/params.js`（插件总闸开关）· `src/schemas/world-step.schema.js`（`location` 去 required）· `web/index.js`（总闸三函数 + 事件闸 + 迁移 + `derivePositions` 去截断 + `CSS_VERSION`）· `web/style.css`（`.sw2-master-switch`）· `demo/measure-single-focus-ab.js`（**新增出数装置**）· `test/position-normalize.test.js`（新增）· `test/plugin-master-switch.test.js`（新增）· `test/worldstep/positions/entity-governance/prompts`（口径同步）· `docs/spec-world-model-widening.md`（新增细案）· `docs/START-HERE.md` · `LEDGER.md` · `docs/ledger.md` · 本文件

---

## 9. 用户可选的实机复验（构建号 **`leg33d-plugin-master-switch`**）

0. ★**Ctrl+F5**（实体表表头应为 `leg33d-plugin-master-switch`）→ **参数页**：**第一张卡**应是「**插件总闸 · 自动推进**」（左侧琥珀色竖条 + 「总闸」徽章），下面一句**当前后果**。
0. ★★**试总闸这一条（本棒的正题）**：
   - 按「**关**」→ 状态栏应出现 `⏸ 插件已关 · 世界原样留在盘上（没有清账、没有拆线）…`；
   - 然后**发一条消息** → 状态栏应出现 `⏸ 插件已关（发消息不自动推进）…`，且**世界轮次不该动**（观棋页的轮次/编年不变）；
   - 再按「**开**」→ 状态栏 `▶ 插件已开 · 发消息会自动推进世界…`，发一条消息 ⇒ 世界照常推一轮。
   ★若关了之后发消息**仍然推进**，立刻回传（说明闸没接上）。
0. ★★**位置这条（leg33c 的正题）**：推几轮，**不该再出现** `position: "…（推）" 不在世界位置集` 这类**整步被拒**；
   模型写书里别处的地名（如 `太清境`）应当**照收**，只留一句 `位置集外:` 的痕迹（状态栏/控制台的警告里）。
0. ★**位置集变大这条**：F12 控制台看包体读数——位置集从 60 项变 **135 项**、整包 est **约 +283**（预算占用仍约 40%）。
0. ★**长跑/并行/主角那几条仍是 leg32 的观察题**（见 `docs/session-handoff-2026-09-12-leg32.md` §9），本棒**未改**那些机制，观察结论照旧有效。

---

*第三十三棒交接 · 2026-09-13 · ★★**本棒最贵的产物：把 leg32 的候选甲与候选乙用真模型对跑一起否掉**（挂主线的新盘算 = 0、结构面本来就不塌缩、塌缩在内容面且改包对它零效果）⇒ 核心发现收窄为**"世界自己只有一件事在长"** · ★**位置＝自由文本**（跨书实测 3/8 不成立 + 大书里 cap 在切真地名 ⇒ 用户拍板，位置集降级为参照表；顺带挖出 `location` 是 required ⇒ "不给位置"根本表达不出来）· ★**插件总闸**（病灶＝每收一条消息自动推一轮；含存量世界一次性迁移，防把正在跑的世界悄悄按停）· ★**我踩的两个坑**（`edit` 截断台账长行两次、PowerShell 读中文文件半次）· **576/576 · 冒烟 PASS（8231 字节逐字节未变）· 8 笔已落未推送 · 工作区 0 条***
