# 会话交接 · story-world v2 · 第二十五棒 b（真机复核 + 按需查书落地 + 术语对齐 · 2026-09-11 · 471/471）

> 接手前必读（按序）：①`ANCHOR.md`（唯一尺子）②`docs/dev-process.md`（执行手册）③本文件 ④`docs/session-handoff-2026-09-11-leg25.md`（同棒前半：检察官审计 + A–H 八组；**本文件修正了它三处**）⑤`docs/spec-entity-field-lookup.md`（本棒唯一已批准细案，**已落地**）⑥`docs/ledger.md`（本棒三行）。
> 状态：**全部改动未提交**——工作区 **42 个已改 + 8 个未跟踪**（最后提交仍是 `88a5756`）。用户验完再落 git。
> 上一棒入口：`docs/session-handoff-2026-09-11-leg25.md`。

---

## 0. 人话版：这一棒干了两件事——把上一棒的账查实（查出真缺陷），把你要的"按需查书"做完

**第一件：上一棒（leg25 前半）的 A 组清理是"假修复"。** 用户拿截图问"兵力怎么还存在"——查实：清理**跑过**（备份里有 `legacyAttrsMigratedAt=2`、留档 330 条），但判词要求"四维**恰好全等**旧默认值"才批，而真实脏数据是**混合行**（一维书里真值 + 其余几维引擎默认，形如 `阐教 {兵力 .25, 权位 .5, 人脉 .25, 耳目 .25}`）→ **整行判词恒假 → 500 维假数一格没清**。修成**逐维判**。

**第二件：用户追问"属性没回写"= 位置/归属/其他字段** → 逐项量完，三条链路里**只有四维是通的**（location 写回 0/623、parent 0/623）。其中 **parent 是真 bug**（模型把上级写成 `/太素帝`，名号对不上册 → 9 条真归属全被弃），已修；**location 是"用到时查书"半截工程**（leg24 停抄书停了生产，替代通道从没接）。用户定调：**查书必须走模型**、**用到时补**（不初始化全量）、**势力不写实力字段**（实力是角色字段，势力实力由麾下成员派生显示）、**实力是文本类型**（分段境界抄档位、不分段抄它自己的原话）。

**交付**：`docs/spec-entity-field-lookup.md`（已批准已落地）+ `src/entity-lookup.js`（选人/查书/回写/熔断/两条 ≤15）。判据：真书 **29/29 成员行档位原话命中**、势力实力全留空、位置 5/5；测试 **471/471**。

**术语对齐（用户当场纠正："四态不是早淘汰了吗，你们文档都没对齐吗"）**：本项目"三态"是**有主的**（盘算满步终结三态），我新造的面板状态**改为「查书标记」**，全仓 43 处已统一；`word-band-attrs-spec` 补了对齐指针 + `decision-index` 加了术语定名表。

---

## 1. 本棒主线（按时间序）

1. 用户问「兵力怎么还存在？属性也没回写」→ 只读扫 ST 全目录（现役聊天 + `backups/` 逐小时备份，一份不落）
2. 查实 A 组判据不成立（混合行漏清 500 维）→ **改逐维**（真账实跑：1132 维 → 632 维，保留维来源逐条核对 632=书里明写、存疑 0）
3. 查实旧世界**已被覆盖**（20:43 `init-world` 重跑建 623 实体新账）→ 交接原文"首次打开会自动清"作废
4. 查实属性回写**是通的**（逐轮备份 + 编年逐条对账 11/11 吻合）→ 驳"属性没回写"；但**书那一层**真丢（旧账 290 条带 attrs+evidence，新世界 0 条）
5. 用户澄清「我说的是**其他属性**，位置、归属」→ 逐项量：location 0/623、parent 0/623（真 bug 已修：`normalizeParentName`）
6. 用户问「实力该用文本？注入时机初始化还是用到时？」→ 量覆盖率（书里 538/623 对得上条目、429 有实力串）+ 真模型实验（批量 5 实体 1 次调用 vs 单条 5 次：**批量漏读 0 实例**、位置 5/5）
7. 用户否掉词表/正则（"被很多词表法弄得很烦"）+ 定调"查书必须模型""用到时补"
8. **用户截图 v1 表单**（`姓名/别名/身份/实力/角色定位/所属势力/备注`）→ 读 v1 源码找到实证先例（必抄规则 + 零 token 兜底；势力格是"五万大军"类规模描述）
9. 写细案 `spec-entity-field-lookup.md` → 用户「a,做吧」（R2 取拒整步）→ 按先写测试再接线落地
10. 用户追加「实力是文本类型，不分段境界可灵活输入」→ prompt 去掉"只取档位"的自设限制
11. 用户实拍面板：`兵力怎么还在 / 根本看不到属性 / 位置未明重复了` → 修面板（补"未查"态、撤销重复徽章、加构建号自证）
12. 用户质问「四态不是早淘汰了吗，文档没对齐」→ **术语统一为「查书标记」**（43 处）+ `word-band` 对齐指针 + `decision-index` 术语表
13. 本交接落盘

---

## 2. 本棒交付（逐项：改了什么 / 为什么 / 判据）

### 2.1 旧账清理判据：整行全等 → **逐维**（真缺陷修复）

- **改**：`src/settle.js` `migrateLegacyAttrs`——`该维 == 本类别旧默认值 ∧ 书里没写这一维 ∧ 编年里该实体无属性变更 → 删该维`；删死函数 `attrsAreLegacyDefault`/`hasBookProvenance`。
- **为什么**：实测用户旧账 283 行 1132 维里 **500 维是旧默认值**，旧判词一格没清（`阐教 {.25,.5,.25,.25}` 这类混合行）。
- **判据（真账实跑）**：1132 维 → **632 维**（清 500：兵力 71／权位 63／人脉 185／耳目 181）；保留 632 维逐条核对来源 **= 书里明写 632 + 编年豁免 0 + 非默认值 0 → 存疑 0**；幂等（连跑两次原对象返回）；实体行序名号不变。
- **测试**：`test/settle.test.js` 净增 6 则（混合行 / evidence 不当挡箭牌 / 编年豁免 / 书无数值的名册实体照清 / 粒度是"维" / schema+幂等）。

### 2.2 上级名号净化（真 bug：9 条真归属全丢）

- **改**：`src/abstract.js` 新增 `normalizeParentName`（只剥**前导**层级符 `/ ／ \ > · - — –` 与空白，名号本体不动），在 `sanitizeCanon`（模型抽取）+ `applyDeclaredToRoster`（照书办）两处收口。
- **为什么**：`/太素帝` 这条形态在名册索引里查不到 → `resolveSeedTarget` 判"上级不在册" → 界面「归属空着」。
- **判据（真账内存重跑）**：9 条 parent 全部写回、`seedBookEntities` **warnings 9 → 0**、账上 parent **0 → 9**。
- **测试**：`test/abstract.test.js` +3 则。

### 2.3 空裁定噪声（用户问「属性硬边界（… 0→0，申请 -0.2）这是什么」）

- **改**：`src/settle.js` `adjudicate` + `applyPlayerImpact`——`after === before`（钳到同值）改记「**越界提议被忽略**」，只有**钳制真改变了落账值**时才叫「属性硬边界」。
- **为什么**：实测用户当前世界 6 条边界裁定里 **4 条是空裁定**，而每轮警告总共才 1-2 条 ⇒ 噪声占裁定面一半。
- **注意（别改回去）**：`applyPlayerImpact` 的 **`playerAffected` 记录照写**（`delta:0` = "世界伸手碰了玩家被吃住"，K9 审计面要看见的事实）。我第一版把记录也去掉，被快照重放测试当场抓到，已撤回并补对照测试。
- **判据**：`settle.test.js` +2 则；4 个快照 fixture 里 **38 条**历史空裁定措辞同步（数值未动）。

### 2.4 按需查书补字段（**本棒主交付** · `docs/spec-entity-field-lookup.md` 已批准已落地）

| 面 | 实现 |
|---|---|
| 选人 | `runSelect`——LLM 从**全量名号**（书序、不排序不裁剪）里选本轮上场实体；引擎护栏：不在册/已灭 → 拒；**超上限 15 → 拒整条**并退回上轮名单（不截断）；坏 JSON/调用失败 → 退回上轮名单，再退兜底（玩家 + 在飞盘算属主） |
| 查书 | `runLookup` + `buildLookupPrompt`——只喂"选中且缺字段"者的世界书原文（最多 4 条/实体、单条截 1200 字）；口径=**照抄原文原话**、**文本类型**（分段书抄档位、不分段抄它自己的写法）、**势力不抽实力**、**不许下判断词**、取不到不写键 |
| 回写 | `applyLookup`——**查书标记**四种结果：有值（落账 + `fields{value,from,fetchedAt}`）/ **未查** / **未加载到**（查过模型没给，**绝不当成"书里没有"**）/ **书未明述**（仅当引擎确认书里无相关条目）；逐字段独立；重名按名号对齐一律丢弃（宁可漏填不可错填） |
| 失败 | 调用失败 → **不写任何痕迹**（下轮重试）；批内返回不全 → 不算该实体"未加载"；同字段自动重试 2 次即止；连续 3 轮失败 → 熔断几轮（**世界推进优先**） |
| 接线 | `src/tick.js` 前置步跑在 `buildEvolutionPack` **之前**（`preStep`/`onPreStep` 注入口，抛错零阻塞）｜`src/pack.js` `picks` 换名单（选择权归 LLM）+ 角色行带 `实力`｜`src/check-step.js` R2 超限**拒整步**｜`schemas/ssot.schema.js` 登记 `entity.实力`、`meta.entityFields`、`meta.entityLookup`｜`src/render.js` 查书标记 + 势力不显示实力栏 + 麾下实力派生｜`web/index.js` `bookTextForEntity` 取世界书原文 + 前置步 + **落盘**（防 Ctrl+F5 重查） |
| **判据（真书 + 真模型，生产 prompt）** | 用户世界书 4 个势力条目里的 **29 个成员行**（档位已知真值）→ **命中 29/29、漏 0、编 0**（原话逐字一致，含 `T9渡劫巅峰`/`T4金丹后期`/`T1感气境`）；**势力条目自身"实力"全部留空**；位置首轮 5/5 |
| 测试 | `test/entity-lookup.test.js` **19 则** + `test/render.test.js` 2 则 → 全量 **471/471** |

### 2.5 面板修正（用户实拍三条）

- 「**根本看不到属性**」= 我的 bug：只做了"未加载到/书未明述"两态标签，**"从没查过"直接空白** → 补 `实力：未查` / `位置：未查`（并写清"轮到时会按需去查"）。
- 「**位置未明重复**」= 位置列写 `未明`、标记列又打 `位置未明` 徽章 → 位置列改说"在哪"（没载到写 `未载`），查书标记统一收到属性区，徽章撤销。
- **加了自证**：`PANEL_BUILD`（`leg25-lookup-4state+posdedup`）渲染进"全部角色与势力"表头——用户实机常遇"改了代码页面还是旧的"（浏览器缓存 `web/index.js`），以后一眼可判。

---

## 3. 与旧文档的**对齐表**（用户令「一定要对齐」；这是本棒最容易踩的地方）

| 旧决定 | 状态 | 现在的口径 |
|---|---|---|
| `word-band-attrs-spec.md`（K50-K53"词档版三态"） | **前提已废 + 已加对齐指针** | 它整套建立在"**属性轮**每轮抄书产出档词"上；leg24 片1 停抄书把属性轮整条删了。其"档词→数值换算 / `bandOf` 派生 / band+ev 进包 / 档位跃迁监控"**由 `spec-entity-field-lookup` 取代**：`实力` = **照抄原文原话的文本**（不换算、不进公式、不做枚举校验），且**用到时**查一次书。头部已补指针（2026-09-11）。 |
| 「三态」这个词 | **有主，别抢** | ①`agenda-tree-spec` §3.4 = 盘算满步**终结**三态（达成/败露/变形，K15 已实施）②`word-band` §2.2 = 属性**值形态**三态（已废）。按需查书那边一律叫 **「查书标记」**（有值/未查/未加载到/书未明述）——全仓 43 处已统一，`decision-index` 加了术语定名表。 |
| 面板「据/无据」 | 保留 | 四维仍是"有据 n/4 / 数值无据"；**新增**的查书标记只服务 `实力`/`位置` 两栏。 |
| 势力实力 | **用户新拍板** | **势力不写实力字段**；面板上用「麾下实力：<成员>（<其原话>）」派生显示；没有就不显示，**绝不替它算总档**。 |
| `资源` 字段 | 登记未做 | `spec-entity-field-lookup` §10 B1：用户已点名"以后想加就加个资源字段"。 |
| 一句话对齐准则 | — | **凡"引擎把书里的词换算成数"的旧设计一律作废**；书里的词**照抄成文本**，引擎不读、不进公式、不排序。 |

---

## 4. 实测账（可复跑）

| 实验 | 结果 |
|---|---|
| 全量测试 | `node --test` → **471/471 全绿**（本棒起点 396→439→445→448→469→471） |
| 端到端冒烟 | `node demo/smoke-demo.js` → **PASS**（50 tick、警告 0、终局 8270 字节） |
| 语法 | `node --check` 全 src+web+demo+test → **99 文件零失败** |
| A 组逐维清理（真账） | 1132 维 → 632 维；保留维来源核对 632=书里明写、存疑 **0** |
| parent 净化（真账内存重跑） | 9 条写回、warnings 9→0、账上 parent 0→9 |
| 按需查书（真书+真模型） | 29/29 命中、0 漏、0 编；势力实力全留空；位置 5/5 |
| 批量 vs 单条（真模型） | 5 实体批量 1 次调用 = 单条 5 次调用（**批量漏读 0 实例**）；位置 5/5 原话准确 |
| 空裁定规模（真账） | 当前世界 6 条边界裁定里 4 条是空裁定（占裁定面一半） |
| **实机未跑** | 选人数/查书调用数/重复查数/未加载数（用户 Ctrl+F5 后观察；见 §6 A2） |

---

## 5. 铁律（延续 + 本棒新增）

1–13 条延续 leg24/leg25（没命令不动代码／上限先报批／文档开口先给人话版／改码三件事／审批 never／先 read 再 edit／检验三问／数据说话／分层归属／说人话是最高准则／不能通用就直接丢／改判据=改承重墙／**出设计前先十思**）。
14. **本棒新增：术语不许自造第三个**——项目里已有"三态"（盘算终结）这类有主的词；新机制起名前**先 grep 全仓**，撞车就换名，并写进 `decision-index` 术语表。（用户当场纠正："四态不是早淘汰了吗，你们文档都没对齐吗"）
15. **本棒新增：模型没返回 ≠ 事实空缺**——凡是"模型抽取"得来的字段，**缺失只能记"未加载"**，绝不用空值反推"书里没有"；只有引擎能确定性判定的事实（如"书里没有该条目"）才允许记 `absent`。（实证：我写错口径时模型对 5 个势力全返 null，而档位其实挂在成员行上 → 单条追问才发现）

---

## 6. 下一棒任务书

1. **第一动作**：用户验收 → **落 git**（42 改 + 8 未跟踪；建议按"①真机复核修正 ②按需查书落地 ③术语对齐"三笔，或一笔 + 台账已记）。**验前不要动代码**。
2. **用户点头才做的四件（本棒登记，未动）**：
   - **A1｜`membersOf` 还在用分量排序**：`src/pack.js:74` 按 `world.weights` 给成员排序取前 N（显示到"麾下"）——而分量吃四维 ⇒ **"引擎还在排序"的唯一残留**。要不要改成确定性序（书序/名字序）彻底断掉？（本棒挖出，用户未拍；现状：用户世界 `parent` 全空 → 这条现在不生效，但 §2.2 的 parent 修好后会开始生效）
   - **A2｜实机复验按需查书**：Ctrl+F5 → 看"全部角色与势力"表头 **`构建 leg25-lookup-4state+posdedup`**（对不上=浏览器还在跑旧代码）→ 推一轮 → 看是否出现 `实力：T?…（据书）`、`实力：未查/未加载到/书未明述`，以及 simLog 里选人数/查书调用数/重复查数。
   - **A3｜选人调用降频**：第一轮起每轮多 1 次小调用（全量名号 ≈3-4k tokens）；实机跑几轮后按调用数定（`spec` §10 B4）。
   - **A4｜旧账里已被旧判据迁过的残留**：`legacyAttrsMigratedAt` 是硬闸，已被旧判据迁过的账（含备份里那个 613 实体世界）再打开**不会重扫**，那 500 维会一直留着；要清只能"重新初始化"或单独立项做版本化重迁。
3. **不要做的事**：①不要再让引擎把书里的词换算成数（`word-band` 那条已废）②不要把词表/正则当判据（用户明令）③不要给缺失字段填占位值 ④不要动 `playerAffected` 的 `delta:0` 记录（K9 审计面）⑤不要把 `实力` 接入任何公式/排序（含 A1 那条要单独拍）⑥不要再用"三态/四态"称呼查书标记。
4. **现场**：`node --test` 必须**在项目目录内**跑（当前基线 **471/471**）；真模型通道 `loadStPresetConfig()` + `createHttpTransport`（密钥仅本机读、不打印）；部署 junction 在 `F:\jiuguanai\...\third-party\story-world-v2`（Ctrl+F5 即载；**改了 `web/` 或 `src/` 都要 Ctrl+F5**）。
5. **本棒留下的判据脚本（可复跑，仓库外）**：`tmp-llm-batch-probe.mjs`（批量 vs 单条抽到率）· `tmp-tier-truth-probe.mjs`（档位真值对照）· `tmp-criteria-realbook.mjs`（29 个成员行命中率）· `tmp-real-migrate.mjs`（生产迁移函数跑真旧账）· `tmp-boundary-noise.mjs`（空裁定计数）· `tmp-current-state.mjs`（盘上现况快照）。

---

## 7. 未决登记（带触发条件）

| # | 事项 | 触发条件 | 前置数据 |
|---|---|---|---|
| A1 | `membersOf` 分量排序要不要换确定性序 | 用户点单 | 本文件 §6.2 A1；`src/pack.js:74` |
| A2 | 实机复验按需查书（四态标记 / 调用数 / 重复查数） | 用户环境 Ctrl+F5 | 本文件 §2.5、§6.2 A2 |
| A3 | 选人调用降频策略 | 实机跑几轮后 | `spec-entity-field-lookup` §10 B4 |
| A4 | 已被旧判据迁过的旧账残留（500 维）怎么清 | 用户点单 | 本文件 §6.2 A4 |
| A5 | 势力 `资源` 字段 | 用户点单（已点名"以后再加"） | `spec` §10 B1 |
| A6 | 存量实体"移动"通道（`stateChanges[].location`） | 用户点单（机制改动） | `spec` §10 B2 |
| A7 | 选人/查书两处用的 `diagExtract`（抽取预算 transport）是否需要独立预算 | 实机看调用表现 | `web/index.js` `advanceTick` |
| A8 | `seedBookEntities` 的 warnings 真跑仍被丢弃（leg25 A5 未变） | 顺手清 | `src/abstract.js` `out.warnings` |

---

*第二十五棒 b 交接 · 2026-09-11 · 真机复核（A 组判据逐维化 / parent 名号净化 / 空裁定噪声）+ 按需查书落地（`src/entity-lookup.js`）+ 术语对齐（查书标记）· **471/471** · 冒烟 PASS · 语法 99 文件零失败 · 改动全在工作区未提交（最后提交 `88a5756`）· 下一动作：用户验收 → 落 git → 再谈 §6 的 A1/A2。*
