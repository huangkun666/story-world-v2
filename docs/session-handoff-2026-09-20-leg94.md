# 交接 · 第九十四棒（leg94）

> **接手第一件事：读 §0 与 §5。** 本棒一句话起、一问收：
> 用户贴出实机截图（**编年页**竖排四行「⚑ 第 1–N 轮已收进大事纪「…」」）说「**把这里显示的文字删了，这是大事纪旧卷的东西**」→
> 再贴大事纪页说「**大事纪更是灾难**」（里程碑卡印 `[object Object]`、展开条目全是 `ev_11_2 链`）→
> 贴出自己写的因果链与事件清单说「**你虽然能看懂整个因果，但是我看不懂，还要你给我讲解我才能知道发生了什么**」→
> 追问「**一定要多一次 llm 调用吗，不能用代码连起来吗**」→ 看了「说书」页回「**我怎么认为这个比编年好多了，就是有个问题为什么事件一直都是还开着**」
> （⇒ §4 那份引擎口径的答复，本棒**没动引擎**，留给下一任）。
>
> **上一棒那三页仍要读**：
> · `docs/session-handoff-2026-09-20-leg93d.md`（leg93b/93c/93d 三笔一页 · **§0 是"越权改码"与三次"空绿"的教训**）
> · `docs/session-handoff-2026-09-20-leg93.md`（leg90/90b/91/92）· `docs/session-handoff-2026-09-20-leg89.md`（带标签提取的设计与实施）
>
> ⚠ 含中文的文件**禁止用 PowerShell 读写**（用 `read`/`edit`/`write`；数字一律 `.mjs` 装置，别用 `node -e`——引号会被 PowerShell 吃掉）。
> ⚠ **本仓 git = monorepo，根在 `F:/deepseek/plugins`**；包根是 `story-world-v2`（**没有 package.json**）。
> ⚠ **`node --test` 必须在包目录里跑**（workdir = `story-world-v2`）。
> ⚠ **真机位 = junction**；改仓即改实机，但**浏览器要硬刷新**才换代码（页脚构建号是唯一验收判据）。
> ⚠ ★★**`src/prompts.js` 的 `MAIN_PROMPT` 是模板字符串**——往里写**反引号**当场截断它（本棒没碰它，雷还在）。
> ⚠ ★★**台账插行必须用脚本 + 落盘前逐格对齐表头**——见 §6 第 2 条（**交接 §4 那条口诀是错的**）。

**git 现场**：分支 `leg62-scales-concept-table` · 上一提交 **`c58c1a9`**（leg93d 交接）· 本棒**工作区未提交**（14 项：11 改 + 2 新增 + 台账行）
**读数**：`node --test` **1049/1049 · fail 0 · skipped 0** · 冒烟 `demo/smoke-demo.js` **PASS · 终态 SSOT 8231 字节 · 警告 0**（与 leg86–93d 逐字节相同 ⇒ 引擎零漂移）
**构建号**：`PANEL_BUILD` = **`leg94-storyview`** · `CSS_VERSION` = **`20260921-leg94-storyview`**（★本笔**真的动了样式**）· `MAIN_PROMPT_V` = `v2-agenda-t1-21`（未动）

---

## 0. ★★★ 接手先看这一条：**"能看懂因果"不是本事，把因果讲出来才是产品**

用户这句是本案的判据原话：

> 「**你虽然能看懂整个因果，但是我看不懂，还要你给我讲解我才能知道发生了什么**」

★**它说的不是能力差，是缺一层**。我能看懂，是因为我**读了 367 行编年 + 88 条事件 + 42 条盘算之后在脑子里拼了一遍**——
而这件事**本来应该插件替用户做**。用户手上有三样东西，没有一样是为"读故事"造的：

| 用户看的 | 它实际是什么 | 为什么读不懂 |
|---|---|---|
| 编年 367 行 | **引擎流水账** | 满是 `满步结算：结清（期满收摊，终结产果 §4.4④）`——**§4.4④ 是写给写代码的人看的设计编号** |
| 事件表 | **数据库** | 只有标题+地点+机器号，**没有"起因是谁挑的、代价是什么"** |
| 因果链浮层 | **图谱** | 珠链上写 `ev_48_2`、「谋/纪/源」徽章——看得出连线，读不出情节 |

⇒ ★**纪律（下一任照做）**：**凡是"我已经看懂了"的东西，都要问一句"用户拿到的是不是同一份"**。
本棒的答法是**加一层输出**（§2），**不是**让用户去学我们的行话。

### 0.2 ★★ 三次「我的判据错了，不是代码错了」——**全在数据形状上**

| # | 我以为 | 真相 | 教训 |
|---|---|---|---|
| ① | 号只有 `ev_<轮>_<序>` 一种形状 | 老/演示账还有**短号 `ev_0`**（`live-world.json` 就是） | 判据红的时候先怀疑**我的夹具/正则**，别急着改产品 |
| ② | `ripples` 里是"谋划的主人的号" | 它是**普通实体号**；归属键才是主人 | 同一个字（"牵动的人"）在两层里指的不是同一件事 |
| ③ | 短号那格是产品的病 | 那是**老代码的口径**（编年行文本里烙的号） | 老账的坏毛病要**如实兼容**，不许顺手"修"成新口径 |

★三条合起来一条纪律：**本仓"数据形状"是最容易骗过判据的地方**——写判据前先把真账里那几格的**所有形状**列一遍。

---

## 1. 这一棒做了什么（一张表看完）

| # | 事 | 用户令 | 节 |
|---|---|---|---|
| 1 | 编年页里程碑插行（`sw2-ch-roll`）**整段撤掉** | 「这个文字出现在编年页签里了」「这是大事纪旧卷的东西」 | §2.1 |
| 2 | 大事纪里程碑卡 **`[object Object]` 修掉** | 「大事纪更是灾难」 | §2.2 |
| 3 | 里程碑展开条目 **印标题不印机器号**（`ids↔titles` 同位配对） | 同上（截图里全是 `ev_11_2 链`） | §2.2 |
| 4 | 编年账目行里**内嵌的机器号渲染时换人话** | 同上 | §2.3 |
| 5 | ★★★**新增「说书」页签**（`src/panorama.js` · **零 LLM**） | 「我看不懂，还要你给我讲解」＋「一定要多一次 llm 调用吗，不能用代码连起来吗」 | §3 |
| 6 | 升位：`PANEL_BUILD` / `CSS_VERSION` 同批 | —— | §3.4 |
| 7 | 台账一行 + 细案 §2.1 + 决策索引三行 + START-HERE 指针 | —— | 各自那一笔 |

★**用户明确放行**：「主角没进场是因为**还没那个能力，不用管**」⇒ 本棒**不碰**玩家棋子那条线（真账取证见 §5.9 那条登记）。

---

## 2. 三处实机病 + 一处撤除（都是呈现层）

### 2.1 编年页撤掉"已收进大事纪"那段（撤渲染，不撤数据）

**病**：`renderChronicleHtml` 把 `world.milestones` **逐纪**画一枚 `.sw2-ch-roll`（「⚑ 第 1–N 轮已收进大事纪「…」」），落在两层列表之后。
**为什么该撤**：同一句话在**大事纪页的里程碑卡**上已经有一份（`renderArchiveHtml` 的 `.sw2-milestone`）⇒ 编年页那份是**同一事实的第二块屏**，而且讲的是**另一页**的事。

| 撤了什么 | 留了什么 |
|---|---|
| `render.js` 里 `notes` 那段渲染（含 `notes.join('')`） | `world.milestones` **一个字没动**（链视图 `chain.js`／记忆桥 `memory-bridge.js`／档案页照旧读它） |
| `web/style.css` 的 `.sw2-ch-roll` 规则（零生产点＝无主规则） | 观棋页那条**带「去翻旧账 →」入口**的归档提示条（`sw2-milestone-strip`）——★**从别页指路 ≠ 在家门口重印门牌** |

★旧口径 **A-16④（"里程碑插行恒显示"）按用户令作废**：细案 `docs/spec-chronicle-page-ia.md` §2 那张表**原地标注已改**（按本仓纪律：旧判据按设计升级、写明理由，不是"测试挂了"）。

### 2.2 大事纪两处纯 bug

1. **`[object Object]`**：`${m.counts ?? 0} 件事` —— `counts` 是 **`{events:N}` 对象**，插值当场 toString。
   ★**口径就在隔壁**：同一份文件里链视图那两处写的是 `n.counts.events ?? 0`（`render.js:2128/2190` 那一带）⇒ 照抄。
   老账兜底：`counts` 是数字 / 缺格 ⇒ 退回 `ids` 条数（**不印 0 件事**）。
2. **展开那一纪印机器号**：`m.ids` 与 `m.titles` **同一个顺序、一一对应**（真账实测四条里程碑 **10/10、20/20、16/16、17/17 条数完全相等**）⇒ 按位配对印「血战煞阵缺口 [链]」；
   对不上的**如实兜底**（印自己的号 + 一句「另有 N 条账上没留标题」）；机器号**退进 `title` 悬停**（A-3 豁免口一字未动）。

### 2.3 编年账目行里的号 → 人话

**病**（用户截图原文）：`因事而生：天璇圣地 由「ev_11_1」生「活捉药尘子炼化神丹药力」`
—— 引擎在**新盘算的出生行**上写的是号（`settle.js:436`），那一行**没有 `eventRef`**，号只活在文本里。

**治法**：`chronicleEventNames(world)`（热事件 → 归档按 `milestones.ids↔titles` 补位）＋ `chronicleDisplayText(text, names)`，
**只换"印出来的那一格"**：行原文 `text`、搜索面、导出、旧卷阅卷行**一个字不改**；**查不到就原样留着**（不猜不编）。

★**判据当场咬出的形状**：号有**两种**——新账 `ev_11_1`、老/演示账**短号 `ev_0`**（`live-world.json` 就是短号）。第一版只写长号 ⇒ "认得出却不认"。
⇒ 定稿 `CHRONICLE_BARE_ID = /\b(?:ev_seed_\d+|ev_\d+(?:_\d+)?)\b/i`（序号段**可选**）。

---

## 3. ★★★ 「说书」视图（本棒主菜 · **零 LLM**）

### 3.1 决定：新开第九个页签，**不是**改编年页

| | 编年页 | 说书页 |
|---|---|---|
| 是什么 | **底层账本**（逐行、可查、可导出、可对账） | **给人读的重排**（按因果线成段） |
| 谁来读 | 想查账的人（我、下一任、导出的机器） | 玩家 |
| 本棒对它做了什么 | 撤一段插行、账目行换人话；**一行不删** | 新增 |

⇒ 用户说过编年"看不懂"——**两种东西挤在一格是错的**（决策索引 leg94 那三行已记）。

### 3.2 它做什么（把散在三四处的字收进一段）

| 一格 | 取数处 |
|---|---|
| 谁在动手 | `agenda.owner` → 名号 |
| 他为什么动手 | `event.source` 四型（书里埋的 / 处境自己拱的 / 接着上一件事 / 由某人的打算推出来） |
| **他一步步怎么做的** | ★`agenda.memory.done`（`t42: 解开死煞核心第一层封印` 这种**逐轮原话**——原来没人端出来） |
| **代价 / 卡住的地方** | ★`agenda.memory.blocked` ＋ 推进话里的"以重伤为代价""舍弃大半人手"那类 |
| 了结没有 | `closed / closedAt`（老账没这格 ⇒ 按里程碑标题兜，**如实不猜**） |
| **还开着的口子** | 未了结的那些事 |

### 3.3 三条硬口径（判据全咬住了）

1. **零引擎词**：`盘算/涟漪/结算/入局/编年/里程碑/闭环/满步` 一律不许出现在可见文本里。
   账上原文里的内部词**降级成标点**（`stripEngine`：**去掉那个词、留下它带的句子**——不改写事实）。
2. **零机器号**：认证得出的就换成那件事的名字（`resolveIds` → 查表），**认不出宁可清掉**（`scrubIds`）——"宁少一个名，不印一串码"。
3. **没有一句是新写的**：来路取指针、过程取原话；判据**逐句回查账上原文**。

★**版式三条**（都是试读后抓出来的）：
· 单件事的线**收进「零散的事」紧凑清单**（真账 A 局 27 条线里 **14 条只有一件事**，铺开来比账本还吵）
· 单件事的线**不摆"一条线怎么走的"**（否则同一句话印三遍）
· **收场话不许冒充代价**（第一版把「白泽成功救出重伤妖王」当成了代价——只因它带"重伤"二字）

**分组两条边**（真账试读后定的，不并的话 42 条谋划碎成 40 多条线）：
①**同一批人（同一 owner）**的线并成一条 ②`state` 生的处境根**只在"牵动名单里恰好只有一方、且那一方账上只有一条线"时**并进它。

### 3.4 升位（两个号）

`PANEL_BUILD` → **`leg94-storyview`**（玩家可见版面真变了）· `CSS_VERSION` → **`20260921-leg94-storyview`**
★**本笔 CSS 必须升**：`.sw2-pan-*` 是**新增的一族样式**——与"只删一条死规则"那次的情形**正相反**（那次不升是对的）。
★起名避禁词：本想叫 `…-chronicle-roll-off`，`chronicle` 正在禁词表里 ⇒ 改 `leg94-storyview`（这类事 leg31b 栽过一次）。

---

## 4. ★★ 「为什么事件一直都是还开着」——**本棒没动引擎，答复在这里**

用户看完说书页问了这一句。**查实：不是显示错误，是账上真的一次都没给了结**，而且卡点是**结构性**的。

### 4.1 引擎现在只有三条自动了结路径（`src/settle.js` 的 `closeEvents`）

| 来源 | 能自动了结吗 |
|---|---|
| `plot`（谋划推出来的） | ✅ 谋划收场 ⇒ 它推出来的事一起了结（A 局 12 条结 10、B 局 23 条结 18） |
| `ripple`（承接上文长出来的） | ⚠️ **同时过三道门**：①**链头已了结** ②落账 ≥ `CHAIN_SETTLE=5` 轮 ③没有未了结的下游 |
| `state`（处境自己拱出来的） | ❌ **没有任何关闭路径**（设计如此：处境不是马达） |
| `seed`（开局埋的种子） | ❌ **也没有任何关闭路径** ← **用户那条线就卡在这** |

★咬死的地方：**种子的"链头已了结"永远为 false** ⇒ 种子底下长出来的每一环**陪着永远挂着**。

### 4.2 逐条数（两份真账 · 装置 `F:/deepseek/tmp/leg94-whyopen2.mjs`）

**A 局（60 轮 · 71 件事 · 挂 42）**：源头没关闭路径 **18** · 链头没结清 **5** · 房龄不够 **9**（合理） · 还有下游挂着 **10**（合理）
**B 局（11 轮 · 88 件事 · 挂 68）**：源头没关闭路径 **10** · **链头没结清 49** · 房龄不够 6 · 还有下游 3
**上游能追到"种子"的涟漪事件：A 局 16 条 · B 局 44 条** ⇒ **结构上永远闭不了**（世界再跑多少轮都一样）。

### 4.3 三条修法（**留给下一任，等用户裁**）

| 案 | 做法 | 代价 / 说明 |
|---|---|---|
| **甲（取证时推荐）** | 种子**连续 `CHAIN_SETTLE` 轮没有新的事从它长出来** ⇒ 当它"时过境迁"了结 | 立刻解开 A 局 16 条 / B 局 44 条；★种子的"了结"与谋划的"了结"**含义不同**（办完了 vs 没人再提了）⇒ 面板**必须分开说**，不许混成一句 |
| 乙 | 放宽门①——涟漪不要求链头了结，只看"自己 5 轮没人接着长 + 无下游" | "涟漪平息（链源已了结）"这句话会变得不诚实 |
| **丙（不动引擎）** | 说书页那格文案从「还开着」改成「**账上没了结**」，并分成**还在往下长的** / **搁着没人管的** 两行 | 分钟级；让"火烧眉毛"和"没人收尾"不再长得一样 |

★**本棒只做了呈现层**（§2/§3），**引擎一个字未动**——按纪律：动引擎要先出设计、等用户点头。

---

## 5. ★ 已登记、**本棒没做**（明确不是待办）

| # | 事 | 现状 |
|---|---|---|
| 5.1 | **种子/处境的关闭路径**（§4.3 甲/乙/丙） | 用户已知情、**未裁**；本棒零引擎改动 |
| 5.2 | 「还开着」文案分化（§4.3 丙） | 未做（等用户挑） |
| 5.3 | 玩家棋子未进世界 | ★**用户明令不用管**（「还没那个能力」） |
| 5.4 | `行动↔盘算不一致` 烟雾报警（A 局 14 次 / B 局 4 次） | 上一棒登记的"计划结算了人还在动"，未治 |
| 5.5 | 620+ 实体里只有 25 个动过 · `status` 618 个缺格 · 位置 475 个"未明" | 老问题（leg52/53 登记），未治 |
| 5.6 | 归档进度：按 `closedAt` 算龄 ⇒ 里程碑停在 1–40（tick 60 时） | 不是坏账（`ARCHIVE.hotWindow=20`），但玩家会以为"旧卷怎么才到 40" |
| 5.7 | `named`（`gate.js:43`） | ★**用户明令别碰**（leg89 登记，照旧一个字未动） |
| 5.8 | 说书页没有交互（不能筛选/只看某一条线/跳链） | 本棒只做"读得懂"，交互留给以后 |
| 5.9 | 说书页「零散的事」里那些**单件事的处境/种子** | 它们就是 §4 那批"永远挂着"的事；甲案一落，它们会自动归位 |

---

## 6. 本棒踩的坑（写下来免得下一任再踩）

1. ★★★**"我能看懂"不等于"用户能看懂"**（§0）——加一层输出，别教用户学行话。
2. ★★★**台账插行必须用脚本**，且**落盘前逐格对齐表头**：本表是 **5 列**（`| 日期 | 步骤 | 变更 | 测试 | 部署形态 |`）。
   ★本棒第一版只写了一件事（撤插行），同一棒后面又落了说书与三处 bug ⇒ **同一棒的那一行要改写成终稿**（装置 `leg94-ledger-append2.mjs`，替换而不是再插一行）。
   ★装置自己的坑：`lines[lines.length - 1]` 是**空行**（文件以换行结尾）⇒ 末行校验要先跳过空行。
   ★★**真相（本棒连错五轮才读出来，写在这里免得下一任再撞）**：**表头有收尾竖线、数据行没有**——
   表头 `| 日期 | … | 部署形态 |` = **6 根竖线**，数据行 `| … | … | … | … | …` = **5 根**。
   ⇒ 定稿判据**只有一条硬的**：「**掐头去尾后格数 == 表头格数**」（5），**别拿"竖线数相等"去比表头**（差的正是收尾那一根）。
   ⇒ 而且**格内容里的竖线一律写成「或」**（照上一棒交接 §6 第 2 条；本棒在勘误文字里写了裸竖线，当场把格数从 5 顶成 6）。
   ⇒ 还有一条我自己的教训：**"落盘后还要再改一次盘"的东西（比如 commit 号），不许提前写进盘**——写进去就会改号，白转一圈（本棒为此 amend 了两次）。
3. ★★**判据红的时候先怀疑夹具/正则**（§0.2 三次全是我的尺子错）。
4. ★★**试读自己产出的东西**（"说书"页的三条版式口径**全是试读抓出来的**：代价误收收场话、单件事重复印、零散的事太吵）。
   ⇒ 纪律：**呈现层的新东西，先自己当玩家读一遍，再写判据**。
5. ★★**`stripEngine` 那种"去词留句"的清洗，顺序要和"认名"配好**：本棒定稿是 **认名 → 去行话 → 清残留的号**（`say()` 一处收口）；顺序反了会把"有用的来路号"一起清掉。
6. ★**PowerShell 读中文文件仍是雷**（本棒一次 `Get-Content` 打出乱码、一次 `node -e` 的引号被吃掉）⇒ 中文一律 `read`/`write`/`edit`，数字/字符串装置一律 `.mjs` 文件。
7. ★**新增模块要同步 `test/module-layout.test.js` 的模块数**（45→46）——那条判据**故意**是硬编码的，就是为了逼你写清"谁加的、加了环没有"。
   本棒 `panorama.js` 是**零 import 的真叶子**，所以环基线一条未动（先例：`render-base.js`、`tag-extract.js`）。

---

## 7. ★ 只有人能跑的真机验收（下一任用）

1. **F5 硬刷新** ⇒ 页脚构建号应是 **`leg94-storyview`**；CSS 号 **`20260921-leg94-storyview`**（**不升会吃旧样式表** ⇒ 说书页会散）。
2. **编年页**：底下不再有「⚑ 第 1–N 轮已收进大事纪「…」」；账目行里 `因事而生：X 由「…」` 那一格应是**名字**（不是 `ev_xx`）。
   ★但**悬停**里仍应看得到机器号（`title="ev_xx"`）——那是给查账的人留的，不是病。
3. **大事纪页**：里程碑卡不该再有 `[object Object]`；展开那一纪应是**标题 + [链]**（`ev_10_1 链` 只剩"账上没留标题"的那几条会印号）。
4. **说书页（新页签）**：头一行四个数（几条线 / 几条还开着 / 几件事 / 几件悬着），下面「还开着的线」默认展开；
   点标题能收起；往下一段「零散的事」是单件事的紧凑清单。
5. **观棋页**：那条「更早的 第 1–N 轮已收进大事纪「…」 去翻旧账 →」**还在**（本棒没删它）。
6. ★**看完回一句"哪一段还是读不懂"**——口径与判据都在，改措辞是分钟级。

★**用户实机现状（本会话收尾 · 未变）**：`injectTagSpec:"1"` · `injectRoster:"0"` · `injectWorldTide:"1"` ·
`tagMaxActions:42` · `callMaxTokens:32768` · 模型 `gemini-3.1-pro-preview` · 世界 `大荒z` ·
★**`autoAdvance` 没设 ⇒ 世界不会自动推进**（想跑起来要去参数页开）。

---

## 8. ★ 本棒的真账取证（装置都在 `F:/deepseek/tmp/`，全只读）

用户给了**两份导出**（同一台机器、相隔 3 分钟、**两局不同的世界**）：

| 装置 | 它回答什么 |
|---|---|
| `leg94-compare.mjs` | 两份账并排（A 局 60 轮 / B 局 11 轮）：实体、事件源分布、`playerId` 形态、警告条数 |
| `leg94-export-audit.mjs` / `-probe*.mjs` / `leg94-new-probe2.mjs` | 断链扫描（**孤儿事件号 = 0**）· 归档对账 · 种子复读 |
| `leg94-drama.mjs` | **戏剧性体检**：收摊方式 25 满步 / 4 取消（**没有一次"失手"**）· 涟漪链与上游标题**共享 ≥3 字的 79%**（单调升级） |
| `leg94-objobj.mjs` | 全站 `[object Object]` 扫描（十个面 × 两局） |
| `leg94-rawid.mjs` | 可见文本里的裸机器号（**剥标签连属性一起剥**——属性里的 id 是豁免口） |
| `leg94-pan-demo.mjs` / `leg94-prototype-narrator.mjs` | 说书视图开箱 + 纯代码拼装原型（**先证明"能不用 LLM"，再动工**） |
| `leg94-whyopen.mjs` / `leg94-whyopen2.mjs` | §4 那份"卡在哪道门"的逐条账 |

**两份账的核心读数**（写在这里免下一任再跑）：
· A 局 60 轮：621 实体（`lastActiveTick` 有值 **25**）· 事件 71（结 29 / 挂 42）· 盘算 42（在飞 13 · **parentId 0**）· 编年 367 行 · 里程碑 4 条（1–40）· 编年引用 126 个事件号 **孤儿 0**
· B 局 11 轮：689 实体 · 事件 88（结 20 / 挂 68）· 盘算 18（在飞 8）· 编年 162 行 · **八个开局种子被反复当因**（26/55 条涟漪的上游是种子，且全不闭环）

---

*第九十四棒 · 2026-09-20 · 用户令：「把这里显示的文字删了，这是大事纪旧卷的东西」→「大事纪更是灾难」→*
*「你虽然能看懂整个因果，但是我看不懂，还要你给我讲解我才能知道发生了什么」→「一定要多一次 llm 调用吗，不能用代码连起来吗」→*
*「我怎么认为这个比编年好多了，就是有个问题为什么事件一直都是还开着」→「交接吧」*
*→ ★**五件都落地**：编年页撤插行 · 大事纪三处病（`[object Object]`／印标题不印号／账目行号换人话）· **说书页签（零 LLM）** · 升位两个号*
*→ ★**1049/1049 · fail 0 · 冒烟 8231 字节未变 · 工作区未提交（14 项） · HEAD `c58c1a9`***
*→ ★三条最值钱的纪律：**"我能看懂"≠"用户能看懂"（加一层输出）** · **判据红了先怀疑自己的尺子** · **呈现层的新东西先自己当玩家读一遍***
