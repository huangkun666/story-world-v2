# 会话交接 · story-world v2 · 第十二棒（2026-09-08 · K35 存储层 + K36 异步可靠性）

> ⚠️ **本文件是 story-world v2 的第十二份交接入口。接手前必读（按序）**：①`ANCHOR.md`（唯一尺子，状态行指下一阶段）②`docs/dev-process.md`（执行手册）③本文件 ④`docs/ledger.md`（工程台账，本棒 L93-L95）⑤本棒产物：`src/storage.js` + `web/idb-backend.js`（K35 存储层）、`src/async-tick.js`（K36 异步编排）、`docs/twostream-ui-spec.md`（已拍板，K30-K36 完成态）。
> 状态：**K35 存储层与 K36 异步可靠性已实施完成（282/282 全绿）· K37-K38 未实施待指令**。下一棒首个闸门：**K37 实体治理（等指令）**。

---

## 0. 人话版：这会怎么样

第十二棒把你交代的两扇门推了过去：**存储层**——世界的账不再只活在聊天里：热账随聊天走（每聊天一份，切换聊天世界跟着切）；编年长大到超阈值（500 轮或 5MB，提案）自动把最旧的段落切进本机卷库（IndexedDB），大事纪与因果链永远留在热账（割的是冗余，不是来龙去脉）；随时可阅旧卷、可导出整聊天备份、可导入恢复（带 SHA-256 验签，文件被改过一律拒收）。**异步化与可靠性**——对话回复完成后世界自动推进一步（不会跟主聊天 LLM 抢配额）；演算中连点按钮会被拦住（防重入）；演算失败世界原样不动、状态条报错、点一下重试；单轮调用有超时（120 秒提案）和字数上限（4096 提案）；设置页的模型通道和开档描述能真正保存了（密钥不显示明文、留空不动）。引擎核心零改动。测试 282/282。**下一步该做实体治理（K37）与端到端收口（K38），按你定的节点停在这里交接。**

## 1. 本棒主线

- **K35 存储层**（L93）：`src/storage.js` 热账形状（chat metadata 每聊天一份）+ 冷档轮转（编年超阈值 → 前置段入卷；**断链防线：milestones/events 原引用留热态**=红线 2 代码化；卷行深拷贝零共享；纯函数入参不改）+ 阅卷还原 + 导出/导入（SHA-256 验签，篡改/格式错/缺字段全拒）；`web/idb-backend.js` IndexedDB 卷库适配（chatId+卷号键，接口与 store 注入面同构，顶层零 indexedDB=Node 冒烟安全）；`web/index.js` 接线（热账读写每次取最新 ctx——v1 adapter 教训：chatMetadata 引用快照过期；`loadWorld()` 面板打开即载含幂等轮转；`__sw2Actions` 真实三动作：阅卷/导出/导入）；渲染层设置页旧卷卡（入卷清单+阈值走 config 提案值+导出导入按钮）+ `renderVolumeReadHtml` 阅卷视图（空卷防御）。测试 +13（storage 11 + render 2）——275/275；
- **K36 异步可靠性**（L94）：`transport-http.js` 超时（AbortController，120s 提案）+ max_tokens（4096 提案）参数化（可覆盖/可关闭，finally 清 timer）；`src/async-tick.js` `createTickQueue`——**防重入锁**（演算中再点 busy 拒）/ 失败世界不动（save 不调=写面零触碰）/ 异常兜底可重试 / 成功链 tick→save→refresh；`web/index.js` 接线：**MESSAGE_RECEIVED → 自动推进**（回复完成后推进，2026-08-28 拍板时机：避免与主聊天 LLM 并抢配额 429）+ CHAT_CHANGED → 世界重载 + 手动补推接真实队列 + **设置页表单 ↔ extension_settings 双向**（`saveSettingsDebounced` 范式；密钥掩码安全：不显示明文/不预填掩码/留空=保持不变）。测试 +7（transport 2 + async-tick 5）——282/282；
- **K37 接缝功课（等指令期完成，零代码）**：九处落点全实读清零——ssot/world-step 两 schema 落点、check-step 同构样板（newAgendas/agendaCancels）、gate `named` 集合复用（复归触发现成机制）、pack 过滤面、settle 覆灭复核参照（applyAgendaCancels 同构）、prompts 模板铁律 7 落点、abstract 书名录通道、extract 双名单（entityNames/objectNames）依据册联动；
- 用户「嗯嗯交接吧」→ 本文件 + 台账回填（L95）+ git 提交。

## 2. 现状盘点

**测试：282/282 全绿**（本棒多次实跑；`node --test` 无参）。

**模块**：src/ 26 个 .js（本棒新增 `storage.js`、`async-tick.js`；transport-http 改超时/上限）；web/ 3 个文件（本棒新增 `idb-backend.js`）+ 插件根 `manifest.json`/`settings.html`；demo 工具 ×10 原样。

**git**：本棒全部改动在工作树（K35-K36 代码+文档）；交接时一次提交。

**文档**：ANCHOR（状态行/§6 已同步）/ dev-process（§5 282/282、§9 分层表+模块地图 +storage/async-tick/idb-backend 行）/ ledger（至 L95）/ twostream-ui-spec（K30-K36 完成态，K37-K38 待指令）/ 交接 ×12。

## 3. 关键决策与实测发现（全部有台账行可查：L93-L94）

1. **K35（L93）**：存储层端点语义——**断链防线=红线 2 代码化**（冷档切的是编年文本段，里程碑/因果指针留在热态；测试断言 `hot.milestones === 原引用`）；**阈值提案态**（PROPOSED_LIMITS=500t/5MB，铁律 2 参数化可覆盖，报批随 K38）；阅卷还原走渲染复用（卷段→编年行形状）；导出导入**验签双防线**（正文篡改拒 + 形状错拒）；store 注入面让浏览器 IDB 与测试内存 mock 同构；
2. **K36（L94）**：**推进时机=回复完成后**（MESSAGE_RECEIVED，2026-08-28 拍板沿袭；v1 曾用 MESSAGE_SENT 撞出 429 与看不见本轮回复两个教训，本棒注释留档）；**防重入=编排层锁**（世界不变式由"save 不调"保证，测试断言失败路径零写）；超时/上限**参数化+提案态**（PROPOSED_CALL_LIMITS=120s/4096）；**密钥掩码三不**（不显示明文/不预填掩码/留空=保持不变防污染）；设置页保存=v1 `saveSettingsDebounced` 范式即时写回；
3. **K37 接缝功课**（等指令期，无台账行）：九处落点已实读，两处重要发现——①gate.js 的 `named` 集合（未决事件波及∪动作 target∪moveFact.object）**可直接充当「被点名复归」触发器**；②extract.js 的 `entityNames`/`objectNames` 双名单本就是细案 §3.7 依据册的**现成载体**（含别名匹配；"白小娥反复被点名"可溯源）；
4. **过程插曲（如实）**：K36 交付后用户问「存储做了检索呢？」——补讲检索职责（查档三步：查目录/按卷提档/卷内还原）与边界（按卷导航式检索，不做全文搜索——全文搜=范围外加码需走细案）；用户「嗯嗯交接吧」= 本棒收口节点（K37-K38 未实施排入任务书，不提前不积压）。

## 4. 未决点 / 待拍板（完整队列见 dev-process §6，摘最重要）

- **K37-K38（等指令）**：实体治理（status 契约/newEntities/entityFates/覆灭复核/背景化 GC/依据册/三点过滤/席位 32，A-10..A-12）→ 端到端收口（合成演练 + 100t 实体池曲线 + 冷档链 + 报批提交，A-14）；
- **ST 实机冒烟（触发即办）**：K30/K34/K35/K36 的 ST 实机验证**未跑过**——重启 ST 后扩展菜单「观棋窗口」：六页签渲染、旧卷管理/阅卷/导出导入、回复完成自动推进/手动按钮/失败状态条+重试、设置页保存全待真机；css 版本号 20260908-k35（K36 未动样式）；
- **提案数字四组待报批**（细案 §6 + 本棒新增）：冷档 500t/5MB · 主调用超时 120s / max_tokens 4096（本棒落代码，提案态）· PLAYER_DESC_LIMIT 2000 · 实体治理数字组（单轮 ≤1·空闲 20 轮·影响力 <0.05·扫描 20 轮·席位 32）；
- **长跑验证**：50t 兵变约定 / 500t 冷档沉点（随 K37/K38 顺带）；
- **缺口②实战验证**：下次真跑新快照带 world → 转制 → 全链接续入面（机制已就位，触发未变）；
- **延续项（触发未变）**：兑现通道细则、方向相悖报警（候补）、实体生灭（设计已入细案 §3.7，**实施=K37**）、未决点 1 深化、熵泵点名扩展。

## 5. 环境与命令

- 运行位置：`F:\deepseek\plugins\story-world-v2`（Node 24+，纯源码形态、零依赖、无构建）；git 仓 = `F:\deepseek\plugins`（v1 + v2 同仓）。
- **测试**：`node --test`（**必须无参**）282/282。**快速查看**：`node demo/status.js`。
- **ST 插件形态（K30+）**：`manifest.json`（id=story_world_v2）+ `settings.html` + `web/index.js`（`refreshWorld(world,{config,oldVolumes})` 渲染入口；`window.__sw2Actions` 已接真实动作：read-volume/export-world/import-world；`loadWorld()` 世界载入；`setupAsyncTicks(ctx)` 回合钩子）+ `web/style.css` + `web/idb-backend.js`（卷库适配）——重启 ST 经扩展菜单「观棋窗口」打开；浏览器侧传输配置走设置页（transport-config 链），Node 侧 env/预设链不变。
- 补跑/真跑：`node demo/capture-demo.js --world live|bystander|player [--ticks N]`（每 tick 落盘当轮 world）；转制：`node demo/convert-snapshots.js`（8 份齐编）。
- 真模型传输：env 优先 → 酒馆预设兜底（Node 侧）；浏览器=设置页（K30/K36 已真保存）。
- 活档/密钥纪律/排障顺序：与前棒完全一致。

## 6. 铁律（九条延续，全部有效）

1. 没命令不动代码；机制新增两段式。2. 上限/阈值先报批。3. 一切机制文档开口先给「人话版」。4. 改码三件事连着做：测试 → 部署 → 台账。5. 审批=never：沙箱拒绝即终局。6. 先 read 再 edit 被改过的文件。7. 任何决定过一遍检验三问。8. 数据说话：阈值/契约争议先出曲线。9. 分层归属：任何修复必须声明所改层（契约/引擎/渲染/编排）。

## 7. 交接给下一棒的现场（可信任的起点）

- 全量快照：测试 **282/282**（本棒多次实跑）、K35 存储层与 K36 异步可靠性实施完成（台账 L93/L94 逐行可查）、本棒 1 提交（交接时落）、工作树干净（除仓库根未跟踪杂项 `plugins/.git`——用户已确认不用管）；
- **ST 实机冒烟未跑**：面板挂载/渲染/存储/异步四条线全留用户环境（重启 ST → 观棋窗口 → 各页签 + 旧卷 + 自动推进 + 设置保存）；
- **引擎核心零改动**（settle/schema 未动；契约层零改动；全部改动在编排层+渲染层，铁律 9 声明）；**K37-K38 按用户节点未实施**——设计全在细案（§3.7 实体治理 + §4 施工序表格）+ 本棒接缝功课九处落点已实读；
- 数字三档分明：**定案**（报批 15 + 二批 13）/ **本棒落代码提案态**（超时 120s、max_tokens 4096——参数化随时可覆盖）/ **提案待批**（冷档 500t/5MB、PLAYER_DESC_LIMIT 2000、实体治理数字组）；
- 已知边界全部在案：熵泵不点名、编年无裁剪（冷档随 K35 已收口——轮转在编排层，引擎不变）、全文搜索未设计（范围外加码需走细案）、实体席位无 GC（K37 补）、status.js 观察项（盘算树滞留，低优先）。

## 8. 下一棒任务书

**主线（等指令）**：双流 UI 阶段续——K37 实体治理（契约+引擎+编排：status/newEntities/entityFates/覆灭复核/背景化 GC/依据册/三点过滤/席位 32，接缝九处落点已实读）→ K38 端到端收口（合成演练 + 100t 实体池曲线 + 冷档链 + 报批提交）；全部按细案 §4（验收 A-10..A-14）与铁律 4 逐步实施。

**现场事务（触发即办）**：ST 重启后「观棋窗口」挂载与四线冒烟（部署形态验证）；下次真跑 → 快照转制入面（缺口②实战验证）；ST 生成快照入库处置惯例不变。

**延续项**：按 dev-process §6 触发条件排期，不提前不积压。

---

*第十二棒交接 · 2026-09-08 · K35 存储层 + K36 异步可靠性（282/282）。下一动作：K37 等指令。*