# 全量棋盘 + 每轮镜头（story-world v2 · 细案草案 · 待拍板）

> 状态：**草案待拍板**。触发：用户实机三连问（2026-09-09，第十九棒）——
> 「我需要大势，张力一并带上」「为什么不把势力和实体全部放进去」「如果只拿席位数量的，那还要搞这么多轮换的算法干嘛」，
> 以及「你得到了全部势力和角色难道一定要 llm 全部看到？什么才是棋盘，还没开演就淘汰了大部分实体的资格」。
> 数字全部**提案态**（铁律 2）。施工声明层（铁律 9）。本细案拍板后回填 ANCHOR 决策记录、修订 K37/T-U5 与报批 #4 口径。

## 0. 人话版：这会怎么样

你的世界一开局就是**全书实体站在棋盘上**（345 个名号全部入账、全都有分量），不再"开演前先淘汰 313 个"。
模型每轮看到的只是**镜头**——引擎按分量和点名情况每轮挑"该拍谁"（预算内自适应），没拍到的人照样在世界里活、照常被算账，
长期没戏份的退二线（背景化），被点名立刻回场（复归）——轮换算法还在，但它管的是**镜头进出**，不是**资格存废**。
大势也改真：张力照常有自己的一行，另外再给一行真·天下大势（张力+环境+浪尖拼装）。你点头即开工。

## 1. 病根（实读证据，非猜测）

| # | 位置 | 现状 | 病 |
|---|---|---|---|
| 1 | `abstract.js` seedBookEntities L313-335 | 按书序入席，`active >= POOL_CAP(32)` 即 break | **开演前淘汰资格**：345 名号只进 32，313 个从未入账 |
| 2 | `settle.js` retireInactive L677-690 | 任何 tick active>32 → 按活力低者强制退 | 即使全量入账，第一轮结算就会把 313 个强制退休——镜头容量当棋盘容量 |
| 3 | `settle.js` spawnEntities L572-583 | 新实体入局按席位上限制 | 同为"镜头容量=资格"混同 |
| 4 | `pack.js` L12 + trimPack | 实体段=全部 active 无排序无镜头选择，超预算靠尾部裁剪 | LLM 视野无"拍谁"机制，全塞或乱裁 |
| 5 | `render.js` 席位计数/大势行 | 「大势」一名实挂张力三件套 | 名实错位（用户指认）；无真·大势行 |

## 2. 机制

### C1 资格=入账：开局全量 + 势力净化折叠（编排+引擎层）

**原则（用户 2026-09-09 指令 + leg16/蓝图 §3.7/敲定稿 J 条续持）：全量资格，但势力必须抽象——不能 345 个名号拍平**
（用户原话：「旧插件抽象了 100+ 势力，大量势力互为父子。若都当独立实体：席位被碎片占满、父子同框行动违背常理、分量失真」——收敛=分支做归属限定+升格/降格通道+提取阶段净化折叠）。

- **角色类名号（当前 237 条）整量入账**（`e_bk_N` 连续编号；同名跳过、dead 不回魂保留）；
- **势力类名号（当前 108 条）净化三步（引擎确定性，规则不靠后缀猜）**：
  1. **地理/环境误标剔除**：抽取契约 `kind` 增第三值 `location`——洲/山/谷/墟等"地名"由模型标注，净化时 **location 不入实体池**（记入 canon 备位置机制用，将来位置集扩展收）；
  2. **归属折叠**：书名录条目增可选 `parent`（势力条目注明**书中明述**的上级势力名；prompt 纪律=只提取不创作，未明述不填）；引擎按 parent 折叠——子名号**不独立入账**，记入父实体分支表 `entities[].branches`（契约可选字段；blueprint §3.7 接轨：分支=盘算归属限定的命名空间来源）；
  3. **校验**：parent 名不在册→弃关系留警告；parent 自指/成环→拒；
- **未明述隶属的势力（如万妖盟 vs 妖师宫）一律独立入账**——宁多勿并，合并只认书的明述；
- **初始分量预填**：seed 时按 `computeWeight`（同 settle 口径含张力）为全部入账实体预填 `world.weights`（K7 教训：空权重首轮全员静默/误判）；
- 现状量级（导出世界实测）：345 名号 = 237 角色 + 108 势力（含 ~13 location 误标 + 若干变体系）→ 折叠后实体 ≈ 300±，均在 `POOL_TOTAL_CAP=512` 之内；
- **升格/降格通道（分裂/吞并 → 分支转主）与盘算 branch 归属限定 = 排期独立 K**（触发=折叠落地后；三家分晋/田氏代齐为历史原型）。

### C2 镜头=每轮视野（契约层 pack）
- pack 实体段改为**镜头选择器**：按 分量降序 排列 + **例外保送**（本 tick 被点名者 ripples/target/落子对象、在飞盘算属主、近 2 tick 活跃者 置顶）→ **预算内取最大前缀**（沿用现有 token 估算与 budget 机制，镜头容量=预算自适应，**不引入新数字**）；
- 未被镜头拍到的实体：仍在账本、分量照常结算、可被事件点名、可被 gate 静默/应答——只是不进本轮 LLM 视野；
- 同口径纯函数 `lensList(world)`（引擎层只读）供渲染"本轮镜头"展示复用（一处定义、多处读取）。

### C3 轮换归位（引擎层 settle/gate）
- **删除「超席强制退」**（POOL_CAP 从 active 语义中移除）；
- 保留：**闲置退休**（连续 20 轮未活跃 + 影响力<0.05 → status=retired，账保留编年「淡出」）、**复归**（被落账点名 → retired 升 active）、**静默/应答门控**（gate 零改动）；
- `spawnEntities`（newEntities 通道）不再按席位拒——改**全量池顶** `POOL_TOTAL_CAP=512`（提案，>345 现状 167 余量；超顶拒建+警告，防 SSOT 无界）；
- 语义澄清（写进注释与文档）：**资格=在册**（除非覆灭裁定 dead）；轮换管的是**镜头在场与否**。

### C4 大势行 + 张力行并带（渲染层）
- **大势行（真·天下大势）**：确定性拼装一句——张力极/方向 + 强度带词（BANDS）+ 环境量危险带 + 浪尖（`derivedFrom` 解析 TOP 来源）——零创作零预测（ANCHOR「走向预测已裁」保持）；
- **张力行**：张力三件套（极/方向/强度）独立一行，不再顶「大势」之名；
- **实体页**：全册展示（345，分页/搜索）+ 「镜头在场」徽（lensList 口径实时名单）+ 计数文案「全册 N · 本轮镜头 M」；
- 暗处/黑名单 A-3 纪律照旧（引擎 id 只进悬停/管理区）。

### C5 观测台口径（引擎层 observatory，只读）
- `residencyStats` 改算**镜头名单内**驻留（新口径：镜头内闲置分位/摸鱼名单）+ 全册计数标注；
- 100t 实体池曲线重跑（345 起步基线），坏账重扫 0 断言保留。

## 3. 验收判据（可测）

- A-1 全量入账+净化折叠：237 角色 + 折叠后势力全部出现在 SSOT（计数锁 + 抽样断言 + location 不入池 + parent 折叠分支表 + 无明述独立 + 幂等重跑 + 环/缺父拒）；
- A-2 初始权重：seed 后 weights 全覆盖（345/345 有值，t1 门控统计合理、无"全员静默"）；
- A-3 镜头：pack 实体段 ≤ 预算（token 估算断言）、排序正确（分量降序+例外置顶）、确定性逐字节；lensList 与 pack 同口径锁；
- A-4 无超席强制：345 active 世界连跑 N tick 无"被强制退休"事件（清扫断言）；闲置退休/复归回归全绿；
- A-5 newEntities 全量池顶：>512 拒建+警告（合成探针）；
- A-6 渲染：实体页全册分页/镜头徽/计数文案；大势行=拼装句（含张力+环境+浪尖）、张力行=三件套；A-3 黑名单双扫零命中；
- A-7 C5 读数口径切换后 100t 曲线入库、坏账 0；
- A-8 全量回归 `node --test` 358 基线 + 新增全绿。

## 4. 范围限制（本批不做）

- 小卡（E 条）/ 焦点公式（H 条）/ 日历+跳卷（G 条）/ 复归拉杆——二期药单不变，随读数放行；
- 棋盘容量（POOL_TOTAL_CAP 512）之外的池治理（超顶后如何淘汰）——触发=顶达线实测；
- 大势行的措辞风格库——先上确定性拼装版，措辞随真跑反馈迭代。

## 5. 拍板点（数字提案态，铁律 2）

1. C1 全量入账 + 初始权重预填 + **势力净化折叠**（location 不入池 / parent 只认书明述 / 分支表）——**做**（方向已被用户钉死：全部放进去 + 势力必须抽象）；
2. C3 删超席强制退 / 保留闲置退休+复归——**做**（推荐）；
3. `POOL_TOTAL_CAP = 512`（全量池顶，提案）——接受/改值/挂起（挂起则 newEntities 不限顶，SSOT 无界风险自担）；
4. C2 镜头容量=预算自适应（复用 4k 提案，无新数字）——**做**（推荐）；
5. C5 观测台口径随调——**做**（推荐）；
6. **势力抽象契约**：bookEntities 条目增可选 `parent` + `kind` 增 `location`（抽取 prompt 纪律=只填书明述；净化/折叠/校验全引擎确定性）——**做**（推荐；数字零新增）；
7. 敲定稿 F 条「统一 32 席」与报批 #4「席位 32」**由本细案修订**（今日指令优先）——确认修订。

## 6. 施工序（拍板后执行，与既有主线零冲突）

- K43 seed 全量+权重预填（编排+引擎，abstract/settle 纯函数 + 测试）
- K44 pack 镜头选择器 + lensList（契约+引擎层 + 测试）
- K45 settle 删超席强制 + 池顶 512（引擎层 + 测试）
- K46 渲染：大势行+张力行+实体页全册/镜头徽（渲染层 + 测试）
- K47 observatory 口径 + 100t 曲线重跑（引擎层 + 测试 + 报告）
- K48 文档回填（ANCHOR/台账/报批 #4 修订/细案状态）+ 冒烟 + 交接

## 7. 检验三问

① 更像自己会转的棋局——✓ 全部棋子都在棋盘上，镜头按分量和点名自转；
② 分量更接近客观——✓ 全量分量可算，无人因"没被拍到"而失真；
③ 棋局更看得见——✓ 全册+镜头双视图，大势与张力分列。

---

*细案草案 · 2026-09-09 · 第十九棒 · 数字提案态 · 待用户拍板。*