# ANCHOR · story-world v2 锚定文档

> 性质：新引擎的第一份文档——**开工前的尺子**。记录方向、架构轮廓、已定规则、未决点、决策记录。
> 状态：**当前进展以 `docs/START-HERE.md` 为唯一入口**（一页读完即可开工：走到哪、**四条不可越的线**、动手前六条规矩、按需查表）。**下一动作**＝用户实机复验（构建号 `leg25f-whereabouts`；观棋侧栏应为「各归何处」21 组）+ 清三条小活 P1/P2/P3（见 `docs/session-handoff-2026-09-11-leg25f.md` §7）；**历次变更**见 `LEDGER.md`（一行一次变更的索引）与 `docs/ledger.md`（全文）。本节此前累积了每棒的状态长文（最长时单行 9.4 KB），已按「文档重塑」收进交接与台账——历史逐条在 git 提交信息与 `docs/handoffs/` 里可追，**本文件不再长肉**（ANCHOR 是尺子，不是流水账）。
> 定位：另起炉灶，永远跑在 SillyTavern 内；旧插件（`plugins/story-world` + `plugins/shared`）保留作对照与活档观察。

---

## 0. 人话版：这会怎么样

旧项目 24 天、525 提交、683 测试证明了两件事：**方向的路是对的**（世界设定抽象是必要地基；"告诉它不如引擎做"是铁律），以及**补丁式治理到头了**（130 条边界、模型绕行、越管越累、主角永远在镜头中央）。v2 不修旧系统，**从零搭一台新引擎**，只搬旧项目里被证明的部分——抽象管线（只提取不创作 + 书指纹缓存）、黄金样本测试、测试基建、部署纪律。

这台新引擎是一个**每轮随 RP 演化一步的活世界**：势力与角色按自己的盘算行动（结盟/背叛/谋划/冲突），事件从行动里长出、带因果落子；没有"主角"类别，玩家只是**一枚由你控制的棋子**，分量按客观状态计算；你通过两个口子与它相处——**RP 对话是你落子的手，观棋窗口是你观棋的眼**（动态流 + 各方位置动作 + 当前格局）。

## 1. 方向（用户逐字确认）

> story-world 想成为的东西：一个你每推进一步 RP、就演化一步的活世界。各方势力与角色按自己的处境和盘算行动——结盟、背叛、谋划、冲突——事件从他们的行动里长出来，带着原因和后果。主角不存在于系统的规则里：只有一枚由你控制的棋子，他的分量由客观状态算出——底层小民时世界的浪潮绕他而过，手握重权时他的一步能改棋局走向。你通过两个口子与它相处：RP 对话是你落子的手；世界窗口是你观棋的眼——那里有一条持续更新的动态流，有各方此刻的位置与动作，有整个世界的当前格局。你随时能看见：谁在谋划什么、哪件事正在落子、这个世界刚才发生了什么、它接下来想往哪走。

## 2. 复盘结论（四句）

1. **实体抽象是必要地基——没错**；错的是目标函数：24 天在优化"账的精确"，而我们要的是"棋的活"。
2. **根因不是没有计划，是没有先立方向**——计划追着实弹问题跑，越跑越乱。本文件就是先立的那把尺子。
3. **账和棋不是两个问题**——账的目的重新定义为：算分量 + 驱动棋局 + 支撑窗口。记什么不记什么，由这三个用途反推。
4. **主角中心化的解法不是约束主角，是撤销"主角"这个类别。**

## 3. 架构轮廓（七块）

### ① 世界状态（SSOT）
一份 JSON 文档。账只记四样：**实体**（势力/角色 + 客观属性）、**分量缓存**、**Agenda 池**、**事件链**。不记一切——只记"算分量、驱动棋局、支撑窗口"要用的。

### ② 每轮管线（Tick = 一次 RP 回合）
- 触发：沿用"回合完成即推进"。
- **世界步原料 = 世界自身的状态**（活跃实体 + 各自 Agenda + 未决事件 + 分量）；你的对话只被提取成"你的棋子这步做了什么"**一条事实**，插进原料——不是对话原文。
- 一次主 LLM 调用 → 结构化输出"世界步"（实体动作 / 新事件（带因果）/ Agenda 推进 / 状态变更），**真 schema 强制**。
- 引擎结算（按序）：schema 校验 → 身份/因果校验 → 冲突裁定 → 因果挂链 → 分量重算 → 落账 → 编年 → GC。
- 双流渲染：RP 注入（可见性过滤）+ 观棋窗口追加（全局）。

### ③ 分量引擎（纯函数，确定性）
入参 = 硬实力 / 职权 / 人脉 / 情报 + 当前环境；出参 = 分量 + 因果传播半径。**模型看不到它、不参与它**。它决定三件事：谁这轮值得动、冲突的空间、你的棋子能感知多远。

### ④ Agenda 系统（轻量 BDI，树形）
{目标、阶段、明暗、步骤、依赖、预计发动}。**引擎管生命周期**（创建/推进/完成/变形/取消/强制结算），模型只负责在"世界步"里提议推进。`visibility=CONCEALED` 的盘算照常流动——**一个人可以合法地待在暗处**（薛铁衣的正路）。

### ⑤ 事件与因果链
事件 = {标题、源（三类：盘算/状态/波及）、波及、上下游指针}。**无源事件引擎直接拒绝**。编年 = 这条因果链的可读展示。

### ⑥ 双窗口
- **RP 注入**：可见性掩码 = 分量 × 位置 × 情报；行迹纪要式事实，预算有限（原对话窗不退化为主角旁白）。
- **观棋窗口**：插件 UI 面板（本地单进程，无 SSE）——动态流（写得像棋局、值得读的文本）+ 各方位置与动作 + 当前格局（宏观张力、在飞盘算、走向预测）。

### ⑦ 已裁掉的
Neo4j / PostgreSQL / Qdrant / WebSocket / SSE / 几十万 NPC / 三级不同模型 / 独立服务器。快照回滚保留（并入现有快照机制，作为"分叉重下棋"的后续增强）。

## 4. 已定规则

### 4.1 Agenda 依赖与死锁
- 不做"依赖最多一层"限制（深链合法，真实政治如此）；**引擎做环检测**（每次新增/变更时）。
- 检出环 → 引擎自动拆：**解除低分量方"等待对方"的那条边**（分量相等则阶段浅者先让）；被拆方转"伺机而动"——自行找机会动，不是旧病"按兵不动"。
- 拆环必须留痕（编年记一笔）。

### 4.2 事件的源（三类，缺一不可）
- **盘算驱动**（agenda 推进出来的）；
- **状态驱动**（实体/环境的处境：醉话说漏嘴、马车翻进沟、兵心不稳）——**意外必须有源，但源不是"预谋"，是"状态"**；
- **波及驱动**（其他事件的下游）。
- 闭环：事件源自盘算/状态/波及；新盘算源自事件/状态/决策。无源之物不存在。

### 4.3 修剪 GC（生与死两半）
- **生**：每轮新生 Agenda ≤2；在飞 Agenda 全局 ≤15（宏大层 ≤5）。超限不新建或把最低分量盘算转"伺机"。
- **死**：Agenda 必须带 `max_steps` + 进度计数，到阈值未决 → **引擎强制结算**（成功/败露/变形）；事件链前端全量展示（历史卷轴），但**演化上下文只含**：未决事件 + 最近 2 tick 关闭的 + 在飞盘算 + 在动实体；归档节点从 LLM 输入剥离，**因果指针保留**——割断的是喂给 LLM 的文本，不是链条本身。
- 数字**已报批定案**（2026-09-07 报批支线 #1-3）；新上限/阈值仍走报批（老规矩不变）。

### 4.4 Agenda 流动五道规矩
1. **诞生挂因**（只能从事件/父盘算/实体处境里生）；
2. **委派与兑现落痕**（大拆小、小兑现大各记一条事件）；
3. **推进留痕**（每步都是可读的编年条目）；
4. **终结产果**（死盘算变成世界的后果，不是删除——每个终点是下一条链的开头）；
5. **明暗双流都在流**（隐藏盘算照走，只有露出水面的部分成为可见事件）。

### 4.5 层级与上限
- **组织单位 = 实体；模拟单位 = 盘算**。
- 树形：势力级大盘算 → 人物级子盘算 → 动作步骤（叶子）。层级要浅（大→子→步骤），不发明多级分类法。
- agenda 上限三必须：**必须有主、必须可被步骤推进、必须能在世界时间里结算**。超出上限的不是 agenda（归环境/大势）。
- 宏大层**里程碑分段**：每段自己的 `max_steps`，到点强制结算一次；宏大 ≠ 永不完结。
- "大势未聚"是合法状态（无主冲突、张力低位），窗口如实显示。

### 4.6 设定大势层
- **无主**：来自世界书，是背景状态/偏置，不参与因果链（不是 agenda）。
- 复用旧抽象管线（只提取不创作 + 书指纹缓存），产出形状升级三处：
  ① 设定事实 → **结构性张力**（极 / 方向 / 强度）+ 环境量；
  ② **冻结层与演化层分离**：大世界观常数冻结（书指纹不变不重抽）；动态环境量引擎小步推、事件可改、**模型不可改**；
  ③ 大势仅两来源：世界书提取、盘算浪尖派生——**模型不得发明第三个来源**。

### 4.7 双作者权威阶梯（一个世界，两个作者）
聊天 LLM 写场景台词，模拟器写棋局状态——冲突不是可能，是必然；在缝隙处按阶梯裁：

**设定（世界书）> 引擎结算（状态/事件/分量）> 对话事实（输入）> 渲染（风味）**

- **分界**：模拟器拥有"结算后的世界状态"；聊天 LLM 拥有"场景内的对话与感官"。对话决定**你的棋子试图做什么**，模拟器决定**这步在世界上兑现成什么**。
- **四缝规矩**：
  1. **时差**：聊天只写"尝试"与"现场"，结果归模拟器，经下一轮注入送达——落子不等于得分，得分是棋局给的；
  2. **NPC 双作者**：名实体的状态以落账为准——对话是输入事实，**账吃对话、对话不改账**；NPC 在对话中的拒绝/行动 → 模拟器登记事件、相应盘算记"受阻"重排；
  3. **发明权**：主角行动只能从对话提取；模拟器永不写主角行动，只写世界对他的影响（旧红线保留）；
  4. **信息裂隙**：风味层脑补允许（街角卖糖人）；对**已落账的有名实体**的断言，必须以注入"已定事实"锚块为准。
- **对话是输入事实，不是最高准绳**（旧"对话窗=最高准绳"定调在 v1 作废——它就是主角中心化的来源之一）。

### 4.8 ★泛用性要求（用户令·2026-09-11 逐字：「记住不能泛用到所有世界书的设计都是垃圾」）

**这是设计否决线，不是优化建议。** 任何机制在动手前、以及落账进台账前，必须过下面这把尺子；**过不了就不做**（不是"先做了以后再说"，那正是这句话要治的病：做出来的东西只对一本书有效，等于给世界装了根只认一本字典的插头）。

**判据三条（层层收紧，可复算）**：
1. **能跑不崩**：换任何一本书（任意编排、任意语言、有无 key、有无层级标题），机制都不得抛错、不得卡住链路（tick/初始化/加载照常）。—— 及格线。
2. **不共线**：**有没有效果**可以随书而变（这世界的事）；但**不能"书里没有某形态 ⇒ 正确处理不了"**。缺形态只许让该机制"不生效"，不许让它"给出错误结果"或"整条链路停摆"。—— 见到"必填前提"就是嗅到垃圾。
3. **宁缺勿造**：形态不满足时**不写**（而不是写猜测值/占位值/默认值）。空着永远优于填错。与 §2 硬规矩第 2 条同源。

**尺子的直接推论（禁用清单）**：
- ❌ 把某本书的**文本形态**（如 `- 名号 (男, T8…)` 成员行、`[势力: X (…)]` 标签行、`核心底蕴：…`、尖括号层级标签）当作**必要条件** → 只能做**加分通道**，且必须"取不到就静默不写"。
- ❌ 把**占位值当判据**（如 `location:'未明'` 被当成"已有值" ⇒ 该栏永远不再查）—— 已在本棒修掉一次。
- ❌ 用**词表**判断书里的语义（是势力/是角色/是地名）——用户明令过"被很多词表法弄得很烦"；且实测实教书的 key 名单是剧集标题，词表必炸。
- ❌ 拿**某一本的覆盖率**当机制有效性证据（大荒 10%/26% ≠ 通用；实测其余书 0–1%）。报告覆盖时必须**附多书实测**。

**唯一被实测证明通用的设计骨架**（八本真实世界书审计，`demo/audit-parent-genericity.js` 可复跑）：
> **① 书条目正文是唯一必需输入**（每本书都有"条目 + 正文"这个最小共同结构）；
> **② 模型负责语义**（哪行是花名册、这名字是组织还是人——v1 就在用这条，跨书零改动）；
> **③ 引擎负责确定性校验**（值 ∈ 名单 / 有书面依据 / 三态：认·反驳·未验证——`verifyClaimedParent` 即此形）；
> **④ 形态判据只能做加分**，取不到就退回"未验证"或干脆不做，**绝不当必要条件**。

---

## 5. 未决点（开工时逐个解决）
1. **落子提取管线**：对话 → 结构化事实（动作 + 对象 + 位置）——引擎规则 + 一次小提取调用，输出必须结构化，不是散文。这条管线质量不过关，棋局第一步就歪。
2. **事件位置合法性**：事件的位置必须来自世界状态（路线/驻点），**不得为贴近玩家而移动**——防止主角中心化以新形态回来。
3. **薄裁定器**：只按量裁硬结果（分量 + exposure），过程与台词交给模型；不按冲突类型堆规则——否则 130 条旧债穿新衣。
4. **张力 schema**：设定大势层的接口设计——先读旧 `abstractCanon` 的产出格式，再定 schema 与接法。
5. **长跑防线（跨轮累积三防线，设计已定、实现时机待定）**：
   - ① **档案摘要化**：每 N tick 或每次结算，把一段因果链压成里程碑摘要，原始链进冷档——防存储无界，防"上限硬砍"复辟（旧系统 18M 聊天文件的教训）；
   - ② **agenda 现状摘要语义随行**：剪枝只砍文本、不砍语义——长命盘算（50 tick 的兵变）后期不"新开一局"，第 12 tick 的约定第 40 tick 还记得；
   - ③ **属性保鲜与熵泵**：Mutation 必带属性增量 + 静止衰减规则 + 设定大势演化层持续制造摩擦——防分量僵化（棋局钉死在旧权力结构）、防世界冷却（盘算都结清、没人开新的）。
   - 实现时机：纵向切片跑通后按依赖并入——摘要化跟 GC 一起做、保鲜跟 Mutation 一起做、熵泵跟设定大势层一起做。

## 6. 决策记录

- **永远跑在 SillyTavern 内**（不变，用户明示）。
- **另起炉灶**（新目录 `plugins/story-world-v2` 起家）；旧插件保留作对照与活档观察，新引擎亮了再说替换。
- **复用清单**：抽象管线（只提取不创作 + 书指纹缓存）、黄金样本三测试、测试框架与部署纪律、UI 渲染范式（具体能否直接搬，读代码后确认）。
- **质量防线**：真 schema 强制解析（还旧债 D-C3）、分量/结算器纯函数单测、黄金样本继续护数据层、"棋好看"单独立一条生成质量锚（黄金样本里放几条"值得读的世界条目"作样板）。
- **运作顺序**：锚定文档 → 纵向切片（第一条动态流）→ 按依赖加深（分量引擎 → 盘算树 → 因果链 → 设定大势层 → 双流 UI）。
- **2026-09-07 拍板**：用户过目，按 ANCHOR 原文执行。未决点 5（长跑防线）设计已具体化于 `docs/longrun-defenses-spec.md`，随切片接缝实施。
- **2026-09-07 拍板（第五棒·设想对照审计处置）**：观棋窗口**不做"走向预测"**（§3⑥ 该半句裁剪——窗口定位=数据/现状视图，避免推断性表述的信任风险）；**暗处渲染采纳**（§4.4⑤ 完整版：concealed 盘算推进不进动态流、露出水面部分照常；数据窗口全局可见不变，K10 不冲突）。
- **2026-09-07 拍板（第八棒）**：报批支线 15 项一处定案（`docs/archive/ratification-batch-2026-09-07.md` 状态转已定案，台账 L65）；设定大势层 T1-T6 全案采纳 → K24-K29 实施完成、判据 A-1..A-8 验收通过（台账 L66-L75）。
- **2026-09-08 拍板与裁决（第十棒）**：报批二批 #1-13 一处定案（#14-16 注入词表撤销——"不能规定词=实力"）；T7 全案拍板并实施（玩家注入改版：词表 → 一次 LLM 小解析，细案 §3.4 v1.1）。台账 L80-L82。
- **2026-09-08 拍板（第十一棒）**：双流 UI 细案全案（`docs/twostream-ui-spec.md` T-U1..T-U8，用户「继续」点头）——UI 六页签骨架 + 玩家语言词典 + 来源一行化 + 设定档案页 + 冷档 IndexedDB + 实体治理（生三通道/覆灭裁定/背景化 GC/席位 32）。台账 L86 起。
- **2026-09-08 实施（第十一棒）**：K30-K34 完成（台账 L87-L91）——浏览器适配套（transport-config/可载性扫描/ST 插件骨架）、抽象管线执行器（abstract.js，指纹缓存 1→2）、玩家解析接线（player-setup.js + meta.playerParse 溯源账，force 重解析只覆盖解析注入键）、渲染核心六页签 HTML 面（render.js renderAll + 玩家语言词典 + A-6 设定档案页全达）+ web 面板接线（refreshWorld/data-action 委托）+ 全量样式。**用户明示节点：K34 收口停手交接（K35-K38 待指令）**。台账 L92。
- **2026-09-08 实施（第十二棒·K35 存储层）**：`src/storage.js` + `web/idb-backend.js` + web 接线 + 渲染层卷清单/阅卷视图（台账 L93）——热账（chat metadata 每聊天一份）、冷档轮转（编年超阈值→前置段入卷，里程碑/因果指针留热态=红线 2 代码化）、阅卷还原、导出/导入验签（SHA-256）；阈值 500t/5MB 提案态（铁律 2，随 K38 报批）。**K36-K38 待指令**。275/275 全绿。ST 实机冒烟留用户环境。
- **2026-09-08 实施（第十二棒·K36 异步可靠性）**：`src/async-tick.js` + `transport-http.js` 超时/上限参数化 + web 接线（台账 L94）——回合完成自动推进（MESSAGE_RECEIVED，2026-08-28 拍板时机=回复完成后）、防重入串行锁、失败世界不动+状态条报错+重试、手动补推接真实队列、设置页表单↔extension_settings 双向（密钥掩码安全）；超时 120s/max_tokens 4096 提案态（铁律 2，随 K38 报批）。**K37-K38 待指令**。282/282 全绿。ST 实机冒烟留用户环境。
- **2026-09-08 交接（第十二棒）**：K35 存储层 + K36 异步可靠性完成（台账 L93-L94），`docs/handoffs/session-handoff-2026-09-08-leg12.md` 落盘（282/282；下一棒任务书：K37 起等指令；ST 冒烟留用户环境；接缝功课：K37 九处落点已实读——两 schema/check-step 样板/gate named 复用/pack 过滤面/settle 参照/prompts 模板/abstract 书名录/extract 依据册）。台账 L95。
- **2026-09-08 实施与交接（第十三棒）**：ST 实机冒烟主场九连修（台账 L96-L106）——部署 junction（L96）、演示桥 export-world.js（L97）、首开死结+板式接线（L98）、属性释义与无障碍 A 案（L99）、设置持久化两连修（L100）、推进持久化（L101）、真模型诊断通道 + ripples 模板 v2-ripples-1（L102-L103）、每轮进展计数（L104）、异步 save 大修（L105）、编年措辞零代号（L106）；288/288；`docs/handoffs/session-handoff-2026-09-08-leg13.md` 落盘。**用户点头新方向：因果链视图 + 编年过滤器**（完整故事链条=连接的事实链；细案未写，排期用户定）——与 K37-K38 主线并存，均等指令。
- **2026-09-08 拍板（第十四棒）**：因果链视图 + 编年过滤器细案（`docs/chronicle-chain-view-spec.md`）**C-1..C-5 全案定案**（用户「可以开工了」，v3 珠链演示图 `docs/chain-view-mockup.html` 认可）——编年行可选 kind 章（五筛）/ 引擎层 chain.js 链展开器（上承+盘算弧线+里程碑聚合穿透）/ 渲染五筛+珠链视图 / 编排视图态+open-chain；零新增提案数字；施工 K39-K42 启动（与 K37/K38 主线零依赖可并行）。台账 L110-L111。
- **2026-09-08 实施与冒烟修复（第十五棒）**：①冒烟三症状修复（链按钮 data-action/open-chain 缺失 + 大事纪 ids 第二链入口 + 闭环/涟漪平息行 chainRef 可选字段（新行盖章）+ 历史闭环行按 id 解析挂链——纯渲染派生零写账；台账 L116-L118）；②编年五筛实机复验=旧账恒显示语义（非 bug）；③ST 冒烟四动作销账（用户实机；自动推进线「编年 +N 行」验证通过）；④小尾巴清理（仓库根嵌套 `plugins/` 空壳目录删除、settings.html 六处占位文案刷新、sw2_limits 只读展示态；台账 L119）。测试 307→325。
- **2026-09-08 实施（第十五棒·K37 实体治理）**：双流 UI 细案 §3.7（T-U5 已拍板）施工完成——契约层（实体可选 status / world-step newEntities+entityFates / meta.dialogueBook 依据册 / frozen.canon.bookEntities 书名录 / prompts v2-agenda-t1-2）；引擎层（check-step 语义校验 / gate 三点过滤②③ + 透传 / pack 过滤① / settle 入局·覆灭复核·复归·背景化 GC·依据册记账）；编排层（abstract 书名录抽取+seedBookEntities 幂等入账挂 loadWorld）；渲染层（席位计数只算 active + 退休/已灭标注）。**A-10..A-12 全达**（`test/entity-governance.test.js` 18 则）；打崩≠灭 K7 万法阁回归在案；数字组提案态（随 K38 报批）。**325/325 全绿**。台账 L119。下一闸门：**K38 端到端收口（等指令）**。
- **2026-09-08 交接（第十五棒）**：`docs/handoffs/session-handoff-2026-09-08-leg15.md` 落盘；**明确进入讨论阶段**——用户实机质询三连（真跑判定/新实体数值/NPC 台词行动与模拟器）引发的三个设计点未拍板未实施（依据册广播缺口 / attrs 零值哑巴 / NPC 断言登记=未决点 1 深化形态），讨论分析原样封存 leg15 §3-4；**下一棒先读该节、等拍板（补差包/K38/NPC 细案排序）**；K38 主线等指令；325/325。台账 L121。
- **2026-09-08 交接（第十六棒·讨论棒）**：`docs/handoffs/session-handoff-2026-09-08-leg16.md` 落盘；**零代码改动**——「模拟化」深谈收敛：**蓝图四柱**（史书单史/史官记录+执法/证人+时钟/镜头=阅读层）；七难点收敛（耦合=facts 呈堂裁决在席、客观=调参+历史盘问（天花板=一致性）、身份=人物常驻簿、时钟=对话宣示（一致优先自转）、验证=观测台找死仪器、实体生命期=名册舞台分工+分量地板离席、子势力=分支做盘算归属限定）；叙事重构（单史多幕；预设汇聚点否决→给场不给点+相遇必记录；**焦点从实体剥离=账本热度重心（事簇=卷）**；活起来=写得活+护得住）；外发稿 `docs/archive/design-discussion-sim-blueprint-2026-09-08.md` 落盘供集思广益；**下一棒第一动作=读用户带回的集思广益方案、三方对照、拍板定序**；K38 等指令；325/325（上棒实测，本棒未重跑）。台账 L122。

- **2026-09-09 拍板与修复（第十八棒·初始化流程修复）**：用户实机质询三连（「角色卡摆在那缺什么源」「v1 都能直接做到」「一张卡能有多大」）→ 初始化流程修复（台账 L124）——「开始新世界」从"显式换源门 + 缺源原生 prompt"改回 v1 手感**「角色卡即设定源」**：worldBook（换源专用）优先，缺省自动合订 角色卡四件套 + 世界信息/卡内置世界书（预算裁剪，巨卡防爆）；原生 prompt 从初始化路径清除；位置集默认单点（提案态）；预算常量全部提案态随报批。此条 = ST 实机复验发现 #1 处置完毕，其余待用户环境复验。

- **2026-09-09 修正（第十八棒·世界书完整摄取）**：上一条「预算裁剪」方案复核 v1 实码被验伪——v1（director.js）是**设定五件套头 3 万单发 + 世界书名录全条目分块多调用**（拆半自适应/失败降级/全书级出处校验），adapter L19 教训「头 400 字砍掉名字密集段」。已按范本改正（台账 L125）：世界书全量不截断，仅留 50 万防御上限；禁用标记尊重；内置书双路径；小书单发行为零变化。355/355。

- **2026-09-09 废除与交接（第十八棒）**：用户判死刑后**换源/worldBook 机制整体废除**（台账 L131）——初始化只剩自动两条路（世界信息/卡内置书 + 卡四件套），"源"概念从 UI 与存储中消失；报批清单增 #11 合订与抽取组、#12 抽取预算 16384（思考型模型 finish=length 实证）；**实机悬案移交**（浏览器运行时 ctx 取数未实证，成功路径源诊断日志待补）；**生成层方案提出待拍板**（v1 式种子：现状/谋划≤3/事件≤3/编年 1-2 行）；焦点公式修订讨论（提及出局/加权）与复归拉杆（名册+建议权）挂起待真跑读数。交接：`docs/handoffs/session-handoff-2026-09-09-leg18.md`，358/358。

- **2026-09-11 拍板与实施（第二十四棒·片1「停抄书」）**：leg23 设计大转向后的**第一次落地**，按交接 §4 五片切法逐片走。**用户拍板两处**：①账本六项字段=**认** ②**分量分数=删**（改确定性粗规则排序：谁刚动过/谁在飞盘算里/谁在镜头名单上）——后者落地在片 3。**片1 实施完成（提交 `a5060ae`）**：**抽取不再把书抄进账本**——关系轮/属性轮/出处细节校验/单实体补抽/批量补抽（`runAttrsRound`/`runRelationRound`/`buildAttrsPrompt`/`buildRelationPrompt`/`validateRosterDetails`/`applyRosterAttrs`/`refineEntityAttrs` + 两常量）**整条删除**；抽象 prompt 的 `bookEntities` 模板去掉上级/所在/种族/四维属性（只留 name+kind）；界面下掉 4 枚按钮（两枚「补抽」+ 两处「↻ 重新抽取设定」）。**保留**：五件套（力量谱系=查数值的原料）· 世情句 · 名册身份（name+kind=主键）· **照书办**（书标签声明的结构照抄，零模型调用）· 名册折叠（parent→branches、角色终点→organs）· 环境初值 · clear-evolution。**核验**：名册轮 1 次调用（原 1+关系批+属性批）｜名册可选字段残留 0｜照书办改判/照抄上级仍生效｜seed 入账 schema PASS。**顺带修正一处判词失真**：弃关系警告按原因分措辞（"上级是地名" vs "上级不在册"）。**测试 403→393 全绿**（删 11 则旧的、加 6 则新的，含"删除位锁"防无声复活）。台账 L154-L155；下一片=片 2 账本换血。
- **2026-09-09 拍板与实施（第十九棒·全量棋盘 + 每轮镜头）**：悬案实证破案（模块化 ST 无 ctx.character/worldInfo，`characters[0]` 回退取到库首卡 Assistant=根因；取数修正 characterId/群成员+官方 loadWorldInfo，台账 L132-L134）。用户拍板四则：①**棋盘=账本、镜头=每轮视野**——345 名号全量入账，LLM 不必全见（镜头 30k 定案，用户「现在的大模型绝对有这个能力」；主调用 max_tokens 4096→16384 随幅改）②**约束哲学=提示词为主，引擎只守诚实底线**（「代码约束这种边界是约束不完的」）③**不设池顶**（「设定上限会把重要的划掉」；超量→重新抽取重建）④**势力必须抽象**（leg16 原话复证：父势力拆子=席位碎片+分量失真；子势力折叠为分支表+盘算 branch 身份链，升格/降格排期）——细案 `docs/full-roster-lens-spec.md` **全案拍板**（K43-K48 实施完成：seed 无截断+净化折叠+权重预填 / pack 镜头选择器+麾下成员打包 / settle 删超席强制+newEntities parent / 渲染大势行·张力行名实分离+实体页全册·镜头徽·分支麾下 / 观测台镜头口径+全量棋盘 100t 曲线：输入峰 7906/30k、坏账 0），**377/377**。报批 #2/#4 按新值修订（#4 席位 32 废；#2 max_tokens 改 16k）。实机复验待用户（Ctrl+F5 → 「重新抽取」吃新抽取形态）。「项目知识库」列为待办（用户令仅登记）。

## 7. 检验标准（四问，任何决定拿它来量）

1. 这个做法让世界**更像自己会转的棋局**吗？
2. 让**分量更接近客观**吗？
3. 让**棋局更看得见**吗？
4. **换一本书还成立吗？**（§4.8 泛用性三条判据——过不了的，前三条答得再漂亮也不做）

对不上，就不做。

---

*2026-09-07 已拍板。现行下一步：纵向切片细案（`docs/vertical-slice-spec.md`）拍板后进入 S1。*