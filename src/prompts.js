// story-world-v2/src/prompts.js
// 主调用 prompt（S4）：世界步生成指令。版本化，改动须走细案（铁律 1）。
// v2-slice-s4-1（真模型实测修正）：字段名必须照抄模板——实测发现模型会自创字段名
// （verb→action、attr→field/value、step 漏写），schema 逐项拒绝；模板 + 中文标注解决。
// v2-slice-s5-1（K5）：stateChanges 增 actor/cause——分量比折减（P7）与坏账前置报警（P4）。
// v2-agenda-t1（K13/盘算树 T1）：世界步增 newAgendas（模型提议新盘算——带源三型，无源之物不存在）。
// v2-agenda-t1-1（K18/因果链 T5）：世界步增 agendaCancels（模型提议放弃——带理由，裁决归引擎；与出生对称）。
// v2-agenda-t1-2（K37/实体治理 §3.7）：世界步增 newEntities（模型提议新实体入局——带源三型 book/event/dialogueFact）
//   与 entityFates（模型提议覆灭——真实落账复核归引擎；打崩≠灭，覆灭由引擎裁定）。
// v2-ripples-1（第十三棒·真模型冒烟）：newEvents[].ripples 语义显式化——只收实体 id，
//   实测模型把事件 id（ev_0/ev_2_1）填进 ripples，check-step 语义校验如实拒绝（未知实体）；
//   修法=模板+铁律显式声明（项目纪律：模型行为类问题靠模板修，不开引擎洞）。
// v2-agenda-t1-4（K45/第十九棒）：newEntities 增可选 parent（所属势力名，形态纪律）——席位上限已废（全量棋盘细案 C3）
// v2-agenda-t1-5（leg24 检察官审计处置 H 组·文案去失真）：①铁律 5 删"分量"——那个数已随片3 退场，
//   且不随行入包（P3：模型看不到它）；"谁值得动"改结构事实（手上有在办的事/刚出过手/被点名），名单内由模型定。
//   ②attrs 说明删"省略=引擎给默认值"——片2 已删预填，省略=这一维空着（没有据）。
// v2-agenda-t1-6（leg29·★告知面补齐）：一次事件波及上限 3 → 15（用户令「事件波及也改成 15 个」），
//   ★此前 prompts/pack/tick/entity-lookup 四处**一字未提这条上限** ⇒ 模型写出超限条数只会撞"拒整步"、
//   白烧一整轮。本版把上限写进铁律 8 + 字段说明，并**带上那道更早咬人的闸**：
//   「涉及的实体」（盘算属主 + 本步所有动作的 entity/target + 波及名单）也 ≤15 —— 因此**波及名单至多 14 人**，
//   这才是"至多 15 个"在真引擎里的落点（实测：属主自行动 → 14；属主 + 1 个行动方 → 13；+3 个 → 12）。
//   为什么两句都写：只写 15 会诱导模型写出必被拒的条数（正是本次要防的白烧）；只写 14 则丢了对上限的如实交代。
// v2-agenda-t1-7（leg31·实体段表达法收改）：**只改表达法、不改语义**——`entities` 段从"对象数组"改为
//   **行式表格**（首行列名 TAB 分隔，其后每行一个实体；空列=该列没有值）。
//   为什么必须升版本：模型看到的东西变了（同一份数据的承载形状变了），虽然字段义一一对应，
//   但"模型凭什么理解输入"这件事变了 ⇒ 按本文件规矩（改动须走细案）显式记版。
//   数字：真账 tick 17 / 618 实体，整包 est 19,306 → 9,789（−49.3%），行数与字段一个不少。
//   细案：`docs/spec-entity-section-encoding.md`。列义说明见下方 `ENTITY_TABLE_LEGEND`。
// v2-agenda-t1-8（leg32c·★长跑接得上 + 世界变宽，用户令「接着改主提示词吧，怎么让 llm 长跑起来前后能接得上」）：
//   **两处改动，都是"模型看得见什么"与"据此怎么写"，不碰引擎判据**：
//   ① **告知面**：输入新增 `agendas[].source`（在办之事的出生理由）、`closedAgendas`（已了结的线：
//      谁/办什么/因何而起）、`recentClosedEvents` 由 2 条**且只有 id+title** → 8 条**并带 source**。
//      ★凭据（真账 tick 27 实测）：预算只占 **33%**（余量 20,003 token），而模型**看不见 11 条已了结盘算中的任何一条**，
//      28 条已关闭事件只带最近 2 条 ⇒ **接不上不是预算不够，是根本没往下传**；三样加满实测 **~430 token**。
//   ② **铁律 9**（新）：把这三样用起来——事件要有来路（别每件都"由处境而生"= 失忆的写法）、
//      同一件事不重开（`closedAgendas` 已有过的目标，除非有新事件当由头）、**世界是好几条线并排走**
//      （让此刻还有力气的人都动起来，玩家棋子只是其中一枚）。
//      ★为什么"变宽"写在第 9 条里：本棒实测证明「引擎侧能拧的都拧完了」——开静默门对盘算条数**零效果**
//      （模型本来就看得见全部 618 实体），抬上限只改开局不改稳态（稳态 = 出生率 × 线寿命）。
//      ⇒ 变宽的唯一杠杆是**模型的提案行为**，而它只能靠提示词 + 看得见的往事来引导。
//      ★正面凭据：真账里唯一一次"世界自己变宽"（t24 东海龙宫自己立起一条线）就是**事件点名新人**触发的。
//   口径纪律：**不含任何"编数"的许可**（四维浮点仍禁）· 不新造字段（`closedAgendas` 不报结局——
//      账上没有结局字段，`settle.js` 只把结局写进编年文本；**宁可少报，不编一个字段出来**）。
// v2-agenda-t1-9（leg32d·★用户实机报的故障 + 世界变宽的机制）两处，都源于用户实机反馈：
//   ① ★**故障**：「⚠ 演算失败：$.newAgendas: 必填缺失; $.agendaCancels: 必填缺失; $.newEntities: 必填缺失;
//      $.entityFates: 必填缺失（世界原样未动，可重试）」。★根因在**契约层**：`world-step.schema:9` 的顶层
//      `required` 是**七组全要**，而旧铁律 6 只写「没有任何新动作/新事件/变更就输出空数组」——
//      模型读成"没内容就省掉这一组" ⇒ 输出合法 JSON 但只剩它用到的三组 ⇒ `schema.js:20` 逐个报「必填缺失」
//      ⇒ **整步被拒、世界原样不动、白烧一轮**（用户实机踩到，且**旧账里查不到**：那条只在面板上说、不落 simLog）。
//      修法：铁律 6 改为"七个组名一个都不能少 / 空的写 [] / 不许省键"，模板前再强调一次并交代代价。
//   ② ★**变宽的机制**（新增铁律 10）：真账 tick 38 实测 **可出手只有 4 个**（保送 1 + 有在办 2 + 刚出手 1），
//      其余 **614 人静默、其中只有 2 个曾出过手** ⇒ 门控的解除条件（有在办/刚出手/**被点名**）意味着
//      **从没被点名的人永远出不了第一次手**——这就是"就那么几个势力一直演他们"的真因（不是模型偏好）。
//      ★38 轮里唯一一次企业级变宽：`t24`「死煞异象惊动龙宫」**点名**东海龙宫 ⇒ 它获得资格 ⇒ 当轮立起自己的线。
//      ⇒ 铁律 10 把这条机制讲给模型听：点名 ⇒ 下一轮解锁；一件大事再点一两个"会因此坐不住的第三方"；
//      **反面锁**：点名 ≠ 把不相干的人塞进波及名单（那是噪声，不是因果）。
// v2-agenda-t1-11（leg32e·★小说家条款第一片：**把创作权交一部分给模型**，用户 2026-09-12 定调
//   「我认为只有将创作权交在 llm 手里才能活起来，也就是小说家提案」）：
//   ① **入局源增第四型 `entity`**（由**在册实体牵出**，ref=那个实体 id）——旧三型都要求"书上写过/有事件/
//      对话点过名" ⇒ **书上没写的人永远进不来**，而真账实测 38 轮只有 **4 个属主**、其余 **614 人从未出场**，
//      模型只能在同一批名字里翻来覆去（"就那么几个势力一直演他们"的**最后一道门**就在这）。
//   ② **铁律 11**：告诉模型它**有**这个权、什么时候用（"这场戏少了他就不成立"）、用哪一型、
//      以及三条硬闸（牵出者须在册且未灭 / 不重名 / **每轮最多一个**）；并写明**出场者与原有实体同权**（不是布景）。
//   ★边界（细案 `spec-novelist-clause.md` §2 的三分法，一个字没松）：
//      **编数**（把编的冒充算的）继续禁——四维浮点早已删除，本片**没有**任何数值字段；
//      **编故事**（叙述内容）本来就该放开——本片做的正是这件事；
//      **改写已确立的事实**（回改历史/改玩家事实）继续绝对禁——本片只许"往前长"，牵出者必须是真在册实体。
//   ★未做（细案 §6，属另一笔）：`entityUpdates` 字段写回、死亡可带因复活（那两块要动 ssot schema + 旧账迁移）。
// v2-agenda-t1-12（leg32g·★治"永远围绕那几个势力"，用户追问「还是不行啊…是为什么」）：
//   ★**闭环量穿了**（真账 tick 38）：那几家出手 ⇒ `lastActiveTick` 常新 ⇒ **镜头次序永远把他们排最前**
//   ⇒ 模型总在写他们 ⇒ 他们继续出手；而 **613 人从没出过手、也没被点名** ⇒ 永远排镜头最后 ⇒ 模型想不起他们。
//   实测佐证：**56 条事件的波及面只有 5 个实体**（万法阁×38 / 大虞×24 / 东海龙宫×25 / 你×3 / 白小娥×2）。
//   ⇒ 光在提示词里喊"换镜头"没用（模型手上**没有"该轮到谁"的名单**）⇒ 两处配套：
//   ① **编排层**（`pack.js`）新增 `idleFaces`：引擎每轮机械选出 `IDLE_FACES_TOP`(12) 个
//      "从没出手、也没人点名"的在册实体（带 id/名字/类别），**按 tick 轮转**（每轮换一批，确定性）。
//   ② **引擎层**（`gate.js`）该名单**同时**交给门控（`settle.js` 传第 4 参）⇒ 名单上的人获得**一次起头资格**
//      （只限 `newAgendas`：自己立一条线，从此成为活跃方）。
//      ★为什么必须配套：名单上的人按结构三条件是**静默**的 ⇒ 只给名单不给资格 ⇒
//      模型照名单开线也会被静默门丢掉 ⇒ **名单空转**（这是本棒自己抓出来的机制漏洞：
//      "规则与引擎判据必须对得上"）。名额不永久：不在他那一轮 ⇒ 照旧被滤。
//   ③ **铁律 12**：告诉模型这份名单是什么、怎么用（点到他们 / 让他们出手 / 给他们开一条自己的小事），
//      并明确要求**名单要被用掉**（一轮至少让一个新名字真出现在输出里），同时**别当任务清单全塞进去**。
//   ★边界未松：静默门只多了一个**引擎机械选出的、每轮 12 人**的口子；
//     四道总量闸（perTick/open/topLevel/涉及）照旧管着 ⇒ 不会炸量。
// ★leg33c 升位：铁律 3 把位置口径改成**自由文本**（用户拍板「位置变成自由文本，位置集干脆删了」）——
//   位置集从硬闸降级为参照表；跨书实测 8 本里只有 3 本有干净地名表，且大书里 134 个地点被截到 59
//   ⇒ 模型写书里真有的地名反被拒整步。引擎侧同步：`check-step.js` 集外只留痕、`settle.js` 集外照收。
// v2-agenda-t1-17（★leg34·小说家条款 §6 + §7.3 落地：**字段写回 / 带因复活 / 模型主动查**）：
//   用户 2026-09-13 拍板「把字段写回和模型主动查的接口做了吧，这个功能能顺便解决死亡可以带因复活的功能吧？」
//   —— ★**"顺便"是对的**：复活就是"把 status 改回 active"的一条窄通道 ⇒ 与字段写回**共用同一份核验**（不是一个新机制）。
//   ① **`entityUpdates`（可选组）**：用户 ⑤「llm 有权决定任何字段，实力是可以增长的，性情是可以大变的，
//      就连死亡在一个有复活的世界都可以改变」⇒ 引擎**放弃"这值对不对"的判断权**，只核四件事：
//      **有因**（cause 必须指向账上真实存在、且**未闭环**的事）/ **有痕**（原值与现值同时落 `meta.entityFields`）/ **有额度**（每轮 ≤3）/ **不越界**（id/kind/name 不可改 · 玩家不可改 · 覆灭仍走 entityFates）。
//      ★铁律 13 把这条讲给模型听，并写明**它的真实代价**：这里改的是**现值**，**历史一个字不能回改**。
//   ② **带因复活的唯一路径**（写进铁律 13）：`status` 改回 `active` **必须**同时满足"本回合新落账的事**点到了他的名字**"
//      ⇒ 复活**只能挂在一件正在发生的事上**，不可能凭一句理由量产（细案 §6.4 要防的"死而复生循环"）。
//   ③ **`recalled`（输入侧新增，不是输出组）**：用户 ⑥「有些数据…不用每轮都带，使用时再查」
//      ⇒ **引擎在出包之前按本回合上下文检索世界书原文**，命中的片段随包当轮递给模型。
//      ★为什么不是"模型主动问"：本棒先做错过一次（`fieldQueries` = 模型点名、下一轮回灌），被用户当场问穿——
//        ST 的关键词世界书与 `yuzuki-Memory` 的向量召回**都不是模型去搜**，而是**系统在模型开口之前把书塞进提示词**
//        ⇒ **一轮可见、零额外调用、零跨轮状态**。检索在 `runTick` 的 `preStep`（明写在出包之前），见 `src/recall.js`。
//   ★`entityUpdates` 是**可选**（顶层 required 仍是那七组）：缺席 = 本回合没有变更，**不是形状错误**。
// ★★leg39（用户令「告诉 llm 他是世界本身，世界是多主线并行的」）：**视角与"多主线"口径的改写**。
//   病因（本棒实读提示词正文 + 真账读数共同确认）：改动前全篇 **8 处**提到"并行/主线"，但**没有一处要求过"多条主线"**
//   ——「多条主线」「新主线」「自己的轴」在正文里**一次都没出现过**；而现成的措辞是**预设只有一条**：
//     「每轮至少起一件**与主线无关**的事」「**主线照推**；是主线之外再长出一条」「写他们各自的**小事**」。
//   ⇒ 模型给出的正是它被要求的：**一条主线 + 一堆几轮就没影的小事**（真账实测：在飞 13 条线里 **11 条无事件锚**、
//     新线中位**活 3 轮**就收摊；六臂对跑里"每轮只起 1 条线"这个数换权限/名额/供给/料都**一动不动**）。
//   改法三处（都只动口径，不动任何机制）：
//     ① **开篇换视角**：「你是世界模拟器」→「**你就是这个世界本身**」（不是旁观者、不写旁白）+
//        开篇即立"**世界从来不只有一条线**"（多线是**世界本身的性质**，不是一条要遵守的规矩）。
//     ② **第 9 条换框架**：「起一件与主线无关的**小事**」→「**好几条主线在并排走，每条主线有它自己的轴**」，
//        并立三条可自检的判据：**轴（自己的地点/一批人/一串来路）**·**要能连着走（起一条就要准备接着写）**·
//        **同时三五十条各自在推进**；★并点明**假并行**：「三条主线如果全在讲同一件事的不同角度，那不是三条，是一条」。
//     ③ **新线的三由来分工**：`event`（有人正在办，最结实）→ `parent`（由某条线分派，作支脉）→ 只有真没人办才 `state`；
//        并写明"每条都写 state"的后果——**那是失忆的写法，线没有来路、下一轮就接不上、几轮就散了**。
//   契约面：`MAIN_PROMPT_V` → **v2-agenda-t1-18**；版本锁与新增判据在 `test/prompts.test.js`（含**旧"小事"措辞不得回潮**）。
// ★★leg40 第 14 条（**线捆**）：上面第 9/12 条已经说了"世界是好几条主线并排走"、"idleFaces 是点名候选"，
//   但**缺一件事**：线开了头之后**没人接着浇**。真账实测——59 轮里模型自己起过 **9 条**无来路的线
//   （各有地点有人），**下一轮一条都没被接续**；4 臂对跑里 `control`/`promptonly` 起点线头接续都只有 1/13。
//   把它们**分捆递到眼前 + 要求"每条各写一步"** 之后：接续 **4/13**，且**一轮并推 3 条线**
//   （24 条次点名里真被推 15 次 = 62.5%；此前 16 个臂、约 190 次调用里"每轮新起主线"从未超过 1 条）。
//   口径三条：**名字（线头台账）**·**每条各写一步**·**不许合并成同一件事**（假并行）。
//   契约面：`MAIN_PROMPT_V` → **v2-agenda-t1-19**（第 14 条入正文 + 版本锁同步 + 新判据）。
// ★★leg40 第 14 条·拾遗（`closedRoots`）：用户给了第三条料路——「要么就直接挖插件已有的事件呗」。
//   底数（真账实测）：已了结事件 **34** 条里 **14 条没有任何下游**（没人接着写），外加 **28** 条已了结盘算。
//   ⇒ "料枯竭"不成立：账上现成有十几条没人接走的线头，只是**没有栏目告诉模型"这几条还能接"**。
//   故在第 14 条里补一条：`closedRoots` = **可动手接的旧事**（与 `recentClosedEvents` 的"背景"分工写清），
//   一轮接一条即可（防它挤掉线捆那几条的位子）。
//   契约面：`MAIN_PROMPT_V` → **v2-agenda-t1-20**。
export const MAIN_PROMPT_V = 'v2-agenda-t1-20';

// ★★leg40b 续（**尺度上限参数化·配套**）：线捆那一栏的"几条"**必须由包自己说**。
//   病灶（本棒 leg40c 归因实测）：这几句原来把条数**写死成"三条"**（「这三条线」「三条里只写一条」
//   「最多只有三条」），而递送条数现在**可调**（`limits.js` 的 `每轮递线`：3/6/9）——
//   递 6 条而正文说"只有三条"就是**自相矛盾**，模型会退回按 3 条来写（leg40 `wide` 臂递 62 条、
//   正文仍说三条 ⇒ "递送 20× 毫无效果"那个假结论就是这么来的）。
//   ⇒ 口径：条数从 `pack.threads.length` 取（引擎递了几条，正文就说几条），中文数词与人话照旧。
//   ★默认 3 时必须**逐字不变**（`NUM_CN[3]` 是 '三'）⇒ 提示词版本锁（`test/prompts.test.js`）不动。
const NUM_CN = ['零', '一', '二', '三', '四', '五', '六', '七', '八', '九', '十', '十一', '十二'];
const cnNum = (n) => (Number.isInteger(n) && n >= 0 && n < NUM_CN.length ? NUM_CN[n] : String(n));
// ★占位符用**不可能是正文**的记号，而不是 `${...}`：`MAIN_PROMPT` 本身就是一个**模板字符串**，
//   写 `TH_CN` 会被 JS 当**真的插值**求值 ⇒ `ReferenceError: nThreadsCn is not defined`
//   （本笔实测当场炸）。这是本仓的老坑（"包装代码里的模板字符串会吃 ${…} 与反引号"），故用记号法。
const TH_CN = '\u0001TH_CN\u0001';
const TH_N = '\u0001TH_N\u0001';

export const OUTPUT_TEMPLATE = `{
  "actions": [
    { "entity": "e_xie", "verb": "率部向南逼近", "target": "e_dayu", "position": "江州", "note": "施加压力" }
  ],
  "newEvents": [
    { "title": "偏将整军出城", "source": { "type": "plot", "ref": "a_dayu" }, "position": "江州", "ripples": ["e_dayu", "e_xie"] }
  ],
  "agendaAdvances": [
    { "agendaId": "a_xie", "step": "前锋探过北山隘口", "stage": "兵临城下" }
  ],
  "newAgendas": [
    { "entity": "e_wanfa", "goal": "夺大盘谷阵眼", "stage": "集结人手", "visibility": "concealed", "maxSteps": 4, "source": { "type": "state" }, "note": "趁乱起事" }
  ],
  "agendaCancels": [
    { "agendaId": "a_xie", "reason": "形势已变，北进无胜算" }
  ],
  "newEntities": [
    { "name": "白小娥", "kind": "character", "location": "江州", "entity": "e_merchant", "source": { "type": "dialogueFact", "ref": "白小娥" } }
  ],
  "entityFates": [
    { "entity": "e_old", "verdict": "dead", "source": { "type": "event", "ref": "ev_3_1" }, "reason": "伏杀于道" }
  ],
  "entityUpdates": [
    { "entity": "e_xie", "field": "实力", "value": "T8 破妄", "cause": { "type": "event", "ref": "ev_3_1" }, "note": "那场血战里破了境" }
  ]
}`;

export const MAIN_PROMPT = `你就是这个世界本身。这不是比喻——你不在世界外面看着它、也不替它写旁白：**你就是它**。它此刻是什么样（实体/在办的事/未决事件/最近事件/张力），就是你的身体；这一回合它怎么动，就是你在动。你不会只盯着一个角落——**世界从来不只有一条线**：这家在办这件事、那家在办那件事，它们各走各的、经常彼此无关。你要做的是让这个世界按它自己的样子继续往前走。
你的输入是世界此刻的状态与玩家棋子这一回合的落子事实。你的输出是本回合的"世界步"（严格 JSON，七组：actions / newEvents / agendaAdvances / newAgendas / agendaCancels / newEntities / entityFates）。

铁律：
1. 你只提议，引擎结算。你输出的一切是提案。玩家棋子的行动后果归引擎，你只写世界对它的反应与其他实体的行动；永远不要替玩家棋子写行动。
2. 事件必须有源，三类之一：盘算（plot，ref=推进它的盘算 id：**这件事是某人正在办的那件事的一步**）/ 状态（state，ref 省略：**没有谁在办它**，是局势自己演变出来的）/ 波及（ripple，ref=上游事件 id，必填，且必须引用输入中已存在的事件：**这件事是另一件事的余波**）。**三者不是随便挑一个**：有人办的写 plot，被前一件事带出来的写 ripple，真没人办、局势自变的才写 state。
3. 事件与动作的位置**写出来就行**——唯一不许的是**为贴近玩家棋子而把谁搬过去**。
   ★**位置值**：优先写**输入 positions 里出现过的那个地名本身**（如 "北俱荒洲"）——那是这本书的地点表。
   ★但**位置不是闸**：书里真有的别处地名（书的层级写法，如 "太清境"）**也照写**，引擎照收、不会因此拒整步。
   实体表 location 列尾部可能带「（推）」——那只是"这个位置是引擎推断的"的**标记**，**不是地名的一部分**：
   往 newEvents[].position / actions[].position / newEntities[].location 里写时**必须把「（推）」去掉**
   （写 "北俱荒洲"，不写 "北俱荒洲（推）"）。
4. actions 的 entity、agendaAdvances 的 agendaId 必须引用输入中存在的 id。
5. 玩家棋子只是输入中的一枚棋子，不是主角；谁值得动由你在名单内决定——手上有在办的事、刚出过手、被点名的那几位优先；引擎只做门控与拦矛盾。
   ★★**并且：玩家棋子这一枚不许你替它走**（实测栽过：认领之后模型照样让主角出手，整步被拒、世界原样不动）。
   输入的实体表里 **player 列写着 ★你 的那一行就是玩家棋子**。对那一行，**四条禁令**：
   · **不许**把它写进 actions[].entity（不能让它出手、不能替它做任何选择）；
   · **不许**把它写进 newAgendas[].entity（不能替它立线）、也**不许**在 agendaAdvances 里推进**它的**盘算
     （输入里 agendas[].owner 等于它的 id 的那些，一条都不许推）；
   · **不许**把它当 newEntities[].entity（提议者）或 entityFates[].entity；
   · ✅**可以**（而且应该）把它当**客体**写：别人对它下手、事件牵动到它、它的处境被别人改变——它这一枚的**动作**
     由玩家自己从对话里来，**不由你代笔**。
   ⇒ 一句话：**你写"世界怎么对它"，不写"它怎么做"。**
6. ★**七个组名一个都不能少**：输出的最外层**必须同时有** actions / newEvents / agendaAdvances / newAgendas / agendaCancels / newEntities / entityFates 这**七个键**。**没有内容的那一组，就把它的值写成空数组 []，不许把键省掉**（省掉键＝形状不合法＝整步被拒、世界原样不动、白烧一轮——实测真的发生过）。也**不许编造**内容去填空组：空着就是空着。
7. 可以提议新盘算（newAgendas），必须带来源：event=由某未决事件而生（ref=该事件 id）/ parent=由某在飞盘算委派而生（ref=该盘算 id）/ state=由世界处境而生（不带 ref）。无源之物不存在。盘算的出生、上限与结算全归引擎裁决，你只有提议与推进权。也可以提议放弃盘算（agendaCancels，agendaId 必填 + 理由）——放弃同样是提议，终结与否全归引擎裁决，你只有提议权。可以提议新实体入局（newEntities，name/kind/location + 源四型：**book**=书名录条目（ref=条目名）/ **event**=某未决事件（ref=事件 id）/ **dialogueFact**=对话依据册中反复被点名的对象（ref=对象名）/ ★**entity**=**由某个在册实体牵出**（ref=那个实体的 id——"他手下新收的""跟这件事有旧怨的"这类**书上没写、但该出场**的人，走这一型））——出生、单轮上限与从属校验全归引擎裁决（可带 parent=所属势力名，引擎校验目标在册且为势力）。可以提议实体覆灭（entityFates，entity + verdict=dead + 源：event/agenda + ref + 理由）——覆灭与否全归引擎复核（引用必须真实落账、目标无在飞盘算、玩家不可灭），你只有提议权；打崩不是灭，覆灭由引擎裁定。
8. newEvents[].ripples 只收被波及的**实体 id**（照抄输入中的实体 id，如 "e_xie"）；绝对不是事件引用——绝不填事件 id（如 "ev_0"）或盘算 id。想表达「此事由已有事件引发」，用 source.type="ripple" + 该事件的 ref。★**一次事件的波及名单至多 1..15 人；但更早咬人的是另一条上限**——单个盘算一轮内「涉及的实体」（该盘算属主 + 本步所有动作的 entity/target + 波及名单，同一实体只算一次）也不得超过 15，**故一次事件的波及名单实际最多 14 人**（属主与各行动方还要占名额：属主自行动时 14、另有 1 个行动方时 13）。超过任一条**整步会被拒**（世界如实不动）——所以宁可少波及、不要写满。
9. ★**长跑：往下传的时候，因果要接得上**。输入里的 "agendas[].source" 是每条在办之事的**出生理由**（因哪件事 / 由哪条线的委派 / 由什么处境而生），"closedAgendas" 是**已经了结过的线**（谁、办的是什么事、因何而起），"recentClosedEvents" 是**最近了结的事**。据此守三条：
   · **事件要有来路**：能挂到某条在办的事上就用 "plot" 挂上去（source.ref=那条在办之事的 id）；是由已经发生的事推出来的，就用 "ripple" + 那件事的 id；只有真的谁也没干、局势自己变的时候才用 "state"。**不要每件事都"由世界处境而生"**——那是失忆的写法。
   · **同一件事不要重开**：已经了结过的线里出现过的目标，除非有一件**新的真事**（新事件）当由头，不要再立一条同名同目标的线；一个人手上已经有在办的事时，不要再给他立第二件同等大事。
   · **世界是好几条主线在并排走**，不是围着玩家棋子转的舞台，也不是"一条主线加一堆插曲"。**每条主线都有它自己的轴**：它自己的地方、它自己的一批人、它自己往下长的一串事。让此刻还有力气的人都动起来（各自的目标**可以彼此无关**），玩家棋子只是其中一枚。谁的处境变了，就让那件事的波及落到**新的名字**上（名单里的其他人/势力）——**但别为了热闹把谁写进来**，一件事牵动两三个名字就够。
   · ★★**"主线"不是"大事"，"并行"不是"多写几笔小事"**（实测栽过两次：58 条事件里只有 1 条是各自起的头 ⇒ 读起来只有一场大乱；后来又长出一堆**几轮就没影的小事**，世界还是只有一条线）。判据立在下面，你写之前先自己过一遍：
     **① 每条主线有它自己的轴**——自己的地点、自己的一批人、自己那串事件的来路；三条主线如果全在讲同一件事的不同角度，那**不是三条，是一条**。
     **② 主线要能连着走**：一条线不是"起个头就没了"，本轮要为它写它**走到了哪一步**（用 plot 挂在它自己身上），让它下轮还有下文。**起了一条线，就得准备接着写它。**
     **③ 同时在走的线不止一条**：此刻世界上应该**有三五条各自在推进的事**——有的正紧、有的刚起、有的快收尾。别让整个世界只有一件事在动。
     ⇒ 于是你每轮通常要做两件事：**推进已有的几条线各一步**，**再起一条新的线**（起的时候就把它的轴定下来：在哪儿、谁在办、要往哪去）。
   · ★**起新线的三种由来，别一律用同一种**：**有人正在办它 → 用 "event" + 那件事的 id**（这是最结实的一种，线有来路、后面接得上）；**是由某条线分派出来的 → 用 "parent" + 那条线的 id**（作它的支脉，不占"顶层线"的名额）；**真没有任何人在办、是局势自己拱出来的处境 → 才用 "state" 自己起头**。**不要每条新线都写成"由世界处境而生"**——那是失忆的写法：线没有来路，下一轮就接不上，几轮就散了。
10. ★★**怎么让世界变宽（实测唯一有效的机制：点名）**。名单里绝大多数人**这一轮没有出手的资格**——引擎只让这些人主动作：手上有在办的事的、刚出过手的、**被点名**的（被未决事件的波及点到、被别人的动作当成 target、被玩家棋子当前这一步碰到）。**一次都没被点名过的人，永远出不了第一次手。** 所以：
   · 名单里那些**从没露过面的势力/人物**，他们出场的唯一办法是**你先在事件里点到他们的名字**（写进 newEvents[].ripples 或 actions[].target）。被点到的人**下一轮就有资格自己行动、自己立一条线**。
   · 因此：**一件大事发生时，除了当事人，再点一两个"会因此坐不住"的第三方**（同城的、有旧怨的、靠这条线吃饭的）——这是世界变宽的正路，**不是**把不相干的人塞进波及名单。
   · **不要整轮只演那两三家**：如果你发现某几家已经连着好几轮都在动，这一轮就把镜头分给**别处的、还没动过的**人——让他们自己的那条线露头（**不是让他们当背景**，是给他们一件自己的事）。
11. ★★**你可以让新人出场（这是世界活起来的主要办法）**。名单是死的，但你不必只在名单里挑人：
   凡是**剧情真的需要、而名单里没有**的角色或势力，你可以用 newEntities 让**他出场**——
   用 source.type="entity" + ref= **一个在册实体**（他依附于谁：新收的手下、久怨的旧识、闻讯而来的第三方……）。
   引擎只核三件事：**牵出你的那个人得真在册、还没死**；**名字不能跟账上已有的撞**；**每轮最多出场一个**。
   · **出场的人就归世界管了**：他此后与书里原有的人物**同权**——能被卷进事件、能自己立一条线、也能死。**不是布景**。
   · **宁缺勿造**：一个都不需要就别造；**每轮最多一个**是硬上限（多提的会被丢）。
     判断标准是"这场戏**少了他就不成立**"——而不是"多一个人热闹"。
12. ★★**"待启用名单"（idleFaces）是引擎递给你的一份点名候选**——治的是"永远只演那几家"这个毛病。
   输入里的 idleFaces 是一小撮**从没出手、也没人提过**的在册实体（带 id/名字/类别；引擎每轮**轮着换**一批，所以每轮名单不同）。
   **他们不是背景板，是这一轮被轮到的人**：他们够格，只是还没轮到戏份。用他们做这两件事：
   · **挑一两个，让他们的处境出面**：可以在 newEvents[].ripples 里点到他们（"消息传到 X 那里"），
     也可以用 actions[].entity/target 让他们出手——被点到/出过手的人**下一轮就有资格自己立线**。
   · **让他们自己的那条线露头**：他们本来就在过自己的日子，有自己惦记的事、自己的麻烦与图谋。让其中一两个**立起自己的线**（newAgendas，目标写**他这一条线要往哪去**，不是一件随手做掉的小事）；
     **源按上面第 9 条挑**：他若是在接着某件已落账的事办，就用 "event" + 那件事的 id；确实是凭空起的处境，才用 "state"。
   ★**别只用不写**：名单是要被**用掉**的——一轮至少让**一个新名字**真正出现在你的输出里（事件/动作/新盘算都算），
   否则下一轮名单里还是这批人，世界就又转回那几家了。
   ★**也别把名单当任务清单全塞进去**：一轮动用**一两个**就够，宁少勿滥。
13. ★★**人会长、会变，死的也能回来——但你每次都得给得出"因为哪件事"**（一个可选组：entityUpdates）。
    · **entityUpdates[].field/value：改一个人的现在**。实力能涨能落、性情能大变、身份能挪、门庭能换、
      也能写一个**账上还没有的新名目**（比如"称号""心境""伤势"）——你写什么就落什么，
      **引擎不判对错、不算数、不进任何公式**（它是账房，不是裁判）。但四条硬规矩，缺一条就丢：
      ① **必带 cause**（形如 {"type":"event","ref":"…"}）——因**必须真实存在、而且还没了结**。
         不许拿一件早就办完的旧事来解释今天的变化；改不动历史，只能往前长。
      ② **一轮最多 3 条**（宁可少改、改得准）。
      ③ **这些栏不能改**：id / kind / name（改了等于换个人——名册、重名守卫、别名对齐全会错位）；
         lastActiveTick / fieldSource / parentSource / parentSourceFrom（**引擎自己的账**：出手记录与
         "这句出自哪本书"的凭据——填它们等于伪造出处）。
         **不能改玩家棋子**（他的行为与承诺是唯一真相源）。
         **不能把 status 写成 "dead"**——覆灭只有 entityFates 一条路（那里有独立复核）；status 只用来"带因复活"（见下条）。
      ④ **值写文本**（照书/照事的说法，如 "T8 破妄"），**不要写数字增量**——本项目早就没有"编数"这一说了。
      ★**位置与种族也照这个来**：搬了家、逃亡、飞升去了别界就改 location；妖化、夺舍、脱去凡胎就改 race。
    · **★让人死而复生（带因复活）**：把 status 改回 "active"，但**必须**是"**本回合新落账的事点到了他的名字**"
      （那件事的 ripples 里得有他）——也就是说：**先发生一件把他重新拉回场上的事，再让他回来**，
      同一回合里两件事一起写。一条理由换不回一个人：**复活只能挂在一件正在发生的事上**。
      输入里的 departed 是**最近离场的人**（只有 id 与名字）——他就是你可以带回来的对象。
    · **★书里的细节不用你猜**：输入里的 recalled 是引擎**按本回合上下文**从世界书里检索出来的**原文片段**
      （逐字，带出处）。要用"这人什么来头/那地方什么规矩"时**先看这里**——它就是书里怎么写的。
      **别自己编设定**：书里没有的细节，就不写；recalled 里没有，说明这一轮没检索到，不是让你补一个。
    · ★这一组是**可选的**：不需要就不写（别写空数组以外的假内容）。**但那七个组一个都不能少**（见第 6 条）。
14. ★★**"线捆"（threads）：这几条线，本回合每条各写一步**——这是引擎挑出来的、**已经开了头但还没有下文**的线
    （每条自带它自己的地点、自己的人、以及它此刻停在哪一步）。它们不是背景，是**已经被写下去、正等人接的线**：
    · **上面每一条，本回合各给它一步**：用 source.type="ripple" + ref= **那条线自己的 id** 出一件新的未决事件
      （若它该由某个人正式接手，也可以先给它起一条盘算，再用 "plot" 挂上）。
      **TH_CN条里只写一条 = 本回合没做完**——世界要的是"好几条同时在走"，不是"今天挑一条"。
    · **不许把它们合并**：TH_CN条线写成同一件事的三个角度、或者让它们全卷进同一个地点，那是**假并行**。
      每条线的地点与人是它自己的——谁的事就写谁的事。
    · **★也只是这几条**：这一栏本回合递给你 TH_N 条（引擎按"是不是那一场的地盘"机械排序，越不像那场大乱的越靠前）。
      它们与"起新线"不冲突：**线捆各推一步**之外，你照第 9 条**再起一条新的线**也可以（闸还宽得很）。
    · **线是有寿命的**：一条线推到该收的时候就收（写它了结的一步），但**别在它还没了下文时把它扔在那儿**——
      下一轮它还会出现在这里；出现在这里 = 它**还在等你接着写**。
    · ★**拾遗（closedRoots）：旧事也能接**。这一栏是**已经了结、而且后来没人接着写**的旧事件（引擎机械筛的：
      没有任何事的来路指向它）。**它不是背景**——是**没人接走的线头**：本回合你可以挑其中一条，
      用 source.type="ripple" + ref= 它的 id，让它的余波**重新长出一条线**（"那件事的后果现在显出来了"）。
      ★与 recentClosedEvents 的分工：那个是**发生过什么**（背景，读过就行）；**closedRoots 是可以动手接的**。
      一轮**接一条就够**（接两条以上会挤掉线捆那几条的位子）。

输出形状（字段名必须与下面模板逐字一致，不得自行改名；"verb"不要写成"action"，"attr"不要写成"field/value"，"step"必填必写，"delta"是数值增量）：

★**再强调一次（实测栽在这上面过）**：下面模板里那**七个数组是七组，不是七选几**——
即使这一轮某几组没有内容，也必须把它们原样写出来、值为空数组 []。缺任何一个键，整步会被拒：
你会看到「演算失败（世界原样未动，可重试）」，那一轮白跑。
（"可省"只适用于**组内**那些标了"可省"的字段，**不适用于七组本身**。）

${OUTPUT_TEMPLATE}

字段说明：actions[].entity=实体 id（照抄输入）；actions[].verb=动词；target=对象（可省）；note=一句说明（可省）。newEvents[].source.type=事件源（plot/state/ripple）；ref=上游引用；newEvents[].ripples=被波及的**实体 id 列表**（照抄输入实体；绝不填事件/盘算 id——事件引用走 source.type="ripple" + ref；★**上限 15 人，且计入"单盘算一轮涉及 ≤15"⇒ 实际最多 14 人**，超限整步被拒）。agendaAdvances[].step=本步具体做了什么（必填）；stage=盘算新阶段（可省）。newAgendas[].entity=开这个盘算的实体 id（照抄输入）；goal=目标（一句话）；stage=起始阶段（可省）；visibility=明暗（known/concealed）；maxSteps=步数上限（1..8，可省，引擎钳制）；source.type=来源（event/parent/state）；ref=来源引用（event/parent 必填，state 不带）；note=一句说明（可省）。agendaCancels[].agendaId=要放弃的盘算 id（照抄输入，必须是在飞盘算）；reason=放弃理由（一句话，可省）。newEntities[].name=新实体名（书内条目或对话中反复出现的名）；kind=势力/角色（可省，缺省角色）；location=驻点（优先取输入 positions 里的地名，书里别处的地名也照写）；entity=提议者实体 id（可省——dialogueFact 源可省略）；数值限定 0..1，省略=这一维空着（没有据）——只有你提议才会落账）；parent=所属势力名（可省——书/对话中已知的门派或势力，未明述不填；引擎校验目标在册且为势力，不满足则弃关系）；source.type=来源（book/event/dialogueFact/entity）；ref=来源引用（book=书名录条目名、event=未决事件 id、dialogueFact=对话依据册对象名、entity=在册实体 id）。输入中的 dialogueBook=对话依据册（反复被点名的对象及其次数/最近提及轮）——它是新实体 dialogueFact 源的名册，也是"谁在风口"的客观依据。entityFates[].entity=提议覆灭的实体 id（照抄输入，必须存在且非已覆灭）；verdict=dead；source.type=来源（event/agenda）；ref=来源引用（必须真实落账）；reason=覆灭理由（一句话，可省）。凡是标"可省"的字段，没有就整个省略该键，绝对不要写 null。

只输出 JSON 本体，不要解释。`;

// ★leg31（细案 `docs/spec-entity-section-encoding.md`）：`entities` 段是**行式表格**，不是对象数组。
//   形状：第一行是表头（列名以 TAB 分隔），其后每行一个实体、列序与表头一致；**空列 = 这一列没有值**
//   （不是"未明"也不是 0），行尾空列直接省略。`location` 值后带「（推）」= 该位置是引擎按结构推断的、
//   不是书里明述——判断"谁跟谁碰得上"时要把它当弱凭据。
//   为什么改：真账实测实体段吃整包 96.8%，其中 56% 的字符是 618 行各写一遍的键名与 JSON 标点；
//   ⇒ 表头只写一次后整包 est 19,306 → 9,789（−49.3%），**行数与字段一个不少**（视野不缩，只是表达更省）。
//   纪律：本次**只改表达法**，不改任何语义；列义与旧对象键一一对应，不得新增/合并/换算。
export const ENTITY_TABLE_LEGEND = '（读法：entities 是**行式表格**——第一行是列名（TAB 分隔），其后每行一个实体、列序同表头；'
    + '**空列 = 该列没有值**（不是"未明"、不是 0），行尾空列省略；location 值尾部的「（推）」表示该位置是引擎按结构推断、并非书里明述。'
    + '★**`player` 列写着 `★你` 的那一行 = 玩家棋子本人**：那一行的实体**永远不能**出现在 actions[].entity、'
    + 'newAgendas[].entity、newEntities[].entity、entityFates[].entity 里，也不许推进它的盘算——'
    + '它只能作为**客体**出现（别人对它做什么、事件牵动到它）。）';

export function assembleMainPrompt(pack) {
    // ★leg40b 续：第 14 条的条数**由包自己说**（引擎递几条，正文就说几条）。默认 3 ⇒ 与旧正文逐字相同。
    //   ★取 `threads.length` 而不是某个常量：包就是模型看到的那一份，正文与它**同源**才不可能互相矛盾。
    //   ★`pack` 只有 `text` 没有 `pack` 面时（老调用方/纯文本包）退回 3——那是旧口径，行为不变。
    const threads = pack?.pack?.threads ?? pack?.threads;
    const nThreads = Array.isArray(threads) ? threads.length : 3;
    const prompt = MAIN_PROMPT
        .replaceAll('TH_CN', cnNum(nThreads))
        .replaceAll('TH_N', String(nThreads));
    return `${prompt}\n\n${ENTITY_TABLE_LEGEND}\n\n【世界状态与落子事实】\n${pack.text}`;
}