// story-world-v2/src/render-base.js
// ★★★leg85（丙案 · `src/render.js` 的**第一个切口**）：渲染层的**共用底**从 `src/render.js` 搬到这里。
//
// ── 为什么切这一块（底数是量出来的，不是"看着像"）─────────────────────────────
//   本棒把 `src/render.js`（2244 行 / 80 个顶层声明）按语义切成 7 段并实测**段间引用**，
//   结果只有这一段是**叶子**：**零跨段反向引用**（它谁也不用），而**其余六段全都用它**
//   （②③④⑤⑥⑦ 都取 `escapeHtml`；②④ 还取 `PANEL_BUILD`/`LABELS`）。
//   ⇒ 它是全文件唯一"切出去不会拽动任何别的段"的一块，也是唯一"切完立刻见效"的一块：
//     消费者从此 import 的是一个 113 行的叶子，而不是一个 2244 行的抽取器旁的渲染器。
//   ★探针与底数：`F:/deepseek/tmp/leg85-render-coupling.mjs`（只读 · 段间引用矩阵）。
//
// ── 边界（逐字节搬来，逻辑一字未改；本笔只动"它住哪" + "外面怎么够到它"）──────────
//   **11 个顶层声明**（全部是 `export` 口）：`PANEL_BUILD` · `LABELS` · `CHRONICLE_FILTERS` ·
//   `BLACKLIST` · `escapeHtml` · `attrText` · `fmtTick` · `fmtPct` · `fmtLimitNum` ·
//   `entityLabel` · `kindLabel`。
//   ★★★本笔当场量出来的边界（第一版划错了，如实留档）：`msIdTick` / `tideLabel` / `dotSteps`
//     这三个**没有搬**——它们虽然也住在旧家那一段里，但**只在本文件各段之间流通**
//     （探针实测：`msIdTick` 被 ②③ 用、`tideLabel` 被 ②⑤ 用、`dotSteps` 被 ② 用）。
//     ⇒ 口径：**搬"别处也会用的公有口"，不搬"只在那一个文件内部流通的私有口"**。
//       第一版把三个私有口也搬了过来 ⇒ 旧家 import 时当场 `SyntaxError: does not provide an
//       export named 'dotSteps'` ⇒ **整个 `web/index.js` 加载失败**（十几条测试连带全红）。
//       ★这正是本仓"一个定义只有一个家"那条铁律的反面教材：新家留一份同名副本 = 两份真相。
//
// ── 归属理由（为什么"共用底"该独立成家）────────────────────────────────────
//   这十四样是**渲染层的词汇表与格式规范**（引擎词 → 玩家词的对照、HTML 转义、轮次/百分比/
//   上限的印法、黑名单）。它们此前住在 2244 行文件的第 265–369 行 ——
//   于是"改一个转义字符"要先穿过 260 行构建号留档、"改一个格式"要先找到那一小段。
//   ⇒ 搬出来之后：**格式与词表的改动只有这一个文件管**。
//
// ── 依赖方向（单向，叶子）────────────────────────────────────────────────
//   `src/render.js → src/render-base.js`（六个段全指向它）· `web/index.js` · `web/param-panel.js`
//   ★本文件**不许** import `src/render.js`（那会成环），也**不许**反向 import `web/` 任何东西。
//   ★本仓明令**不搞 re-export**（leg71 立的规矩）：所以旧家 `src/render.js` **不再**转出这些符号，
//     消费者一律**改指向**（这一笔改了 5 个文件 8 处 import —— 少一处就当场加载失败，躲不掉）。
//
// ── 判据（本笔不新增"它住在哪"的空锁，而是靠**既有锁自动咬合**）──────────────────
//   ① `test/render.test.js`（命令式地锁 `blacklistOverPlayers` / `BLACKLIST` 的**内容**与
//      `PANEL_BUILD` 的**形状+禁词**）—— 搬动若改了一个字节，它当场红；
//   ② `test/snapshot.test.js` / `test/chronicle-page.test.js` / `test/retired-controls.test.js`
//      等**真跑 `renderAll`** 的用例 —— 渲染产物若变一个字节，它们当场红；
//   ③ `node demo/smoke-demo.js` 的**终态 8231 字节**（本仓"可见面零变化"的硬读数）。
//   ★本笔**零新增判据文件**：这一刀的全部风险都在"搬错一个字节"，而上面三条本来就咬得比
//     任何新写的结构锁更紧（照 leg73 那条纪律：**"搬走了"的记号必须只属于这一族**）。
//
// 下述注释**随各自的声明一起搬来**（它们解释的是"为什么长这样"，与本文件无关的那部分留在旧家）。
// ★★★leg87 升位（用户令「A 删了 C 也删了」+「这个也有问题，改也改不了是死的」）：
//   玩家可见面**真的变了**——设置页少了一整张「你的开档描述」卡、那格只读的「单轮演算上限」
//   变成两个**可填输入框**、世界设定卡的抽取说明改成了实抽的六类（旧文案是 leg60 前的化石）。
// ★★★leg89 升位（用户令「注入提示词让聊天llm生产带标签的内容然后插件提取」+「标签点了名的角色
//   插件不得出手，告诉插件llm就好了」+「应该还有流逝的时间」→「累加吧」+「角色的行动全都注入」）：
//   设置页**多了一张「与聊天模型的接线」卡**（三开关 + 一个可填数字框 + 注入读数）——
//   这是插件**第一次会动玩家的对话**（发消息前往上下文里塞两段），所以这一格的升位必须留痕。
// ★★★leg90（用户实机第二次骂，两句）：①「**这注入的是啥，直观吗？？？**」⇒ ③段原来把**整条编年**拼进注入，
//   而编年里混着**引擎记账行**（「盘算「X」满步结算：结清（期满收摊，终结产果 §4.4④）」这种）；
//   ②「**换成大白话**」+「万魔之祖完成初步接引，聊天llm怎么知道是接引谁」⇒ 不搬账本腔、改用事件本体重述，
//   并让世界模型写标题时**自带对象**（第 8b 条）。
//   ⇒ 这两件都改了**模型看到的东西**（注入正文 + 主提示词）⇒ 按本仓规矩（leg29 立）**必须升位**。
// ★★★leg90 再升一格（用户真机取证后的第二刀）：真机读数 `sw2_tags | 475字 | 锚点=true`
//   ⇒ **注入有效、模型就是不写**（问题在措辞软：原来写「**请**用标签标出」+「不要为了标签改变你的文风」）。
//   ⇒ 那段规范改成**硬要求**（必须 + 不写的后果 + 写完自检 + 合格判据），475 → 878 字。
// ★★★leg90c 真因修（用户：「**上下文我好像都没看见注入**」——这一句才是对的）：
//   上面那一刀（措辞硬化）**打偏了**：真正的病是**注入位置**。leg89 拍的是 `IN_CHAT`(=1)，
//   而 ST 的 chat completion 组装（`public/scripts/openai.js:1345`）写着
//   `if (![BEFORE_PROMPT, IN_PROMPT].includes(prompt.position)) continue;`
//   ⇒ **`IN_CHAT` 的扩展提示词根本不进 prompt**（只写在 ST 的字典里，所以读数"看着注入了"）。
//   位置改成 `IN_PROMPT`(0)（`getPromptPosition` 映射成 `'end'`，作为 system 消息排末尾）。
//   ★又一次"读数对了但方向错了"：leg89/leg90b 的判据只锁"接口被调了、内容非空"——
//     那**不等于**"进了发给模型的 prompt"。现在锁的是**位置值本身**（且有咬合演练证明它会红）。
//   ★起名纪律（本条**被两条判据各咬一次**，都留档）：`test/param-hub.test.js:414` 与
//     `test/render.test.js:1912` 都要求构建号合 **`/^leg\d+-/`** —— 注意是 **`\d+`（纯数字）**，
//     所以 `leg90c-…` 与 `leg90b-…` **都不合格**（`leg40b` 那种写法能过，是因为它 = `leg`+`40`+`b-…`，
//     `\d+` 只吃了 `40`）。⇒ 定稿 **`leg91-inject-position`**（纯数字，且不含任何禁词）。
// ★★★leg92 再升一格（**真缺陷修** · 用户报「开关写着 `injectTagSpec:"1"`、构建号是新的、`sw2_` 一个都没有」）：
//   三个原因叠在一起：① 装配时那次 `apply()` 跑在 `loadWorld()` **之前**（世界还没进内存 ⇒ 两段都空，
//   而 `clear()` 会把 key **删掉**）；② 重设注入只挂在"世界推完一轮"上，而队列的 `refresh` 只在
//   **演算成功**后才跑；③ 用户**总闸关着** ⇒ 世界永远不推 ⇒ `apply()` 永远没有第二次机会。
//   ⇒ 修法：把重设注入放进 `refreshWorld()`（世界进内存之后，覆盖首载/切聊天/切世界/推一轮），
//     并让读数能分开"没跑过 / 跑了但 0 字 / 开关全关"三种状态（原来那句含糊兜底话正是把方向藏住的原因）。
// ★★★leg93 再升一格（用户裁示「**就甲吧**」· **模型看到的东西真的变了**）：
//   用户那一问：「**正文里有【…】呢？不能包裹在一个标签里吗？**」
//   实测抓出两处真会出事的（装置 `F:/deepseek/tmp/leg93-brackets-in-prose.mjs`）：
//     ① 正文里一句**以 `【行动】` 开头**的叙述 ⇒ 被当成**一条真行动**（名字 = 整句话）；
//     ② 正文里一句**以 `【时长】` 开头**的叙述 ⇒ `elapsed` **收下一整句话**，而引擎不校验这个值，
//        它**原样进世界模型的 prompt**。
//   ⇒ 甲案：标签必须包在一个围栏块里（开围栏一行写三个反引号加 tags），**提取器只扫块里**
//     （`src/tag-extract.js` 的 `shellRange`）；
//     块不在 ⇒ 退回逐行扫（老账逐字节不变）。⇒ **注入给聊天模型的那段规范原文改了**（`web/inject.js`
//     的 `tagSpecText()`：新增"块"的硬要求 + "一个标签一行"），所以这是"模型看到的东西变了"，升位。
//   ★起名纪律照旧（两条判据都咬）：合 `/^leg\d+[a-z]?-/`（**`\d+` 是纯数字**，补笔允许一个小写字母）、
//     且零禁词（agenda/tick/ssot/schema/chronicle/entity/kind）⇒ 定稿 **`leg93b-tag-block`**
//     （★`leg93` 这个号已被"交接落盘"那一笔占了 ⇒ 本笔按本仓补笔先例记作 **leg93b**）。
//   ★★**这一笔顺手把形状判据放宽了一格**（两条版本锁都改了）：原来写的是 `/^leg\d+-/`，
//     它把补笔钉死——`leg90b`/`leg90c` 当年就是被它咬红之后**改的名**（leg91 交接 §4 第 5 条留过档）。
//     那是**判据错，不是名字错**：本仓一直在用补笔写法，`leg40b` 能过纯属侥幸
//     （它 = `leg`+`40`+`b-`，`\d+` 只吃了 `40`）。⇒ 判据改成 `/^leg\d+[a-z]?-/`。
//   ★★★leg93c 再升一格（**玩家可见面真的变了** · 用户令「**事件链条点击后不要放在编年页了，
//     直接弹出一个小窗口**，要不然我在大事纪页签点击还得回到编年页」）：
//     病在**接线层**——旧 `bus['open-chain']` 把链视图 `insertAdjacentHTML` 进**写死的
//     `#sw2_view_chronicle`**，而「链」按钮在**两处**（编年页 + 大事纪·旧卷页）⇒ 从大事纪点，
//     内容进了另一个页签。⇒ 改成浮层（`web/style.css` 新增 `.sw2-cv-mask` 一族 + `web/index.js`
//     的 `CHAIN_MASK_ID`）⇒ **CSS 与玩家可见面都变了**，两个版本号同批升位。
//   ★起名纪律照旧：合 `/^leg\d+[a-z]?-/`、且零禁词 ⇒ 定稿 **`leg93c-chain-popup`**。
// ★★leg94：`leg93c-chain-popup` → **`leg94-oldvol-clean`**。玩家可见的**版面**变了（编年页不再印
//   里程碑插行「第 1–N 轮已收进大事纪「…」」——用户令「这是大事纪旧卷的东西」）⇒ 按升位纪律必须动；
//   而 `CSS_VERSION`（`web/index.js`）**本笔不动**：样式表一个字符没改（那条 `.sw2-ch-roll` 规则只是
//   随渲染一起撤了，无生产点的死规则删除**不算**"玩家可见的样式变了"——升位纪律要的是浏览器缓存会吃旧件）。
//   ★起名避开禁词（`chronicle`/`agenda`/`entity` 等一律不许出现在构建号里）。
// ★★leg94 再升一格（**玩家可见版面真变了 · 用户实机取证**）：① 大事纪里程碑卡那格 `[object Object]`
//   修好（`m.counts.events`）② 展开那一纪**印标题不再印机器号**（ids↔titles 同位配对）
//   ③ 编年账目行里内嵌的机器号渲染时换成人话 ④ **新增「说书」页签**（零 LLM：把账上的字重排成段落）。
//   ⇒ 四条都动玩家看得见的东西 ⇒ 必须升位。`CSS_VERSION`（`web/index.js`）**本笔也要升**——
//   说书视图一整族样式是新增的（`.sw2-pan-*`），不升号浏览器会吃旧样式表。
// ★★★leg95（用户令「让 llm 来决定何时结束」+「你先做吧」）：**说书页点层那格判词改了**——
//   原来每一环都印「还开着」（借的是引擎的账目状态 `closed`），用户截图当场指认「这些事件都是已经完成了呀，
//   这不是误导人吗」。定稿：点层三种状态分开说——**已收场**（模型判的）/ **已了结**（引擎机械扫的）/
//   **没人再提了**（还开着但再没新事从它长出来）/ 还开着（这一段还在往下长）。
//   ⇒ 玩家可见面真的变了 ⇒ 升位。
//   ★`CSS_VERSION` **本笔也要升**：`.sw2-pan-idle` 是**新增的一条规则**（点层第三种状态的颜色，
//     与 `-open` 必须长得不一样）。★与 leg94 那次"只删一条死规则"的情形**正相反**——那次不升是对的。
// ★★★leg95b（用户驳回第一刀：「**开没开着不应该挂在事件上**」）：**位置也要改对**——
//   第一刀只把判词从「还开着」改成三态，**状态却仍挂在每一件事上** ⇒ 用户当场驳回。
//   定稿：**点层一个字都不印状态**（只留轮次/事名/来路/地点/牵动的人；已收场的留痕退进悬停），
//   状态只在线头那枚徽上说一次，且分两态：**还在往下长**（最近 5 轮还有新事长出）／**挂着没了结**；
//   并且"没讲完的事"不再把开局的种子前提算成欠账。
//   ⇒ 玩家可见文本又变了 ⇒ **再升一格**。
//   ★`CSS_VERSION` **同批再升**（`20260922-leg95-points`）：新增 `.sw2-pan-badge.stale` 一条规则。
//   ★起名避禁词：`event` 在禁词表里（`点层` 直译会撞）⇒ 定稿 **`leg95-points`**（"点位那一格"）。
// ★★★leg95c（用户第三次指认：「**这个 7 还是事件的数量啊**」）：线头那枚徽**只许说状态、一个数都不带**
//   （第一版写成 `还在往下长 · 7 件没了结` ⇒ 把事件计数缝回了一枚"说线的状态"的徽里，而同一行左边
//   已经印着「第 0–12 轮 · 8 件事」——数还说重了）。⇒ 玩家可见文本又变 ⇒ **再升一格**。
// ★★★leg96（接手棒）：**线头那枚徽的判据用错了信号**——"还在往下长"原先量的是
//   `最晚一件事的轮次 > 世界轮次 − 5`（＝最近**有没有事落在这条线上**），而它该问的是
//   "**最近几轮还有没有新事从这条线长出来**"。真账当场证死（装置 `F:/deepseek/tmp/leg96-badge-verify.mjs`）：
//   还没收场的线里 **8 条被判反**（A 局 3 · B 局 5）——`血屠魔君现身南疆掀起杀戮`／`菩提禅院钟声震荡大荒`／
//   `天庭仙将玄鹤空降长城`／`敖天玄率领龙族精锐封锁东海海域` 这些**一个下游都没有**的线，
//   只因为"刚落账一件事"就被印成"还在往下长"。⇒ 玩家看到的那一态真的变了 ⇒ **再升一格**。
//   ★`CSS_VERSION`（`web/index.js`）**本笔不升**：没有动任何样式规则——三态的颜色/边框早就在
//     （`.sw2-pan-badge` 的 `.live/.stale/.done`），本笔只是**判得更准**（哪条线落到哪一态）。
//     ★与 leg94 那次"只删一条死规则 ⇒ 不升"是**同一条纪律**：**动没动样式，才是升不升 CSS 号的判据**。
//   ★起名避禁词：`state`/`grow` 均不在表内，且要能一眼看出"徽 + 往下长"⇒ 定稿 **`leg96-badgegrow`**。
// ★★★leg95d（另一路的用户指认：「**没讲完的事又是啥，这也不对吧**」）：线里那一整块**删除**——
//   它既不是计数（线头那行已印 `第 A–B 轮 · N 件事`）也不是过程（上面那条时间线已印每一件事），
//   而是**把账目状态换个说法再列一遍**；且"没讲完"是引擎行话，列出的那几件**全都发生过了**。
//   顺带撤掉统计行那格同样说行话的「N 件悬着 · N 件了结」（读者要的是"几条线还在往下长"）。
//   ⇒ 可见文本又变 ⇒ 再升一格。★起名：`leg95d-quiet`（"那一块不再出声了"）。
//   ★★本笔续（同一棒）：统计行与两个区块标题**也统一成徽那套词**——原来同一页有三种说法
//     （统计行「13 条还开着」／区块「还开着的线」／徽「还在往下长」），读者得自己换算。
//     定稿：全页只认 **还在往下长 / 挂着没了结 / 已收场** 这三个词；"还开着"整页退场。
//   ⇒ 起名改成 **`leg95d-oneline`**（"全页一套词"）。
//   ★`CSS_VERSION` **本笔不动**（零样式改动）——与 leg96 及 leg94 那次"只删一条死规则"同一条纪律：
//     **动没动样式，才是升不升 CSS 号的判据**。
// ★★★leg97（**「面」四层落地** · 用户口径「**以人物为切口感觉就不像面了**」⇒ 面＝空间上的一个地方）：
//   说书页整套换了组织方式——**大势（一句前提）→ 此刻（活数）→ 面（一个地方一段卡）→ 线（一段故事）→ 点（一件事）**；
//   「块」降为**面与面之间的关系**（印在面头上的"与别处相连：某某 → 哪几处"）；
//   末尾新增那条**自证闸**（`线 N 条 = 面内 ＋ 单线地点 ＋ 散落 · 掉出页面 0`）。
//   ⇒ 玩家可见版面与文案都真的变了 ⇒ 按纪律升位。
//   ★`CSS_VERSION` **同批升**（`20260922-leg97-places`）：`web/style.css` 新增了「面」那一族规则
//     （`.sw2-pan-face` / `-fh` / `-brg` / `-dashi` / `-other` / `-selfcheck` 等），
//     不升号浏览器会吃旧样式表（同 leg94 新增 `.sw2-pan-*` 那次）。
//   ★起名避禁词：`place` 是玩家词面的英文、不在禁词表内 ⇒ 定稿 **`leg97-places-org`**（"按地方组织"）。
// ★★★leg98（**面卡片那三处误读** · 用户实机把两张卡并排看之后问「**你发现什么问题了吗**」）：
//   三处都在**面上印出来的字**里，一处比一处更误导：
//     ① `大虞` 那张卡的"与别处相连"里印着 **`大虞 → 大虞皇陵`**——`大虞` 既是势力名又是地名、
//        驻地是「未明」，旧判据要求"名字 = 驻地 = 本面"三样同时成立才算自指 ⇒ 从尺子底下走过去；
//     ② 那一行的标签「与别处相连：」读起来像"这一处连着哪几处"，而箭头左边**是人**（读者以为是地名）；
//     ③ 面头「**N 条线在这里交会**」把**落脚**与**路过**糊成一个数 ⇒ `大虞` 与 `大虞皇陵` 两张卡
//        印出来几乎一样（同一批线、同样的条数），读者看不出哪张是"这条线就住在这儿"。
//   ⇒ 玩家可见文本真的变了 ⇒ 按纪律升位。
//   ★`CSS_VERSION` **那一笔不升**（`web/style.css` 零改动 ⇒ 同 leg95d/leg96/leg94 那条纪律：
//     **动没动样式，才是升不升 CSS 号的判据**）。
//   ★起名避禁词：`bridge` / `clean` 都不在禁词表内 ⇒ 定稿 **`leg98-bridgeclean`**。
// ★★★leg98 补（同批第二笔 · 用户令「**是改这一栏的颜色**不是标题文字**」**）：把**线头那一栏**的底
//   从 `transparent`（= 透出面卡底、两边亮度差 0）换成它自己的台面（`#282f3c`）＋ 竖线提亮。
//   ⇒ 玩家可见版面又变了 ⇒ **同批再升一格**。★这一笔**真动了样式** ⇒ `CSS_VERSION` **同批升**
//     （`20260922-leg97-places` → **`20260922-leg98-rowbg`**）——与上一笔"只管文本 ⇒ 不升号"正相反，
//     判据仍是同一条：**动没动样式**。
//   ★起名避禁词：`rowbg` 不在禁词表内 ⇒ 定稿 **`leg98-rowbg`**。
// ★★★leg98 补二（同批第三笔 · 用户令「**里面有两个大势卡片留一个就好了**，世情 · 3 键，还有浪尖，
//   张力，盘算数量你看看怎么集合起来好看」——三套候选里选了**乙**）：
//   ①**撤掉信息带里的「大势」格**（它与四层顶上那格是同一句话，照 leg97 裁 `digest` 的口径：
//     同一件事不说两遍，留"书的原文"那一份）；
//   ②信息带**五格平铺 → 两栏**：左＝世情 3 键（竖排）· 右＝三行读数（盘算 / 张力 / 浪尖）。
//   ⇒ 玩家可见版面又变了 ⇒ **同批再升一格**。★动了样式（`.sw2-band-read` / `.sw2-readrow` /
//     `.sw2-readkey` / `.sw2-tides` 由竖列改横排）⇒ `CSS_VERSION` **同批升**。
//   ★起名避禁词：`band` / `merge` 不在禁词表内 ⇒ 定稿 **`leg98-bandmerge`**。
// ★★★leg98 补四（同批第四笔 · 用户令「**你总得把这个放在上面吧？而且我要往下翻很久才能看到这些**」）：
//   ①**信息带升成页头**（原来它排在**页尾**，而四层正文有可见 **10150 字**、那条状态条只有 **94 字**
//     ⇒ 用户得滚过一万多字才看见它）；②这一页**改两栏**（左＝故事轴：四层 → 散落 → 自证 → 动态流；
//     右＝盘算总览 ＋ 地图），不再是"一条竖线到底、右边 330px 一路空着"。
//   ★这一族（页头 ＋ 信息带 ＋ 两栏怎么拼）**整族搬进 `web/page-compose.js`**——
//     `web/index.js` 有条行数锁（`lines < 3100`），它搬走之后接线层 3098 行（比并页时还薄）。
//   ⇒ 玩家可见版面又变了 ⇒ **同批再升一格**。★动了样式（`.sw2-merged-grid/-main/-side`）
//     ⇒ `CSS_VERSION` **同批升**。
//   ★起名避禁词：`page` / `cols` 不在禁词表内 ⇒ 定稿 **`leg98-pagecols`**。
// ★★★leg99 再升一格（`leg98-pagecols` → **`leg99-seedborn`**）：**两把"事件出生轮"尺子合成一把**——
//   `ev_seed_N` 里的 `N` 是**播种时的枚举号**（出处 `src/seed-roots.js:149`），不是轮次；
//   而引擎那口按"取首个数字段"把它读成了第 `N` 轮 ⇒ 种子被算进"近 10 轮"窗口。
//   ★玩家看得见的那一笔（真机账 大荒z · 第 12 轮）：「近 10 轮事件」 **83 → 76**。
//   ⇒ 面板上印出来的那个数真的变了 ⇒ 按纪律升位。
//   ★`CSS_VERSION`（`web/index.js`）**那一笔不升**（仍是 `20260922-leg98-pagecols`）：
//     `web/style.css` **一个字节没动** ⇒ 按同一条纪律（**动没动样式，才是升不升 CSS 号的判据**）。
//     ★那是**头一次两个号停在不同的 leg 号上**（CSS 停在 leg98、面板走到 leg99）——而这本身是一条真事实：
//     "文本/数字变了、样式没变"完全可能；此前 leg94/leg95d/leg96 那几次虽然也是"只动文本"，
//     但 `PANEL_BUILD` 与 `CSS_VERSION` 当时要么同批、要么 CSS 号干脆是上一次的值，
//     ⇒ 那条耦合锁（`cssVer.includes(PANEL_BUILD)`）**从没被这种情形碰过**。★★它当场顶破了：
//     只升 `PANEL_BUILD` ⇒ 那条锁红。⇒ 定稿把它**收窄成"CSS 号的 leg 号只许同批或恰好落后一笔"**。
//     ★口径**一个字没放宽**：原来它管的是"两个号同批升"，而"同批"本来就只由 **leg 号**表达；
//       名字那一截（`rowbg` / `pagecols` / `seedborn` / `pageflow`）说的是"这一笔干了什么"，
//       两个号各说各的**才是对的**。
//   ★起名避禁词：`seed` / `born` 不在禁词表内（禁的是 `agenda`/`tick`/`ssot`/`schema`/`chronicle`/`entity`/`kind`）
//     ⇒ 那一格的号定稿 **`leg99-seedborn`**（**随后同批被下面第二笔顶成 `leg99-pageflow`**）。
// ★★★leg99 同批第二笔（用户令「**动态流可以不要了**，其他就按那个模板做，但要注意**两列都能独自滑动**」）：
//   **动态流撤出并页那一页**（`web/page-compose.js` 不再引用 `board.feed`）＋ **两栏各自独立滑动**
//   ⇒ 玩家可见的**版面真的变了** ⇒ 同批**再升一格**。
//   ★与上一笔**正相反**的地方在这里：上一笔（种子出生轮）只动了**文本/读数** ⇒ `CSS_VERSION` 不升；
//     这一笔真动了 `web/style.css`（容器定高、两栏 `overflow-y`、撤掉右栏 sticky、窄屏退单列）
//     ⇒ `CSS_VERSION` **同批升**（`20260922-leg98-pagecols` → **`20260922-leg99-pageflow`**）。
//     ★两笔的判据是同一条：**动没动样式**——这正是那条纪律的价值（它同时管"该升"与"不该升"）。
//   ★起名避禁词：`flow` 不在禁词表内 ⇒ 定稿 **`leg99-pageflow`**。
// ★★★leg99 同批第三笔（用户令「**再把参数页微微修一下，没必要每个参数还要显示当前是什么，
//   当前是什么值就是下拉栏的值**」）：参数页**撤掉重复印的当前值**——
//   ①自变量（天时/时局）那一行整行撤掉「**当前 X**」（它与下面下拉选中的档**逐字相同**）；
//   ②尺度上限那 7 行撤掉输入框旁边那枚**琥珀色数字**（同一个数并排两遍）；
//   ③★**因变量（乱象）那一格留着**——它**没有控件**，那格是它唯一的读数面（撤了玩家就看不到乱象是多少）。
//   ⇒ 玩家可见的**文本变了**（少印了 9 处重复）⇒ 同批**再升一格**。
//   ★`CSS_VERSION` **不升**：`web/style.css` **一个字节没动** —— `.sw2-row` 的 `em` 本来就是 `flex:1`，
//     撤掉中间那格之后说明自然补位，不留窟窿（**不靠"要不要补一条样式"这种猜测**，靠既有规则就够）。
//     ★与同批第二笔（真动了样式 ⇒ 升）**又一次正相反**，判据仍是同一条：**动没动样式**。
//   ★起名避禁词：`param` / `quiet` 都不在禁词表内 ⇒ 定稿 **`leg99-paramquiet`**。
// ★★★leg100（本棒第一笔，用户令「**那行你改一下**」——指"收场被拒时那句出路"）：
//   **收场被拒的出路话术改成"指对栏"**（`src/ref-rules.js` 的 `'eventClosures.event'` 那一格，**只动文案**）：
//   ★病（本棒真机取证，装置 `F:/deepseek/tmp/prototypes/leg100-closure-leak.mjs`）：用户实机那两条
//     `$.eventClosures[0..1]` 报错里的号（`ev_6_4` / `ev_7_2`）**不在** `pendingEvents`（72 条未了结，
//     引擎没喂错料），**却在** `recentClosedEvents`（最近了结的事：尾巴 8 条）——而那段在包里
//     （`pack.js:1020`）**只剩 `{id}`、没有标题** ⇒ 一串光秃秃的号，读起来正是一份"还能收的事"的候选池，
//     两个被拒的号恰好是那份尾巴的**头两条**。★旧话术只指 `pendingEvents` 那一栏 ⇒ 模型照旧话去**错的**
//     那一栏里找（它本来是从另一栏抄的）⇒ 换号重试；而收场是**整步拒**（净化器不替模型摘这一项）
//     ⇒ 每试一次**白烧一轮**（`ref-rules.js:524` 那条自律的原话）。
//   ★修法：把"**这个号住在哪一栏**"与"**要收场只认哪一栏**"两栏的名字都点出来（`recentClosedEvents` /
//     `pendingEvents`——`packTextOf` 是 `JSON.stringify`，模型看到的就是这两个键名）。
//   ★**这真的改到了玩家/模型可见面**：那句话是拒收时**印给模型与用户**的原文 ⇒ 按本仓纪律必须升位
//     （同 leg64/leg66/leg67 那条：**报错不给路 ⇒ 模型反复换号重试 ⇒ 每试一次白烧一轮**）。
//   ★`CSS_VERSION` **不升**（仍是 `20260922-leg99-pageflow`）：`web/style.css` **零改动** ⇒
//     同 leg95d/leg96/leg94 那条纪律（**动没动样式**，才是升不升 CSS 号的判据）。
//   ★起名避禁词：`closure` / `exit` 都不在禁词表内（禁的是 `agenda`/`tick`/`ssot`/`schema`/`chronicle`/
//     `entity`/`kind`；★`event` 也在表内，见 leg95 那次改名）⇒ 定稿 **`leg100-closureexit`**。
// ★★★leg100 同棒第二笔（用户令「**可以按甲吧**」——甲案：**让"归档"与"候选"在给模型的资料里长得不一样**）：
//   ★为什么第一笔不够（这是本笔存在的理由）：第一笔只改了**报错时那句出路**——那是"骂得更清楚"，
//     而真病是**模型手上没有分辨"候选/墓碑"的尺子**：`recentClosedEvents`（最近了结的事，归档）
//     与 `pendingEvents`（还没结束的事，候选）此前**形状完全一样**（都是 id+title+source）。
//   ★改动两处（**引擎与提示词各一半**）：
//     ① `src/pack.js`：归档那一栏每一行盖 **`closed: true`**（独立一格），**并且预算裁剪时跟着留**
//        ——`trimPack` 的 ③ 级原来只留 `{id}`，而"只剩一串光秃秃的号"**正是**最像候选池的形态；
//     ② `src/prompts.js` 第 15 条补指路（只从 `pendingEvents` 挑 / `recentClosedEvents` 是归档、别从那拿号），
//        并同步 `MAIN_PROMPT_V` → **`v2-agenda-t1-23`**（提示词正文变了 ⇒ 版本锁同批升）。
//   ★读数：真账那一栏 **8 条全部带记号**（包文本 54790 → **54902 字节**，8×14 字节，预算可忽略）；
//     冒烟 **8231 字节逐字节未变**（引擎零漂移——本笔没动账、没动读数，只动了"给模型看的形状"）。
//   ★`CSS_VERSION` **不升**（`web/style.css` 零改动）——判据仍是那一条：**动没动样式**。
//   ★起名避禁词：`archive` / `mark` 都不在禁词表内 ⇒ 定稿 **`leg100-archivemark`**。
// ★★★leg101 `fitwindow`（用户令「**没事不急你直接改吧**」，起因是他贴了实机截图并说
//   「**窗口太小了，最重要的说书都没位置了**」）：**修面板把内容裁掉那个版面病**。
//   ★病（两处，都在 `web/style.css`，且是 leg99 那一笔留下的）：
//     ①**内外两条滚动条打架**：leg99 把"滚动"从外壳下放到两栏（`.sw2-merged-main/-side{overflow-y:auto}`），
//       但**外壳那条 `overflow:auto` 没撤** ⇒ 两栏在内部滚、外壳也在滚，**外壳滚到哪内容就被裁到哪**；
//     ②**`calc(88vh - 190px)` 里那个 190px 不够**：它是按"页头一行 ＋ 信息带"估的，而外壳里实打实
//       还有 状态条 ＋ 页签行 ＋ 页内边距（实测约 61+31+50+32）⇒ grid 比外壳真剩下的高度**更高**，
//       于是它把外壳撑出滚动条——**这正是①那个打架的机理**。
//   ★修法（三处，全在 `web/style.css`，**一个字都没改产品的行为**）：
//     ①`.sw2-window` → `height:88vh; display:flex; flex-direction:column; overflow:hidden`（撤 `max-height:88%` + `overflow:auto`）；
//     ②`.sw2-view.sw2-active` → `flex:1 1 auto; min-height:0; overflow:auto`（当前页吃掉余高，滚动**按页**给出）；
//     ③`.sw2-merged-grid` → `height:calc(88vh - 190px)` **改 `height:100%`**。
//   ★★为什么是"改结构"而不是"把 190 调大"：**没有浏览器就量不出那个数的真值**（leg89 §5.5 那条自律），
//     调常数等于再猜一次；而 `100%` 由 flex 分配的真高度解得出来 ⇒ **页签多一行少一行都不再算错**。
//   ★这是"玩家可见版面真变了" ⇒ **`PANEL_BUILD` 升一格**；**`CSS_VERSION` 同批升**
//     （动了样式才是升 CSS 号的判据 —— 本笔**真动了**，与 leg100 那两笔"样式零改动 ⇒ 不升"**正相反**）。
//   ★起名避禁词：`fit` / `window` 都不在禁词表内 ⇒ 定稿 **`leg101-fitwindow`**。
// ★★★leg102 `fullscreen`（用户令「**占满整个屏幕吧**」）：leg101 之后用户回「**老样子没变大**」——
//   实测（用户 Console 读数）证明 leg101 **完全生效**（`height=790px | overflow:hidden`），
//   病在**窗口本身没变大**：`88vh` 相对旧 `max-height:88%` 只差一点点，而**宽度是写死的 `1120px`**
//   （1080p 上只占 58%）⇒「太小」是**宽高两头都小**。★这一笔按用户那句话定口径：**占满整屏**。
//   ①宽度 `1120px` → `100%`；②高度 `88vh` → `calc(100vh - 40px)`（40px = 遮罩两侧留边）。
//   ★用 vh/百分比而不是更大的常数：窗口多大由**视口**决定，不必替别人的屏幕猜一个数。
//   ★`.sw2-view.sw2-active` 与 `.sw2-merged-grid` 一个字没改——它们吃 flex 分出来的余高，
//     窗口一变大自动跟着变大（这正是 leg101 改结构的红利）。
//   ⇒ 玩家可见版面又变 ⇒ **同批再升一格**；`CSS_VERSION` 同批升（真动了样式）。
//   ★起名避禁词：`fullscreen` 不在表内 ⇒ 定稿 **`leg102-fullscreen`**。
export const PANEL_BUILD = 'leg103-switchglow';

export const LABELS = {    env: { 民生度: '民生', 动乱度: '乱象', 天时: '天时', 张力推手: '时局' },
    kind: { faction: '势力', character: '角色' },
    visibility: { known: '明', concealed: '暗' },
    status: { active: '活跃', retired: '背景', dead: '已灭' },
};

// leg25 c（用户令「删」）：`LABELS.attr` 与 `ATTR_HINTS` **整条删除**——四维浮点（兵力/权位/人脉/耳目）
//   不存在了：它们没法精确表示（书里没刻度、现实里也没有），压成 0–1 就是拿精确的外壳装模糊的内容，
//   而且手拍值让"编的"看起来像"算的"（design-core-leg23 §4 第 1 条）。
//   书里的说法一律**照抄成文本**显示（实体 `实力` = 「T9渡劫巅峰」，据书；见 spec-entity-field-lookup）。
//   面板从此不再有「有据 n/4 / 数值无据」这类说法——那些数没有了，"有几维有据"自然无从谈起。

// K41/链视图细案 §3.1（A-16）：编年五筛（chips 玩家词面 ↔ kind 契约 token）
export const CHRONICLE_FILTERS = Object.freeze([
    { token: 'scheme', label: '谋划' },
    { token: 'major', label: '大事' },
    { token: 'ripple', label: '牵动' },
    { token: 'shade', label: '暗处' },
    { token: 'state', label: '时局' },
]);

// 渲染产物黑名单（引擎术语不得出现在玩家视线）
export const BLACKLIST = [
    '分量', '熵泵', '里程碑', '上溯', '波及',
    // leg26：参数键本身（民生度/动乱度…）是**账本口径**，不是玩家词——面板一律走 LABELS.env
    //   （民生/乱象/天时/时局）。带上它们才能锁住"引擎键名不许漏进玩家视线"。
    '民生度', '动乱度', '张力推手', '异想天开键',
    'fingerprint', 'derivedFrom', 'intensity', 'polarity', 'direction', 'tension',
    'hardPower', 'office', 'network', 'intel', 'visibility', 'concealed',
    'schema', 'ssot', 'worldstep', 'agenda', 'chronicle', 'milestone', 'tick',
    // ★★leg52：**「派生源」补上**。`render.js:8` 的文件头注释从 leg26 起就写着"禁：…派生源…"，
    //   而数组里**只有英文 `derivedFrom`、没有这个中文词** ⇒ 设定页那句「浪尖（派生源）」印了十几棒
    //   都没被咬住（本仓判据只扫渲染产物，而它不在数组里就等于不存在）。
    //   ★这条同时是"注释与实现不一致"的实例：**声明禁的，数组里必须真有**（否则守门是空绿）。
    '派生源',
    // ★★★leg103（用户令「我捋一遍」后做的文案体检）：**「派生源」那一族的第二批漏网**。
    //   这一笔不是靠人眼想起来的，是**把八页渲染产物整段抽出来扫**（装置 `leg103-copy-audit.mjs`）量出来的：
    //   引擎×12 · 落账×5 · 账本×3 · 进包×1 · 接线×1——**全都在玩家眼前**，而全都不在数组里
    //   ⇒ 与 leg52 那条**同一个病、同一个机理**（判据只认数组，数组里没有的词等于不存在）。
    //   ★口径：这五个词是**我们这行的行话**，玩家读到的应该是「世界每轮自己算的」「记到账上」「收进每轮给模型的那一份」
    //     这类人话；数组是**机器可执行的禁令**，写在这里才算数（判据见 `test/render.test.js` 的 K33/A-3 全量扫描）。
    '引擎', '落账', '入账', '账本', '进包', '接线',
];

export function escapeHtml(s) {
    return String(s ?? '')
        .replaceAll('&', '&amp;').replaceAll('<', '&lt;').replaceAll('>', '&gt;')
        .replaceAll('"', '&quot;').replaceAll("'", '&#39;');
}

// ★leg40b（A5）：**进 title 的文案走这里**。
//   病是复发过三次的同一个：`escapeHtml` 把 `"` 转成 `&quot;` 之后，属性值在 HTML 层面是安全的，
//   但**源码里那个裸 `"` 仍然把模板字符串的 title 掐断**（`leg25 d` 那次、`leg25 f` 那次、本次体检又一处）。
//   治法是形态判据、不靠记性：凡是写进 `title="…"` 的文本，一律先过这一层——
//   它把半角引号**换成中文引号**（玩家读起来一样、源码里再也不可能截断），再交给 escapeHtml。
export const attrText = (s) => escapeHtml(String(s ?? '').replaceAll('"', '「').replaceAll("'", '『'));

export const fmtTick = (n) => `第${n}轮`;
export const fmtPct = (n) => `${Math.round((n ?? 0) * 100)}`;
// ★★★leg55：冷档阈值那两个数的显示口径（`src/storage.js` 的 `PROPOSED_LIMITS.bytes` 是**字节**，
//   面板印的是 MB ⇒ 换算只许住这一处，别在公式里再写一遍 `/1024/1024`）。
//   ① 值来自 `web/index.js` 的 `renderCfg()` 现读 `PROPOSED_LIMITS`（唯一真源）；
//   ② **缺值照样印字面量**——但那是**诚实**的：`render()` 的夹具/直调不经过接线层，
//      真源在那儿本来就不在手上；生产路有判据锁着必须注入（`test/render.test.js` leg55）。
//   ③ 整数不带小数点（`5MB` 而不是 `5.0MB`），非整数才留一位——两类阈值都不该被显示层改写。
export const fmtLimitNum = (n, fallback) => {
    if (typeof n !== 'number' || !Number.isFinite(n)) return String(fallback);
    return Number.isInteger(n) ? String(n) : n.toFixed(1);
};

export function entityLabel(world, id) {
    if (id === world.context?.playerId) return '你';
    const e = world.entities.find((x) => x.id === id);
    return e ? e.name || id : id;
}

// ★leg49（细案 J11）：**玩家那一支已删**——旧版对玩家返回「你的棋子」，而三列版式里
//   玩家那一行靠「归属与来历 + 在办的事」自证（细案 §3.1：玩家标记不许搬回来）。
//   类别词只由 kind 决定（角色/势力），玩家与旁人同口径。
//   ★承重改动，已核过调用面：全仓只有实体页一处消费（kindLabel 的玩家支无人依赖）。
//   ★评审修正 #5：那唯一一处调用点仍按两参调用（`kindLabel(e, world)`）——签名早已只收 `entity`，
//     多传的第二参是改签名时的残留。已核全仓（src/web/test/docs）确无别处按两参调用，去掉那个实参。
export function kindLabel(entity) {
    return LABELS.kind[entity.kind] || '实体';
}

