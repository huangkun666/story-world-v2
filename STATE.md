# STATE · story-world v2 现状（接手第一入口）

> **接手先读这一份，再读最新交接文档。** 这一份只写**当前值**，不写"某棒做了什么"。
> 历史逐棒档案在 `docs/START-HERE.md`（**已冻结**）、`LEDGER.md`（**已冻结**）、`docs/ledger.md`（全文台账，原地不动）。
> ★**纪律**：任何文档要引用判据数 / 构建号 / 行数 / 发布位，**只许写"见 `STATE.md` §1"，不许再写死数字**——写死就是第二份真相。
> ★本文件由 `node scripts/audit-docs.mjs` 守门（大小上限、按棒存档段、与 README 读数一致、与 `docs/index.json` 一致）。
>
> 建立于 leg106（2026-09-21），起因：`docs/START-HERE.md` 自订 ≈5 KB 上限、实测 **291 KB（超标 58 倍）**；
> `LEDGER.md` 索引停在 leg98、全文已到 leg105（**缺 7 棒**）；判据数在活文档里有 **6 个并行"当前值"**。

---

## 0. 三十秒版：这台东西是什么

一个跑在 SillyTavern 里的**活世界引擎**。你每推进一步 RP，世界演化一步：势力与角色按自己的盘算行动，事件从行动里长出来、带因果。

**没有"主角"**——只有一枚玩家棋子，分量由客观状态算出。你通过两个口子与它相处：**RP 对话是落子的手，观棋窗口是观棋的眼**。

**三者分工（承重墙，别越界）**：

| 谁 | 干什么 | 不干什么 |
|---|---|---|
| **世界书** | 作者的原稿，只读 | 一个字不改 |
| **引擎** | **账房 + 史官**：记账、守连续性、守身份、管盘算生命周期、算能见度 | **不裁胜负、不算战力、不给实体打分、不发明事实** |
| **LLM** | 读者 + 提议者 + 笔：每回合提议"接下来发生什么"，把账本写成场景 | **不记账**（写账只有引擎能做） |

---

## 1. ★★ 当前权威读数（唯一出处）

> 这一节是**全仓唯一的当前值出处**。别处引用一律写"见 `STATE.md` §1"。
> 机器可读取数在 `docs/index.json`（由 `scripts/audit-docs.mjs` 生成）——**两处必须相同**，不等即被守门判红。

| 读数 | 当前值 | 怎么复量 |
|---|---|---|
| 判据 | **1096 / 1096 · fail 0 · skipped 0 · todo 0** | `node --test`（**无参，必须在插件目录内**） |
| 冒烟 | **PASS · 终态 SSOT 8231 字节 · 警告 0** | `node demo/smoke-demo.js` |
| `PANEL_BUILD` | **`leg105-deadpager`** | `src/render-base.js` |
| `CSS_VERSION` | **`20260923-leg104-cues`** | `web/index.js`（`injectCss()`） |
| `MAIN_PROMPT_V` | **`v2-agenda-t1-23`** | `src/prompts.js` |
| `web/index.js` 行数 | **3099 / 3100**（硬锁 `<3100`，`test/web-view-state-layout.test.js:339`） | `split('\n').length` |
| 发布仓 main | **`8e04fca`** · 构建号 **`leg105-deadpager`**（★leg106 已推：leg103–106 四棒一次补齐，远端核验 20/20） | `node scripts/verify-release.mjs` |
| release tag | `v1.0.0-preview.1` → **`1a54424`（= leg100 那一版）** | ★点 release 下载的人拿到旧面板（语义待裁定，见 §3-A） |
| 版本号 | `manifest.json` = `web/index.js` 的 `VERSION` = **`1.0.0`**（两处，判据锁着第二处） | `test/browser-compat.test.js` |

**三个"真的变了才升"的号（判据锁着）**：
- 动了 `web/style.css` ⇒ **必须同批升 `CSS_VERSION`**；没动样式 ⇒ **不升**（判据只看"动没动样式"）。
- 动了 `src/prompts.js` 的模板 ⇒ 同批升 `MAIN_PROMPT_V`。
- 玩家可见版面真变了 ⇒ 升 `PANEL_BUILD`。

---

## 2. 红线与规矩（逐字搬家，不许改写）

### 2.1 ★最高准则：没命令，不准动一个字节（用户 2026-09-11 立）

> **用户原话：「不要动代码，把不要动代码写进最高准则，没有我的命令不准动一个字节」。**

**这一条高于其它所有规矩。** 具体含义：

1. **没有用户当次明确命令，不许改任何文件的任何一个字节**——包括 `src/`、`web/`、`test/`、`docs/`、`demo/`，也包括"顺手补一句注释""更新一下台账""加个测试"。
2. **不许把交接/任务书/未决清单里的"下一步"当授权**。任务书是**待选项**，不是命令。
3. **"只读取证不算开工"这个借口不成立**——新建脚本、复制文件、跑真模型调用，都要先问。
4. 想动任何东西之前，**先说出来打算改什么、改哪几个文件，等用户点头**。
5. 用户说"停"就**立刻停**，不解释、不续做、不问"那要不要顺便…"。

> ⚠ **同级铁律（2026-09-12 血的教训）**：**含中文的文件，禁止用 PowerShell 读写**。
> `Set-Content` 按系统码页有损解码再写回 ⇒ **1,155 字符变 U+FFFD（不可逆）**、303 行损坏、406 行被吃换行。
> **正确做法**：改文件只用 `edit`/`write` 工具；要程序化处理**先复制到 TEMP 再用 Node 读写字节**；"先证红"只走副本，**绝不原地重写整文件**。

### 2.2 四条不可越的线

1. **不许把书里的词换算成数。** 书里的说法**照抄成文本**（实体 `实力` = 「T9渡劫巅峰」，据书）。引擎不换算、不进公式、不排序、不比较。四维浮点（兵力/权位/人脉/耳目）就是因此被整体删除的，别再请回来。
2. **空着就是空着。** 没依据就不填值，不许用默认值/占位值冒充客观。缺数据只许记「未加载」，**绝不用空值反推"书里没有"**。
3. **★不能泛用到所有世界书的设计都是垃圾**（用户 2026-09-11 立，见 `ANCHOR.md` §4.8）：三条判据（能跑不崩 / **不共线** / 宁缺勿造）+ 禁用清单（形态当必要条件、**占位值当判据**、词表判语义、单本覆盖率当证据）。**加任何机制前先跑 `demo/audit-mechanism-genericity.js` 拿八本书的数。**
4. **改判据 = 改承重墙。** 判据要改，先出数据、再拍板、再动手；不许"顺手加个小功能"。

### 2.3 动手前的规矩（六条）

1. **没命令不动代码**；机制新增**两段式**：先细案 → 用户拍板 → 再实施。
2. **数字先报批**：上限/阈值/系数都是**提案态**，先出曲线再定案。
3. **改码三件事连着做：测试 → 部署 → 台账**。缺一件 = 变更没完成。
4. **先 read 再 edit**；改哪层要声明（契约层 / 引擎层 / 渲染层 / 编排层，见 `docs/dev-process.md` §9）。
5. **★新机制先过泛用性**（§2.2 第 3 条）。
6. **★接线必须有测试**：要真 `ctx` 的接线，要么提成可导出函数**真跑**，要么写注入 fake ctx 的测试——"测试全绿但实机就炸"是本仓的常客。

**验证手段**（都在插件目录内跑）：

| 命令 | 看什么 |
|---|---|
| `node --test` | 全量判据（**无参**，在插件目录内） |
| `node demo/smoke-demo.js` | 端到端 50 tick 冒烟（**单测绿 ≠ 成品**） |
| `node scripts/audit-docs.mjs` | 文档守门：生成 `docs/index.json` + 三条自检（本文件大小/按棒存档段/读数一致） |
| `node --check <file>` | 语法 |

### 2.4 ★待办纪律（leg106 立，治"任务堆积"）

1. **§3 是唯一的活儿清单。** 交接文档**只许引用它，不许复制它**（本仓已实测：同一份"留给下一任"清单曾同时活在 **5 处**，同一件事的优先级在 3 份文件里分别是 ①/③）。
2. **新增任务要用户点头才能进清单**；agent 顺手记的观察**先写进当棒交接文档**，不进 §3。
3. **做完立刻划掉**（不划掉 = 下一棒会重做。已实测：leg103 §5 里 3 条做完了，leg105 §5 照抄时没划线）。
4. **清单上限 12 条**：满了要先划掉或合并，才能加新的。

---

## 3. ★★ 未决事项（唯一一份活儿清单）

> 每条带：**出处** · **触发条件** · **拍板人**。做完就地划掉并写"已办结（哪棒）"。
> 清单外的观察留在当棒交接文档里，**不进这里**——这里只放"下一步真会做"的。

### 等你拍板的（4 条）

| # | 事 | 为什么现在问 | 出处 |
|---|---|---|---|
| A | **tag / release 语义**：tag 跟每个可安装版本走？还是只跟 release 走？ | 现在 tag 落后 5 个版本 ⇒ 点 release 下载的人拿到 leg100 那一份 | leg100 裁定「丙案：不动 tag」；leg103 §8 记「要改先问用户」 |
| B | **大书首跑耗时上报**：实测 **20–30 分钟**，面板现在**只有"第 i / N 块"、没有预估、不能取消** | 这是陌生人第一小时最大的劝退点 | `src/abstract.js:2136` 留的旧读数（22 次调用 / 1909 秒）与你的实测差一个量级 ⇒ **以你的实测为准** |
| C | **`snapshots/*.jsonl` 8 份真跑快照**已被 git 跟踪，内含真实 RP 输入与模型原始输出（`origin` 是 **public 仓**） | 公开仓里带着你自己的对局；发布仓树里**没有**它（安装者拿到的是干净的） | leg106 实测 |
| D | **`docs/START-HERE.md`（291 KB）与 `LEDGER.md`（326 KB）已冻结**，你要不要有一天整批迁进 `docs/handoffs/`？ | 冻结已让冷启动从 ≈545 KB 降到 ≈30 KB；迁移只是收拾门面 | leg106 文档重塑 |

### 已排序的活儿（照 leg103 §5 的优先级，leg106 更新现状）

| 优先 | 事 | 现状（leg106 复核） |
|---|---|---|
| ① | **`recalled`（世界书检索注入）该拆**——三步、**顺序不能反**（先给实体开"点名" → 同批改 `src/prompts.js:301-303` 并升 `MAIN_PROMPT_V` → 再拔 `injectWorldBookRecall`）；★先量再动 | **未动**（leg102 §8 排的序，leg103/105 都没做） |
| ② | **B4 长操作无忙态**（只重抽设定 / 只抽刻度 / 采用草稿）⇒ 连点两次发两串模型调用 | **未做**（最接近真事故的一条） |
| ③ | **B3** 注入开关取不到节点时**静默不更新**（状态条与控件同一刻说两件事） | **未做** |
| ④ | **B5/B6/B8/B9** 零散：推进去重没回答"成功了吗" / 快照页没标"盘上是哪一份" / 落盘三种说法 / 关面板不清状态条 | **未做** |
| ⑤ | **观感那一批**（85 处 `font-size` <12px · `--sw2-text-faint` 3.05–3.59:1 · 13 套徽 / 3 种圆角） | **等你一句"看一眼截图拍板"**——它需要你的眼睛，不是 agent 的 |

### 已办结（近三棒，别再重做）

- ✔ **发布面追赶（leg106 已办结）**：发布仓 main `f6368fc` → **`8e04fca`**（leg103–106 四棒一次补齐，远端核验 20/20）。
  ★**仍未决的只剩 tag 语义**（见上表 A）：tag 还指 leg100 那一版 ⇒ 点 release 下载的人拿到旧面板。
- ✔ **C2 每轮重绘把滚动位置弹回顶部** — leg104 已修（`web/scroll-keep.js` 在位，`web/index.js:474/482` 取后放回）。
- ✔ **`:disabled` 样式 / 断点 / A4 旧卷浮层** — leg104 已修（`web/style.css:613` 那一族在位）。
- ✔ **leg105**：编年·账目层死分页器已撤 + `data-layer` 补拾 + 判据/细案跟改。

---

## 4. 上一棒

- **leg106**（2026-09-21）：**文档结构重塑**（本文件 + `docs/index.json` + `scripts/audit-docs.mjs` + 三份历史文件加墓碑）+ **发布流程收进仓**（`scripts/publish-release.mjs` / `verify-release.mjs`）+ **发布面追赶**（leg103–106 四棒一次补齐）。
  交接：`docs/session-handoff-2026-09-21-leg106.md`（§1 病与治法 · §2 待办纪律 · §5 踩的坑）。
- **leg105**（2026-09-21）：编年·账目层的死分页器撤了（用户令「没用就删了吧」）——双重死控件查实（渲染侧没人消费 + 接线侧 `data-layer` 不拾）⇒ **删，不修**；同批补拾 `data-layer`。
  交接：`docs/session-handoff-2026-09-20-leg105.md`（§0 一句话 · §5 留下的活 · §6 三条最该带走的）。
- ★**leg100–105 没有独立交接文档**（内容只活在已冻结的 `docs/START-HERE.md` 按棒段里）；`docs/` 根最新独立交接 = leg99。

---

## 5. 现场（本机）

- **项目**：`F:\deepseek\plugins\story-world-v2`（仓库根是 `F:\deepseek\plugins`，**不是** `F:\deepseek`；分支 `leg62-scales-concept-table`）
- **部署位**：`F:\jiuguanai\SillyTavern-Launcher\SillyTavern\public\scripts\extensions\third-party\story-world-v2` = **junction → 项目根**（改 `web/` 或 `src/` 后 **Ctrl+Shift+R** 即载）
- **面板构建号在哪看**：**设置页**里那行 `构建 <号>`（`src/render.js:636`）。★它**不在**"面板右下角"（README 旧文案写错过一次，leg106 改正）。
- **真模型通道**：`loadStPresetConfig()` + `createHttpTransport`（密钥仅本机读、**不打印**）
- **用户真账**：`F:\jiuguanai\...\chats\大荒z\*.jsonl` —— **一律先 cp 副本再解析**（铁律级现场纪律）
- **插件目录内**：零依赖、无构建步骤、**没有 `package.json`**（`scripts/*.mjs` 用 `.mjs` 后缀，正是为了不引入 `package.json`）

### 5.1 发布流程（leg103 跑通两遍，leg106 首次收进仓）

**「导出独立根树 → 仓外独立跑判据与冒烟 → 推发布仓 → 远端逐字节核验」**，**全程不碰工作区**（那个目录同时是用户的真机安装位）。

- 工具：`node scripts/publish-release.mjs`（导出 + 仓外跑 + 推）· `node scripts/verify-release.mjs`（远端只读终检）
- ★**绝不重写工作区**；推送用 `git` 底层（`read-tree` / `write-tree` / `commit-tree` + 临时 index）。
- ★**每一步先核对现状、条件不成立就停下报因**，不硬来；**幂等守卫要比"本笔真正改掉的那一处"**。
- **★上线（第 4 步）**：`node scripts/verify-release.mjs`——**远端只读终检**（版本三处一致 · 构建号 · README 读数逐字 · 新基建在位 · 发布树里没有 `snapshots/` 与 `package.json` · **8 个文件逐字节同 blob 哈希** · tag/release 现状）。
- ★**远端核验必做**：main 指向 · 构建号 · README 读数 · 关键改动真在远端 · ★**逐字节同 blob 哈希** · tag/release 是否动过。
